# CLAUDE.md — Twilight of the World (Сумерки мира)

> Этот файл Claude Code читает автоматически в начале каждой сессии. Здесь —
> описание проекта, карта файлов и правила кодинга. **Держи его в актуальном
> состоянии: меняешь структуру — правишь этот файл.**

## 1. Проект

**Twilight of the World (Сумерки мира)** — roguelite-копалка, вид сбоку, с
гравитацией. Игрок-ИИ через
базу-принтер управляет ботами-бурами: бот выходит на поверхность, копает вглубь,
добывает ресурс, возвращается. Дизайн-источник — `gdd_mvp_v0.3.md` (этап M0+).

**Стек:** чистый HTML + JS + Canvas 2D. **Без npm, без сборщиков, без фреймворков.**
Открывается двойным кликом по `index.html`. Главный цикл — `requestAnimationFrame`.

**Что уже есть:** тайловый мир со слоями/твёрдостью, юнит-Tachikoma (ход/лазанье/
гравитация/клинг к породе/авто-копание), энергия выводится из модульной сборки
(«ядро»), гекс-конструктор модулей, меню/пауза, сохранение лучших метрик в
localStorage.

## 2. Запуск и проверка

- **Запуск:** `index.html` двойным кликом, либо статик-сервер из
  `.claude/launch.json` (порт 8123).
- **После КАЖДОГО изменения проверяй в браузере** (preview): скриншот ключевых
  экранов + `preview_console_logs` (ошибок быть не должно).
- **rAF в превью тормозит** между «тихими» eval-вызовами (фоновая вкладка). Чтобы
  проверить логику детерминированно — шагай симуляцию вручную:
  ```js
  // пример: подержать "вниз" 2 секунды
  game.input.keys = new Set(['KeyS']);
  for (let i=0;i<120;i++) game.unit.update(1/60, game.input, game.world);
  game.input.keys = new Set();
  ```
  Либо форсни кадр скриншотом (он будит цикл).
- **Окно превью иногда схлопывается в 1px** (глюк харнесса) — чини
  `preview_resize` (например 980×1000), затем `game.resize()`.
- **`?v=Date.now()` бьёт кэш только у JS-файлов, НЕ у самого `index.html`.**
  Добавил новый файл в загрузчик (`index.html`) — браузер может отдать СТАРЫЙ
  `index.html` из кэша (без нового `<script>`), и функции файла будут `undefined`
  БЕЗ ошибки в консоли. Симптом: `typeof drawFoo === 'undefined'`, а в
  `[...document.scripts].map(s=>s.src)` нет твоего файла. Лечение: перезагрузка с
  cache-bust на сам html — `location.href='/index.html?cb='+Date.now()`.
- `window.game` — точка входа в состояние для отладки.

## 3. Файлы

| Файл | Ответственность |
|---|---|
| `index.html` | Разметка, canvas, **лоадер-оверлей** (`#loader` + прогресс-бар, палитра PAL: void-фон, gold-заливка), **загрузчик скриптов** (массив имён, `?v=Date.now()` cache-busting; порядок = порядок массива). Модули грузятся ПАРАЛЛЕЛЬНО через `createElement('script')` + `s.async=false` (выполнение строго в порядке вставки) — НЕ `document.write` (тот блокировал перерисовку, бар не анимировался). Каждый `onload` двигает бар (`loaded/total`); по завершении `finish()` гасит оверлей и зовёт `window.bootGame()`. **`window.game` занят браузером** как `<canvas id="game">` (именованный глобал) — поэтому `bootGame` идемпотентен по флагу `window._gameBooted`, а НЕ по `!window.game` (тот всегда истинен). |
| `style.css` | Полноэкранный canvas, тёмный фон. |
| `constants.js` | Общие константы мира/юнита, типы тайлов (`AIR/ROCK/BORDER/INDESTRUCT` — последний не копается), `isSolid`, `RESOURCE_DEFS` (железо/органика/кристалл: цвет, `tough`-множитель порога копания), `digThreshold(t)`, `DRILL_HOLD_ADVANCE` (после пробития: въезд в дыру тем же удержанием только спустя это время → разовое нажатие = стой, см. `unit.js`), `HULL_DEFS` (корпус: `hp` + список слотов: `['drill','engine','scanner','cargo']`), `MODULE_DEFS` (по одному модулю каждой категории: `drill` digMult, `engine` speed, `scanner` scanR, `cargo` capacity). `SESSION_GOALS` (3 цели-директивы сессии: `id`/`accent`(ключ PAL)/`short`(компакт для HUD)/`text`(директива); пока СТАТИЧНЫ — отображение на главном меню `drawDirectives` крупно + в игре компактно справа-под-виджетом-города; задел под прогресс/лог/банк данных). `SERVER_COUNT`/`SCAN_RADIUS`/`SCAN_TIME` (старые серверы — источники данных: число серверов, радиус скана в ТАЙЛАХ, секунды до полного извлечения; см. `world.servers`/`render_scan.js`/`game.updateServers`). `SCAN_RING` (`{dx,dy,r}` — геометрия HUD-виджета извлечения в design-координатах; общий якорь для `hud.js` и попапа кодекса `codex_dom`, чтобы диск появлялся РОВНО на месте кольца скана). **Видения в темноте**: `VISION_MIN_GAP`/`VISION_MAX_GAP` (пауза между, редко), `VISION_IDLE_AFTER`/`VISION_IDLE_RATE` (простой юнита ускоряет), `VISION_FADE_IN`/`VISION_LIFE`/`VISION_FADE_OUT`/`VISION_DISSIPATE` (огибающая + мгновенное исчезновение при повороте), `VISION_ALPHA` (пиковая видимость, очень низкая), `VISION_SPEED`/`VISION_MAX` — см. `visions.js`/`render_visions.js`. **Подсказки**: `HINT_DUR`/`HINT_FADE`, `HINT_DEPTHS` (отсечки высоты к поверхности → подсказки при подъёме, директива «зерно истины») — см. `hints.js`. **Пещеры-сцены**: `BACKDROP_COUNT`/`BACKDROP_RX`/`BACKDROP_RY` (размер полостей), `BACKDROP_SWEEP` (сек объёмного скана), `BACKDROP_DATA` (фрагментов за объект) — см. `world.genBackdrops`/`render_backdrop.js`/`game.updateBackdrops`. **Нестабильная порода** (флаг `tile.unstable`): `UNSTABLE_SEED_CHANCE` (шанс «зерна» группы → группы 1-3 тайла, ~20% покрытия, НЕ большие кластеры), `UNSTABLE_FALL_DELAY` (≈0.75с «шатается» перед срывом), `UNSTABLE_GRAVITY`/`UNSTABLE_MAX_FALL` (полёт валуна), `UNSTABLE_DAMAGE_MIN`/`UNSTABLE_DAMAGE_MAX` (урон валуном СЛУЧАЙНО в диапазоне, раз за валун) — визуально как порода + ветвящиеся объёмные трещины, без опоры падает валуном; см. `world.genUnstable`/`falling.js`/`render_falling.js`. **Тяжёлый ВАЛУН** (флаг `tile.boulder`): `BOULDER_SEED_CHANCE` (реже, одиночные тайлы), `BOULDER_HARD` (твёрдость 1.9× — «самая плотная порода слоя»), `BOULDER_DAMAGE_MIN`/`BOULDER_DAMAGE_MAX` (урон больше, чем у нестабильной) — падает как нестабильная, НО при ударе ЗАНИМАЕТ (блокирует) клетку (`world.settleRock`) и ОТТАЛКИВАЕТ юнита (`unit.shove`); визуально — большой одиночный камень на тайл. **Мета-прогресс**: `META_NAME` (МЕГА-ТОКЕНЫ) + `META_ABBR` (МТ) + `META_COEF` (коэффициенты пересчёта забега: `cycle`/`dug`/`resource`/`data`/`directive`) — см. `game.computeMeta`/`drawGameOver`/`save.meta`; дерево апгрейдов — `meta.js` + DOM-оверлей `meta_dom.js`/`meta_dom.css`. `UNIT_DRAW_SCALE` (дефолт-масштаб юнита в мире под тайл-сетку) + `unitDrawScale(unit)` — ЭФФЕКТИВНЫЙ масштаб: `UNIT_DEFS[hull].drawScale` (калибровка тайл-сетки из редактора, `=TILE/tilePx`) ИЛИ дефолт; все потребители отрисовки (кольцо/FK-корпус/ноги/прожектор) берут его → пол/потолок в редакторе реально меняют размер юнита в игре. Энергии/реактора/нагрузки/кожуха больше нет — модели упрощены. Закольцовка: `WORLD_W`, `wrapX`/`wrapPx`/`wrapDeltaPx`. Генерация: `MAP_H=264`. **Нарратив-ось тороида**: ВВЕРХ — погребённая людская цивилизация (тёплые тона), ВНИЗ — «внешняя сторона»/космос (холодные тона к фиолету). Город опущен ГЛУБОКО (`CAVE_Y0=98..CAVE_Y1=105`) → над ним ~95 тайлов страт цивилизации (длинный подъём = директива «зерно истины»). `CRUST_Y0/Y1`/`CRUST_HARD` (корка хлама снизу, y=109-110). **4 СТРАТЫ НАД городом** (`CEIL_BANDS`=[24,50,78] — границы tileY): `пепел`/`ржавчина`/`завал`/`перегной` (плотность разная, читает `world.hardnessForY`/`layerName`) + `CEILING_CRUSTS` (5 плотных «завалов»-барьеров y=12/30/50/70/88, плотнее к поверхности — спрессованные катастрофами руины, поверхность не сразу достижима). Цвета страт — в `render_world._STRATA` (тёплые вверх / холодные вниз). `VOID_SCALE`/`VOID_THRESHOLD` (пустоты, только ниже города), `OTHER_CITIES`/`CITY_MIN_GAP_X` (чужие города), `WILD_NESTS`/`WILD_HP` (дикие гнёзда). **Враги диких гнёзд**: `ENEMY_HP` (прочность врага = 100, задел под перехват/бой; поле `hp/maxHp` + `Enemy.damage(n)`), `DIGGER_MIN_Y` (=`CAVE_Y0-2`: копателям ВЫШЕ нельзя — страты/поверхность для них закрыты) / `DIGGER_MAX_Y` (=`round(MAP_H·0.82)`: глубже «городского диапазона» нет, копатель ниже только поднимается в него) — копатель ищет города ТОЛЬКО в этой полосе горизонтальным свипом-лесенкой (см. `enemy.wanderDir`). Радиация-помехи: `RAD_TOP_Y`/`RAD_BOT_Y` (пороги фона у полюсов) + `RAD_SOURCES`/`RAD_SOURCE_R`/`RAD_SOURCE_BAND`(сдвинут под опущенный город)/`RAD_SOURCE_SPREAD` (локальные очаги у базы, см. `world.radAt`). **Дизайн-система** (см. `design/`): `PAL` — тёплая тёмная палитра (10 нейтралей void→chalk + акценты gold/blood/toxic/amber/cobalt с `*Dim`/`*Bright`); `FONT_DISPLAY` (Tektur), `FONT_MONO` (JetBrains Mono), `FONT_BODY` (IBM Plex Sans Condensed). Новый UI-код берёт цвета/шрифты отсюда; «закон одного пигмента» — один акцент на элемент. |
| `save.js` | `SAVE_KEY`, `loadSave`/`writeSave` (localStorage). Хранит `bestDug` (лучшая проходка — прокопанных тайлов), `runs`, **`meta`** (накопительный банк МЕГА-ТОКЕНОВ), **`metaUnlocks`** (`{nodeId:1}` — открытые узлы СЕТИ ПАМЯТИ) и **`codex`** (`{frags,restored,found}` — прогресс дисков КОДЕКСА + найденный глоссарий, см. `codex_dom.cxPersist`). Межсессионный прогресс. |
| `meta.js` | **СЕТЬ ПАМЯТИ** — данные + граф + состояние (порт структуры дизайна `meta/project/src/tech_web.jsx` 1:1, темы — по списку игрока). `META_SECTORS` (5 секторов: `mast` МАСТЕРСКАЯ/cobalt, `print` ПРИНТЕР-ЦЕХ/amber, `amb` ПОСОЛЬСТВО/gold, `kart` КАРТОГРАФ/crystal, `vault` ХРАНИЛИЩЕ/blood; `accent`/`sys`), `META_CONTENT` (на сектор: `hub`+`m0`/`m1`+`o0`/`o1`/`o2`, каждый `[имя,кратко,icon-ключ,описание]`). `_metaBuildGraph` (полярная геометрия `MW×MH`=2400×2000; рёбра `wire` core→hub→mid→out + кольцевые `ring` хаб↔хаб и out↔out + вершина `cap` ПРОТОКОЛ ВОСХОЖДЕНИЯ, зависит от 3 внешних мастерской) → `META_NODES`(32)/`META_EDGES`(48)/`META_BY_ID`/`META_NEI`/`META_TOTAL`(31). Цены `META_TC`(hub8/mid10/out16, cap36 — ×2), радиусы `META_RADIUS`. Состояние из save: `metaUnlocked`/`_metaDist`(BFS)/`metaAvail`/`metaState`(owned/avail/**visible**/hidden)/`metaCanBuy`/`metaBuy`(списывает МТ,writeSave)/`metaReset`(возврат)/`metaPoweredCount`/`metaDepNames`. Геймплей: `metaBindSave`(конструктор game)+`metaHas(id)`. Эффекты узлов ПОКА не подключены (UI+банк); подключать по `metaHas('id')`. |
| `codex_dom.js` + `codex_dom.css` | **Экран «БАЗА ДАННЫХ» (КОДЕКС) — DOM-оверлей** (HTML-слой поверх canvas; **порт `database/project/src/codex.jsx` на ванилле** 1:1, как мета). `codexDomShow(game)`/`codexDomHide()`/`codexDomBack()` — `<div id="codexScreen">`; содержимое перерисовывается ЦЕЛИКОМ (`cxRender`) при каждом действии, события — делегированием (`cxClick`). Стиль — ацтекский «камень солнца» × киберпанк. **Две вкладки:** (1) **ГЛОССАРИЙ** — журнал находок ИИ (`CXCATS`: серверы/дикие города/спящие города/вражеские юниты/пещеры·культ.слой/останки роботов; `CXENTRIES`): слева список по категориям с циклом/ИД-скана, справа карточка (цикл/ИД/глубина + `ИНТЕРПРЕТАЦИЯ ИИ`-лор). Открыто = есть; неоткрытое отсутствует, данные целые. (2) **ВОССТАНОВЛЕНИЕ** — **мандала-диск кодекса** (`cxMandala` — строка SVG: зубчатый венец-мерлоны `spin-rev`, кольцо-греки `spin-slow`, радиальные секторы заполняются фрагментами `frag/max`, внутреннее глиф-кольцо, центральный Тонатиу + счётчик %): клик по сектору → его «поток данных» в сайдбаре; при 100% — **ЗАПЕЧАТАТЬ ДИСК** (`cxFinalize`: `restored=true`, разблокирует следующий, более сложный кодекс — с **анимацией перехода** `_cxSeal`/`cxSealTick`: фаза A (~620мс) — запечатанный диск «уезжает в слот» восстановленных (transform translate к сайдбару + scale↓ + fade на обёртке `#cxDisc`), мини-диск с маркером появляется в списке; фаза B (~1300мс) — НОВЫЙ диск собирается ПО ЧАСТЯМ через `cxMandala` `anim.build={center,seg,ring}` (0..1, стаггер: центр+Тонатиу+% → секторы → внешний венец+греки, каждая группа с opacity+zoom от центра); уход с вкладки/закрытие завершает мгновенно); восстановленные диски — мини-мандалы с маркером `◆ НОВАЯ ЗАПИСЬ`, клик → **`cxViewerHTML`** модалка с видео-расшифровкой (`decrypt`-текст + слот под клип). **Кнопки «симуляция скана» НЕТ** (фрагменты приходят только из игры). **Кнопка «назад» — лаконичная стрелка** (`#cxBack.cx-back`, бордюр+угловой клип) слева в шапке (+ ESC). **Intro-анимация заполнения ПО ОЧЕРЕДИ**: при открытии вкладки ВОССТАНОВЛЕНИЕ непустые секторы заполняются один за другим (`cxSeqFracs` → массив долей `{segFrac}`, длительность ∝ числу секторов; `cxStartFill`/`cxTickFill` rAF перерисовывает ТОЛЬКО обёртку `#cxDisc`; активный сектор подсвечивается `seg-pulse`+glow; на время анимации кольца-спины «замирают», после — возобновляются). `cxMandala` принимает `anim`: `{segFrac}` (массив долей по секторам — intro по очереди) ИЛИ `{growSeg,growFrac}` (рост одного сектора) ИЛИ `{build:{center,seg,ring}}` (поэтапное проявление при запечатывании: три группы SVG — центр/секторы/внешний венец — оборачиваются `<g opacity transform="scale от центра">`). `CX` — палитра (= `C` из исходника); `CXICON` — SVG-иконки категорий. **Система данных→%** (`CODEX_TIERS`/`cxInitCodices`): диск = сегменты с РАЗНЫМ числом фрагментов (`frags[]`, неравномерно), % = набрано/всего фрагментов; поздние диски КРУПНЕЕ (I 8 сег/24 фр, II 10/38, III 14/64 — прогрессия требуемых данных выражена прямо в геометрии). Один извлечённый сервер = `CODEX_DATA_PER_SCAN`(=1) фрагмент. **Интеграция с игрой**: `codexGainData(n)` (из `game.updateServers` при извлечении) добавляет `n` фрагментов в ТЕКУЩИЙ диск (`cxCurrentIdx`) → `codexPopupShow(r, anchor)` — **НЕ модалка, БЕЗ рамки**, диск появляется РОВНО на месте кольца скана и того же размера (`game._codexAnchor()` из `SCAN_RING`/`coordScale` → CSS right/bottom/size инлайн; `#codexPopup` z-55, `pointer-events:none`). Фазы (`cxPopTick`): (1) мигает завершённое **КОЛЬЦО СКАНА** (`cxLoaderRing`, cobalt 100%, opacity-пульс) → (2) **схлопывание** (scale+fade) → (3) **проявление диска** (expand+fade-in за `appear`≈200мс), ПОТОМ **ЛИНЕЙНО** (не easeOut — иначе изменение «съедалось» в окне проявления и не читалось) растёт фрагмент (`growSeg`/`growFrac`) + **счётчик %** тикает от старого к новому (`before→after`, КРУПНО под диском, без «+N»; на каждой смене цифры — **отбивка** `scale 1.34→1` за 150мс, `_lastPct`/`_popT`); затем hold (~2.2с) и **последовательный fade**: сперва гаснет диск (первая половина окна), потом % (вторая) — `discFadeOp`/`capFadeOp` на детях, а не на контейнере. `disc._mode` гарантирует содержимое в ЛЮБОЙ фазе (фикс «пустого диска» при позднем первом кадре rAF). Попап заменяет HUD-надпись «ДАННЫЕ ИЗВЛЕЧЕНЫ» (`_scanDoneT=0`, если фрагмент добавлен; иначе 2.4с). **Цепочка из 10 дисков** (`CODEX_DEFS`: от простого к сложному, неравномерные сегменты/фрагменты `frags[]` → диски «рваные»; диск открыт, когда предыдущий запечатан — `_cxRelock`). **Персист в `save.codex`** (`codexBindSave` в конструкторе game; `cxInitCodices` гидратит фрагменты/`restored`/найденный глоссарий; `cxPersist` на каждое изменение → `writeSave`). **Глоссарий — по находкам**: записи открываются при ПЕРВОЙ встрече объекта (`codexDiscover(id)`/`codexDiscoverCat(cat)` из `game.checkDiscoveries`), показываются только найденные (`_cxFound`, пустое состояние «Журнал пуст»). Видео-расшифровка — заглушка. ESC закрывает модалку, затем экран (`_cxKey`); «← назад» → `codexDomBack` → `game.mode='menu'`. CSS — токены + анимации (`codex-rot`/`spin-*`/`seg-pulse`/`newdot`/`scanlines`), общие для `#codexScreen` и `#codexPopup`. Открытие — кнопка «База данных» на главном меню (`game.doMenuAction('database')` → `mode='database'` + `codexDomShow`; canvas под оверлеем просто тёмный, ветка рисовки общая с `progress`). |
| `meta_dom.js` + `meta_dom.css` | **Экран «СЕТЬ ПАМЯТИ» — DOM-оверлей** (HTML-слой поверх canvas; **порт `tech_web.jsx` на ванилле**, не приближение). `metaDomShow(game)`/`metaDomHide()` — `<div id="metaScreen">` (строится раз). Pan/zoom холста (`_mtView` translate+scale, `mtFit`/`_mtZoom`, wheel/pointer). `_mtWorldHTML` рисует **SVG**: grid-паттерн + концентрические пунктир-кольца + **рёбра октилинейными лентами** (`_elbow`/`_routeOcti`/`_bundle` — 45°-изломы, 2–3 параллельные дорожки, паяльная подложка `#0b0807`, у запитанных — `.pcb-flow` бегущий ток + via-ромб) + via-пады; **узлы** — `<div>` ромб rotate(45°) с чип-рамкой, 4 паяльными пинами, иконкой (`MICON`, currentColor), подписью; 4 состояния (owned/avail/visible/hidden), selection-ring. Инфо-карта 340px (`mtRenderCard`): иконка-клип, sys-тег, статус, ОПИСАНИЕ, ТРЕБУЕТ(`metaDepNames`), СТОИМОСТЬ + кнопка `ЗАПИТАТЬ` — покупка **УДЕРЖАНИЕМ** (pointerdown → rAF-заливка `#mtBuyFill` слева-направо ~620мс → `metaBuy`; pointerup/leave отменяет). Низ-лево прогресс+легенда(4), низ-право zoom, СБРОС → **модалка подтверждения** (`#mtModal`: ВНИМАНИЕ + ОТМЕНА/СБРОСИТЬ → `metaReset`). **Кнопка «назад» — лаконичная стрелка** (`#mtBack.mt-back`) слева в шапке; ESC/стрелка → `metaDomBack`. CSS — токены `meta/project/tokens.css` + `.vp`/`.pcb-flow`/scanlines. **`render_meta.js` суперседед, НЕ подключён.** |
| `world.js` | `World(seed)` — карта, тайлы, слои, генерация из seed (разнообразие за сессию), туман войны (`seen`/`reveal`). Мир закольцован по X (тор): `tileAt/isSeen/reveal` wrap через `wrapX`, боковых границ нет (только дно). `rand` (mulberry32) — размещение пещер; `hash` (с seed) и `pnoise` (периодический value-noise, бесшовный по кольцу) — плотность зонами (`tile.dens`→`hardness`), природные пустоты ТОЛЬКО ниже города (`y > CAVE_FLOOR_Y`, порог `VOID_THRESHOLD` — над городом коридоров-шорткатов через крусты нет), радиационный фон `poleRad(y)`. Спецслои: корка спрессованного хлама `CRUST_Y0..Y1` снизу + «кровли» `CEILING_CRUSTS` над городом (барьеры на пути к поверхности; `layerName` → `кровля`). **`hardnessForY(y)` и `layerName(y)` описывают слои И НАД городом** (страты погребённой цивилизации `пепел`/`ржавчина`/`завал`/`перегной` по `CEIL_BANDS`), и под (`верхний`/`средний`/`глубокий`). Неразрушимый фундамент (`INDESTRUCT`, `layFoundation`) только под самим городом (стартовый — под принтером+спавном; чужие — под маркером), остальной пол пещеры копается. Чужие города (дружественные): `genCaverns`→`this.caverns` (эллипс-пещеры, `name`/`rep` — задел под задания, фундамент под маркером). Дикие города-гнёзда: `genWilds`→`this.wilds` (глубже, `hp` для рейда, без фундамента; источники волн; копят `loot` от собирателей и `charge` от разведчиков). **Пещеры-СЦЕНЫ**: `genBackdrops`→`this.backdrops` = `BACKDROP_COUNT` больших эллипс-полостей ниже города + **1 ТЕСТОВАЯ у базы** (`startCx+16`, чтобы быстро докопаться; убрать перед релизом) (`{cx,cy,rx,ry,kind,seed,scanned,scanning,sweepT,reveal}`, `kind` city/machine/idol; разнос по тору) — карта-фон с «космическим объектом», вход юнита включает объёмный скан (`game.updateBackdrops`→извлечение данных в кодекс), рендер — `render_backdrop.js`; в `generate` вырезаются как AIR (`inBackdrop`), исключены из спавна серверов. Размещение — `placeNest` (бэнды глубины ∝ `MAP_H`, разнос по тору 2D). `inCavern`/`inWild`/`inBackdrop`/`inEllipseList`. Слои/радиация масштабируются с `MAP_H`. Карта 144×264 (тор по X); бэнды городов/гнёзд/серверов сдвинуты НИЖЕ опущенного города (≥~0.45·H). `tile.resource`/`resourceFor`. `setAir` ставит `tile.dug=true` (прорытый ход ≠ природная пустота — враги избегают своих ходов); если у тайла был `tile.server` — ставит `server.dug=true` (сервер откопан → стал «хламом», включает сканер); и кладёт клетку СВЕРХУ в `unstableTriggers`, если она нестабильна (потеряла опору → срыв). **Нестабильная порода**: `genUnstable` метит ROCK ниже города флагом `tile.unstable` МЕЛКИМИ группами по 1-3 соседних тайла (зерно по `hash<UNSTABLE_SEED_CHANCE`, размер 1..3, анти-слипание по левому соседу → ~20% покрытия, не крустa/сервер, ТОЛЬКО где снизу опора на старте) + тест-группа у базы; `unstableTriggers` (очередь «потеряла опору», `setAir` кладёт клетку сверху если она `unstable` ИЛИ `boulder`) читает `falling.js` (нестабильная при ударе разбивается на осколки и пропадает). **Тяжёлые ВАЛУНЫ**: тот же `genUnstable` метит одиночные ROCK флагом `tile.boulder` (реже, `hash<BOULDER_SEED_CHANCE`, твёрдость `×BOULDER_HARD`) + тест-пара у базы; `settleRock(x,y,hardness,resource,boulder)` — валун приземлился → клетка снова ПОРОДА (блокирует ход). **Старые серверы** (источники данных): `genServers`→`this.servers` = `SERVER_COUNT` объектов `{tx,ty,dug,data,done}` в ПОРОДЕ (бэнды глубины ∝ MAP_H, не в кавернах/гнёздах/у старта/крусте, разнос по тору) + **1 ТЕСТОВЫЙ сервер у базы** (`startCx+4`, `CAVE_FLOOR_Y+3` — пара тайлов вниз от пола стартовой пещеры, чтобы быстро проверить dig→хлам→скан; убрать перед релизом); в `generate` каждый кладётся как ROCK-тайл чуть твёрже фона + обратная ссылка `tile.server`. Скан/прогресс — в `game.updateServers`, рендер — `render_scan.js`. **Радиация** (НЕ урон, а интенсивность помех интерфейсу): `poleRad(y)` (0..1) — фон у «полюсов» (растёт к поверхности `y<RAD_TOP_Y` и в глубину `y>RAD_BOT_Y`; середина тихая); `genRadSources`→`this.radSources` — `RAD_SOURCES` локальных очагов НЕДАЛЕКО ОТ БАЗЫ (разброс по X от спавна `RAD_SOURCE_SPREAD`, глубина `RAD_SOURCE_BAND`; один обычно прямо под базой); `radAt(x,y)` = max(полюса, вклад очагов: в центре 1, линейно к 0 на радиусе `RAD_SOURCE_R`=20 — «гейгер»). game берёт `radAt(unit.tileX,unit.tileY)`. Очаги невидимы в мире (в дебаге — ☢-маркер + кольцо). Эффект помех — `render_glitch.js`. |
| `falling.js` | `FallingRocks` — нестабильная порода → падающие валуны (логика; рендер — `render_falling.js`). `update(dt,world,unit)`: (1) тянет `world.unstableTriggers` → клетки в фазу «дрожит» (`tile.shaking=true`, телеграф); (2) по `UNSTABLE_FALL_DELAY` — срыв: если опоры всё ещё нет, `world.setAir` (цепочка вверх) + спавн валуна `{tx,py,vy,hardness,resource}`; (3) полёт: гравитация `UNSTABLE_GRAVITY`/`UNSTABLE_MAX_FALL`, ищет первую твёрдую породу снизу, по дороге БЬЁТ юнита (раз за валун — `hp<=0`→ gameover уже в `game`), при ударе: **нестабильная** — `_shatter` (осколки в `this.debris`) и ПРОПАДАЕТ; **валун** (`b.boulder`) — `world.settleRock` ЗАНИМАЕТ клетку + бьёт сильнее (`BOULDER_DAMAGE`) + `unit.shove` отталкивает юнита на соседний свободный тайл. Блок несёт `boulder/hardness/resource` (снято с тайла до `setAir`). `debris` — осколки (лёгкая гравитация + затухание). Создаётся в `startSession`. |
| `visions.js` | `Visions` — редкие призрачные **видения в темноте** (логика; рендер — `render_visions.js`). Грандиозные пугающие силуэты — 3 типа: `monster` (гигант-шагоход) / `landscape` (руины) / `tendrils` (САМОСТОЯТЕЛЬНЫЙ призрак-щупальца, без тела) — **наплывают из-за края экрана со стороны, ПРОТИВОПОЛОЖНОЙ взгляду** (`side = -unit.faceX` — там темно, прожектор светит к `faceX`), медленно дрейфуют внутрь, еле видны, сами растворяются. `update(dt,unit,W,H)`: детект простоя юнита (`idleT`, позиция не меняется и не бурит) → ускоряет таймер; таймер `VISION_MIN_GAP..MAX_GAP` (в простое ×`VISION_IDLE_RATE`) → `_spawn` (тип/сид/сторона/координаты за краем, экранные коорд.); `_step` двигает + огибающая видимости (`VISION_FADE_IN`→`VISION_LIFE`→`VISION_FADE_OUT`, пик `VISION_ALPHA`); **взгляд повернулся В СТОРОНУ видения (`unit.faceX===v.side`) → мгновенное растворение** (`VISION_DISSIPATE`). `VISION_MAX` одновременно (1). Создаётся в `startSession`, апдейтится в playing. |
| `render_visions.js` | `drawVisions(ctx,visions,W,H,t,ux,uy)` — призрачные ХОЛОДНЫЕ силуэты в ЭКРАННЫХ коорд. **Без светлых пятен и тонов** (нейтрально-холодный `_vcold rgba(92,99,108,a)`, без светящихся глаз). **ПЕРФ (важно)**: НЕТ `ctx.filter` (blur в Canvas2D — десятки мс/кадр) и НЕТ офскрин-буфера (render-в-canvas→`drawImage` давал GPU-столл ~30мс на сложной форме). Силуэт рисуется ПРЯМО на canvas (форма ~0.004мс даже при ~12 перерисовках); мягкость — overscale-ореол + 2-tap + edge-fade; сканлайны — кэш-паттерн `_vScanPattern` (1 `fillRect`). CPU ~0.01–0.03мс/кадр на видение. **Призрак-щупальца** (`_vTendrils`, отдельный тип `tendrils`, нужен экранный `ux/uy`): 3 КРУПНЫЕ тёмные волнистые ленты тянутся от тёмного края к юниту и ПРОПАДАЮТ НЕДОТЯНУВШИСЬ (вылет `<1` дистанции + пульс reach 0→max→0 + тающий кончик); самостоятельный призрак, НЕ накладывается на monster/landscape. **Мягкий край**: многоступенчатый радиальный фейд в тьму (нет резкой кромки). **Двухпроходный туман** (`ctx.filter` blur 6px ореол + 1.8px форма → объём, не «схематично»; просветы силуэта сохраняются). **Собственная анимация** (`_drawVision`): дыхание (масштаб от низа), покачивание (sway/bob), редкий горизонтальный «дёрг» сигнала, у фигур — живые конечности/шаг по `t`. **Глитч**: мерцающие горизонтальные срезы-сдвиги + **тёмные глитч-КВАДРАТЫ** (`_glitchBlocks`: «выпадение сигнала» void-блоки + смещённые датамош-куски силуэта, переставляются ~6/с, `_vh` хеш) + тёмные сканлайны + радиальный edge-fade (края тают, нет рамки). Силуэт детерминирован сидом (`_vrng`). **`_visionMonster` — КОЛОССАЛЬНЫЙ ШАГОХОД** (массивный корпус-хулл на длинных раскоряченных суставчатых ногах `_vstride`/`_vseg` — широкие просветы читаются даже размытым; голова-мачта, антенна, манипулятор `_vlimb`); `_visionIdol` — колоссальный ступенчатый идол-тотем (ниши глаз/рта вырезаны `destination-out` на офскрине); `_visionLandscape` — слоистый горизонт руин + конструкция + рваная земля. **Тип `glyphs`** (`_vGlyphs`/`_grecaUnit`) — лента ацтекско-киберпанк меандров, проявляется и БЕЖИТ слева→направо (ряды с разной скоростью = паралакс), с фейдом краёв. Рисуется в `drawScene` ПОСЛЕ тумана/прожектора, ПОД юнитом/валунами; гейт `mode==='playing' && !debug`. |
| `hints.js` + `render_hint.js` | **Крупные сюжетные подсказки.** `Hints` (логика) — одна подсказка за раз (`show(text)`/`update(dt)`/`alpha()` с fade `HINT_FADE`, длительность `HINT_DUR`). `drawBigHint(ctx,hints,W,H)` (рендер) — очень крупный короткий текст (~⅓ высоты, авто-ужатие по ширине), ПОЛУПРОЗРАЧНЫЙ, по центру, с тёмной тенью; НЕ HUD. Триггеры (в `game`): первая встреча объекта (через `discover`) и подъём к поверхности (`HINT_DEPTHS`, путь наверх = директива «зерно истины»). Рисуется в `drawScene` после города; гейт playing/!debug. |
| `render_backdrop.js` | **Фон пещер-сцен** (генерация — `world.backdrops`; вход/скан — `game.updateBackdrops`). `drawBackdrops(ctx,world,unit,camera,W,H)` — сцена рисуется ПОВЕРХ тумана, но КЛИПИТСЯ по `clipVisibleAir` И по ЭЛЛИПСУ полости → вписывается в любую форму пещеры. **Главный объект — РАСТРОВЫЙ ассет** `assets/junk/old_robot.png` (`_bdRobot`, грузится `new Image()` → работает и на `file://`; `_bdDrawRobot` вписывает по ширине пещеры с сохранением пропорций, низ — чуть НИЖЕ самой нижней точки полости, т.е. объект «стоит» на дне; клип эллипсом обрезает низ/края); пока картинка не загрузилась — фолбэк на процедурный объект по `b.kind` (`_bdKind`: city/machine/idol). **Паралакс**: слои `sky`(дальний) + объект смещаются на разную долю смещения юнита от центра полости (передний `ground`-слой убран — давал тёмное пятно поверх ассета). **REVEAL** (`b.reveal` 0..1) — объёмный сканер: сцена проявляется по ходу свипа; `_bdSweep` — вертикальная полоса свипа слева→направо, **МАСКИРОВАНА по ассету** (не по пустому воздуху пещеры): в полосе свипа ассет перерисовывается в режиме `lighter` с клипом по той же полосе — подсвечиваются ТОЛЬКО непрозрачные пиксели объекта (`b._astRect` — точный прямоуг. отрисовки, стэшится `_bdDrawRobot` рядом с центром `b._astX/_astY/_astR`); фолбэк (картинка не готова) — старая cobalt-полоса, клипнутая эллипсом полости. **Конус сканера к объекту**: `render_scan.drawBackdropScan` рисует луч/конус ОТ модуля сканера юнита К ассету пещеры (общий `drawScanBeam` с серверным сканом), пока `b.scanning` — видно, что юнит сканирует пещеру. `_bdRng`/`_bdrr` — сид-рандом. Размер полостей уменьшен вдвое (`BACKDROP_RX`/`RY`). Гейт `!debug`. |
| `input.js` | `Input` — held-state по `e.code`. |
| `unit.js` | `Unit` — движение, прерываемое бурение (копит `tile.dig`, порог через `digThreshold`), `hp`/`maxHp` (из корпуса). **⚠️ При пробитии тайла юнит НЕ двигается — РЕШЕНО С ИГРОКОМ, НЕ ЛОМАТЬ** (не добавлять `startMove`/движение в drill-блок на кадре пробития, ни для какого направления): юнит остаётся на месте, избыток мощности бура над остатком твёрдости отбрасывается. **«Замок» прокопанного тайла (`_dugBlock`):** въезд в ТОЛЬКО ЧТО пробитый тайл ТЕМ ЖЕ удержанием подавляется, пока его не держат дольше `DRILL_HOLD_ADVANCE`≈0.26с → **разовое нажатие (пробил → отпустил) = юнит СТОИТ** (а НЕ въезжает в дыру), **удержание дольше = непрерывный тоннель** (бурит и продвигается, `effectiveSpeed`, контролируемо — бур держит стенки, поэтому бур ВНИЗ через породу = плавный спуск, не падение). Замок снимается при отпускании направления. Обычная ходьба в ОТКРЫТЫЙ воздух (не свежепрокопанный тайл) — без замка. Если опоры рядом нет (шахта) — гравитация. Авто-въезд в дыру на разовом нажатии — баг, был убран; следи, чтобы не вернулся. Энергии/расхода нет: модули дают абсолютные статы. Движение — только с ОПОРЫ (клинг к соседней породе): любой ход/лазанье/прыжок в воздух требует `anchored`, иначе действует гравитация — в свободном падении управление не действует (зажатые клавиши отделены от физики, «полетать» вбок нельзя). Подъём по «вверх»: явный «вверх+вбок» → диагональ на уступ; «вверх» в воздух с опорой наверху → лезет по шахте/стене (без приседа); если прямо вверх не зацепиться, но сбоку ЕДИНСТВЕННЫЙ уступ — авто-цепляние к нему (одной кнопкой «вверх»); и только настоящий прыжок в ОТКРЫТЫЙ воздух даёт присед `crouchT` (`JUMP_CROUCH_T`) + замедленный прыжок (`JUMP_SPEED_FRAC`) — ощущение веса. `anchoredAt` — только соседняя порода (искусственных полов нет); пол стартовой пещеры держится за гарантированный породный грунт под ним. **Падение НЕПРЕРЫВНОЕ**: при завершении тайла в свободном падении (`moveSpeed===FALL_SPEED`, `!anchored`, снизу воздух) тайлы цепляются в ОДНОМ кадре с переносом остатка `progress` (без кадра-заморозки на стыке) — иначе `py` замирал на 1 кадр → скачок скорости → ложный «удар» squash на КАЖДОМ тайле (дёргано). `unit.dug` — событие «выкопан ресурсный тайл»; `unit.broke` — событие «прокопан ЛЮБОЙ тайл» (game считает проходку). `effectiveSpeed()` = `stats.moveSpeed` (замедления от груза больше нет). `faceX` — последний ГОРИЗОНТАЛЬНЫЙ взгляд (для рига). `shove(nx,ny)` — мгновенный перенос в клетку со сбросом движения/бура (толчок валуном, см. `falling.js`). |
| `camera.js` | `Camera` — тороидальное следование (мир закольцован по X): `x` в `[0,WORLD_W)`, `follow` идёт кратчайшим путём через шов, `screenX(px)` даёт экранную X сущности (ближайшая копия по кольцу). По Y — клампы. |
| `city.js` | `City` — таймер гибернации города и кольца HP. `timerMax`/`ringMax` АПГРЕЙДЯТСЯ в сессии (`applyUpgrades(timerBonus, contourBonus)` — апгрейды города «батареи»/«контуры»; живые кольца дозаряжаются до нового макс.). Реактор города в юните → вне базы город обесточивается; на базе `update` дозаряжает таймер и ставит `charging` (растёт) / `full` (полон). Кольца гаснут снаружи внутрь → гибернация (`dead`) = конец. `damage(amount)` — урон по ТЕКУЩЕМУ hp кольца (база его лечит). **`drain(amount)` — ПЕРМАНЕНТНЫЙ дренаж рейдером**: срезает МАКС активного кольца навсегда (база восстанавливает hp лишь до уменьшенного макс.) → набеги НАКАПЛИВАЮТСЯ и реально рушат город (кольцо с нулевым макс. потеряно безвозвратно). Раньше рейдер звал `damage()`, и набег «не чувствовался» (база лечила). Враги МОГУТ уничтожить город одними набегами (даже без гибернации по таймеру). |
| `enemy.js` | `Enemy` — враг диких гнёзд. **`hp/maxHp`** (= `ENEMY_HP` 100) + `damage(n)` (задел под перехват/бой). Локомоция как у юнита: клинг к породе, гравитация (без опоры и снизу воздух — падает), копание настоящих тоннелей; в воздух — только при опоре назначения (в открытом пространстве ползёт к дальней стенке и копает там). `passable` запрещает КОПАТЕЛЮ заходить выше `DIGGER_MIN_Y` (страты/поверхность для него закрыты). **`scan`/`scanned`** — прогресс скана врага игроком (см. `game.updateEnemyScan` → данные кодекса). Поведение задаёт `game`: `seek` / `goto` цель (`return` в гнездо, `goresource`). **`wanderDir` КОПАТЕЛЯ — КРУТОЙ подъём к потолку диапазона + горизонтальный свип у потолка**: ниже `DIGGER_MAX_Y` — поднимается; в диапазоне — почти ВЕРТИКАЛЬНО к `DIGGER_MIN_Y` (vy-bias ≈ −1.1; путь короче по тайлам → до базы у потолка быстро и со СТАБИЛЬНЫМ временем, ~целевое окно), у потолка (`atTop`) — почти ВБОК (свип по долготам, `sweepSign` обходит тор → база на любом X). Тюнинг времени до базы — этим vy-bias (НЕ скоростью бурения): круче = быстрее/уже разброс, положе → у ~−1.0 ОБРЫВ в большой разброс (копатель залипает в середине). `sweepSign` задаётся в `ai.onCycleStart` ЧЕРЕДУЯСЬ (копатели делят тор). Ранг копателя = `[-heading, старый-ход]` (heading ГЛАВНЫЙ; `rank()` не даёт развернуться назад). Прочие враги — прежний flow-вандер (`[вдоль-корки, посещён, старый-ход, -heading, воздух]`, тяга вверх `UP_BIAS`). `DETECT_CITY_PAD=6` (радиус обнаружения базы → 14). `homeR` (= размер каверны гнезда) — радиус «дома» для despawn (центр-воздух недостижим). Копатель **не подбирает ресурс**: роет как обычно, но прокопанная жила не пропадает — остаётся на месте лутом (`enemy.dug`→`game` зовёт `loot.spawn`). Типы: **копатель** (разведчик ищет города; радиус обнаружения — на город, покрывает каверну + запас), **собиратель** (1 ресурс) и **разведчик** (`raider`, фаза 5: `RAIDER_SPEED`=5, **НЕ копает**, лёгкий — **летит** по воздуху без гравитации/клинга; бежит по тоннелям копателей к базе, у города ВСТАЁТ и «заполняется» `RAID_DRAIN_TIME`≈2.6с (`draining`/`drainT` — кража НЕ мгновенная, видно подход+накопление; `enemy.update` при `draining` стоит на месте), затем высасывает контур и несёт заряд `carry='charge'` домой — навигация/урон/таймер дренажа в `game.raiderBrain(e,dt)`). `this.speed` per-type. |
| `render_enemy.js` | `drawEnemies` — дроны врагов: красный «глаз» (копатель) / зелёный (собиратель), бур по направлению, несомый ресурс над собирателем; `drawRaider` — разведчик: красный угловатый ромб с глазом; при `draining` у города — растущее кольцо заряда + пульсирующее ядро (`drainT/RAID_DRAIN_TIME` — видно «заполнение»); светящийся заряд энергии над ним при возврате (`e.carry`). |
| `cycle.js` | `Cycle` — внутренний таймер-фаза мира (`CYCLE_TIME`≈75с): макро-эскалация, тикает реальным временем в `playing`. Волны диких городов — будущие фазы (см. `design_cycles.md`). |
| `intro.js` | `Intro` — заставка старта забега (таймер `INTRO_*`): печать корпуса → установка реактора → синхронизация. Пропуск — Пробел. |
| `unitdefs.js` | `UNIT_DEFS` — блупринты юнитов (визуальная сборка, **шаблон-слоты по корпусу**). Ноги с `ik:true` — щупальца (рендер через `legik.js`, FK-поза игнорируется; у scout 4 ik-ноги по умолчанию). `scout` экспортирован из `tools/rig_editor.html` (`assets/scout/scout-rig.json`); спрайты деталей — в `render_parts.js` (`PART_SPRITE_SRC`, по id детали/сегмента). Деталь: слот в facing-локали (`f` вперёд / `s` вбок, доли R), `z` (порядок), `need` (условие присутствия из `unit.stats` через `unitHasPart` в `rig.js`: `dig`→`canDig`, `engine`→`canMove`, `scanner`→`scanR>0`, `cargo`→`capacity>0`, `shield`→`radResist>0`; reactor — декор без `need`). Без флага модуля деталь НЕ попадает в `rig.parts` и не рисуется (даже со спрайтом) — в редакторе все флаги вкл. по умолчанию, плюс тумблеры в секции «Модули (отсоединение)». `proc` (рисовать ли процедуру). Виды деталей (`kind`): reactor/hold/sensor/drill/**engine**/**leg**. Нога несёт **`segments`** (бедро→голень→стопа), `cables`. Расширяемо. Только данные. |
| `anim.js` | `ANIM` — аниматор рига: ЧИСТЫЕ функции времени → смещения узлов, каналы независимы (idle-`bob`, `reactorPulse`, `drillWob`). `unit.noAnim` (статика) → bob/drillWob = 0 (юнит стоит ровно). Без Canvas. |
| `rig.js` | `resolveUnitRig(cx,cy,unit,t)` — блупринт + аниматор → детали в ЭКРАННЫХ коорд. (по `z`), кабели, узел ног. Деталь без `need` пропускается. **Вся масса вдоль оси ВЗГЛЯДА** (`dx/dy`): трюм (f<0) сзади → бур (f>0) впереди. **Один кадр для ВСЕХ деталей**: лево/право = ЗЕРКАЛО (по `unit.faceX`), вверх/вниз = ПОВОРОТ ±90° (реактор/трюм/оружие крутятся, не только бур). Базис массы выводится из (flip,fwdAngle): при ЗЕРКАЛЕ «низ» (s) ОСТАЁТСЯ внизу (не уезжает вверх). **Двигатель — масса** (крутится, под реактором). **Ноги** (kind:leg) — отдельный выход `rig.legs`, несут ГЛОБАЛЬНЫЙ `z`. `supportDirOf(world,unit)` — общий помощник (пол снизу→`down`, иначе существующая боковая стенка). `resolveUnitRig(cx,cy,unit,t,supportDir)`: базис НОГ от опоры (LD «низ» к опоре, LF продольная ось), `resolveLeg` хип=центр+f·LF+s·LD, доворот `legRot` (в `drawLeg` ДО зеркала). NB: в игре `drawTachikoma` ВСЕГДА зовёт риг горизонтально (`supportDir='down'`, dy=0) и сам поворачивает юнит целиком при лазании — так масса и ноги не расходятся; left/right-ветки базиса ног остаются для прямых вызовов/редактора. `legHub` — пивот приседа. |
| `loot.js` | `Drop` (выпавший ресурс: гравитация по тайлам) + `Loot` (`spawn` ресурса, `update`). Модули во время игры не выпадают/не подбираются (сборка только перед стартом). Ресурс лежит `PICKUP_ARM` (1с) → подхват в радиусе `PICKUP_R`=1, втягивается за `SUCK_TIME` (≈0.55с): дроп едет от старта (`d.sx/sy`) к юниту (`d.ux/uy` — для рисовки лап-граберов). При успехе `inventory.addCargo(type)` (счётчик до `cargoCapacity()`). **Трюм полон** (`inv.cargoFree()<=0`) → подбор НЕ происходит, дроп остаётся лежать (юнит не пытается; заберётся после сдачи). |
| `effects.js` | `Fx` — лёгкие визуальные частицы (логика, не влияет на игру). `burst(px,py,types)` — единицы ресурса вылетают из юнита вверх и тают (сдача груза). |
| `inventory.js` | `Inventory` — сборка юнита ПЕРЕД забегом. Дефолтный корпус — `core` (кольцо). Слева **живой рендер собранного юнита** с idle-анимацией (тот же риг со спрайтами, что в игре: `resolveUnitRig`+`drawTachikoma`, а для `kind:'ring'` — **IK-щупальца** (`_previewLegRig`: `makeLegRig`+`updateLegRig` на ФЕЙКОВОМ полу, как в игре — не старые FK-ноги; пол на ~60% вылета ноги — вытянуты, не поджаты; **ФИКС. dt=1/120** в апдейте (не реальный «гуляющий» dt рендера — тот давал дрожь сглаживания ног, ВИДНУЮ на крупном масштабе панели; 1/120 вместо 1/60 — меньше `smk=dt*30` → мягче лерп draw-точек, гасит остаточное дрожание длинных правых ног при bursty-рендере; idle-«дыхание» внутри legik всё равно по реальному времени, движение живое) ПОД + `drawRingUnit` с `dx/dy=bodyOff` (корпус едет на ногах); `blueprintScale` вписывает крупно по габаритам — для кольца по выносу модулей `rad`, т.к. `rig.parts` тут NaN; кольца гнёзд для ring не накладываются — модули расставляются в рег-редакторе; кольца гнёзд резолвятся на том же `_rigTime()`, чтобы следовать за дыханием). Гнёзда-кольца (`SLOT_META`: category→kind детали) наложены НА реальные детали (позиция из рига → всегда совпадает): установленный — тонкое кольцо cobalt, пустой — пунктир amber, при перетаскивании подходящей карточки — подсветка gold. Подписи — фоновые **выноски** (`_drawCallout`). Справа список модулей: вертикальный стек категорий, каждая — ГОРИЗОНТАЛЬНАЯ галерея высоких узких карточек (сверху — спрайт-ассет модуля из `PART_SPRITES`, фолбэк на иконку; имя + стат; «✓ УСТАНОВЛЕН»), скролл колесом (`onWheel`/`scrollY`/`maxScroll`, клип + индикатор). Drag карточки на гнездо своей категории МЕНЯЕТ установленный модуль (полезно при >1 варианте). **По умолчанию юнит СОБРАН** (`defaultBuild` ставит первый модуль каждой категории); `getStats().valid` = все слоты заняты. `modules: {category → moduleType}`. `getStats()` собирает `maxHp` из корпуса, `moveSpeed/digMult/scanR/capacity` из модулей. Груз — счётчики `cargo: {iron, organic, crystal}`: `addCargo`/`cargoUsed`/`cargoCapacity`/`cargoCounts`/`deliverOneCargo`/`resetCargo`/`reset`. **Единый источник эффективных статов в забеге — `unit.stats`** (апгрейды пересобирают его через `game.onChange`→`unit.setStats(upgrades.applyToStats())`; все потребители — движение/бур/сканер/HP/ГРУЗ — читают `unit.stats`, отдельных кэшей нет). `cargoCapacity()` = `this.unit.stats.capacity` если задан `inventory.unit` (ссылка на юнита забега, ставится в `startSession`), иначе `getStats().capacity` (база — для ЭКРАНА СБОРКИ); `reset()` сбрасывает `unit`. Новый стат на будущее: добавить в `MODULE_DEFS`/`getStats` (база) + `UPG_TRACKS` (бонус), потребитель читает `unit.stats.X` — больше ничего прокидывать не нужно. Открывается ТОЛЬКО перед забегом; во время игры модули не снимаются. |
| `upgrades.js` | `Upgrades` — внутрисессионные апгрейды (структура Dome Keeper), открываются на базе по **Пробелу** (`game` режим `upgrades`). Валюта — **банк** сданных ресурсов (`bank`, копится в `deliverCargo`, обнуляется за забег). Набор определяется на СТАРТЕ (`init(baseStats, cityName)`): треки ЮНИТА по установленным модулям (`UPG_TRACKS` с `need(stats)`: бур→`digMult`, привод→`moveSpeed`, сенсор→`scanR`, трюм→`capacity`, корпус→`maxHp`) + треки ГОРОДА (его системы: «батареи»→`timerMax`, 3 отдельных контура «внешний/внутренний/ядро»→HP конкретного кольца через `cityRingBonuses()`/`city.applyUpgrades`) + ГАДЖЕТЫ (`UPG_GADGETS`: магнит→радиус подбора, ремонт→реген HP вне базы, пинг→вскрыть карту). До `UPG_MAX`=5 ур., цена по `UPG_TIER_COSTS`. **Покупка — УДЕРЖАНИЕ** (`UPG_HOLD_TIME`≈0.55с): зажать ПРОБЕЛ (выбранный трек) или ЛКМ на покупаемой карточке → карточка заполняется градиентом снизу вверх (`holdFrac`), по достижении порога `buyTrack`; отпускание/смена выбора (`moveSel`) отменяют (`beginHold`/`endHold`/`tickHold`, `holdId`/`holdSrc`/`holdT`). **Мышь**: `pointerDown` по карточке выбирает трек (`sel`) + на покупаемой стартует hold; `pointerUp` отменяет недозаполненную (`buttons` — hit-rects ВСЕХ карточек с `trackId`/`level`/`next`/`buyable`). `buyTrack`/`buyGadget` → `spend(bank)` + `onChange(kind,id)` (game пересчитывает `unit.setStats(applyToStats())` и `city.applyUpgrades(...)`). Рендер по дизайн-системе (кошелёк, секции, треки с пипсами/эффектом/ценой; скролл колесом). |
| `render_world.js` | `drawWorld` (порода-«кладка»: тёмный раствор + куски-булыжники размером ∝ плотности; неоднородное дно ходов; рваные края). Цвет базы/булыжников/дна интерполируется НЕПРЕРЫВНО по глубине из `_STRATA` (y-привязанные стопы всей колонки → `_strat(y)` лерпит `backColor`/`gapColor`/`shadeColor`, мемоизация по int y). **Нарратив-ось тороида**: ВВЕРХ — ТЁПЛЫЕ страты погребённой цивилизации (пепел→ржавчина→завал→перегной), город — нейтральная земля, ВНИЗ — ХОЛОДНЫЕ к фиолетовой бездне («внешняя сторона»/космос). Непрерывная интерполяция убирает горизонтальный шов на сетке (прежние `layerFloat`/`_LBAND`/3-стоповые палитры заменены). Трещины по `tile.dig` — паутина на каждом осколке, проступает прогрессивно от стороны входа бура (соседний воздух) вглубь тайла. `raggedTeethRects` — НЕПРЕРЫВНЫЙ рваный контур кромки прохода (БЕЗ ровной линии по сетке): заливает полосу от кромки тайла (утоплена под цвет дна → невидима) внутрь породы до зубчатой линии. Профиль — `_ragDepth(along, side)` в ДОЛЯХ ТАЙЛА: ОСТРЫЕ зубцы (высокочастотный `_ragLin` — value-шум на ЛИНЕЙНОЙ интерполяции, не smoothstep → углы, а не пологие волны; подходит для бурёных тоннелей), мин. глубина убирает плоские участки. Чистая функция мировой координаты вдоль кромки → у соседей бесшовно, на концах тайла глубина не нулевая (нет «защипов»). Тот же `_ragDepth` читает конус света (`render_light._coneSolid`) — клип строго по этому ландшафту. Один контур кормит и видимую кромку (`raggedEdge`), и силуэт тени. `_appendTeeth` — тот же зубчатый контур БЕЗ `beginPath`/`fill` (добавляется в общий путь); `raggedTeethRects` = `beginPath`+`_appendTeeth`+`fill`. `clipVisibleAir(ctx,world,camera)` — клип по ВИДИМОМУ воздуху (прямоуг. воздух-тайлов + эрозийные каймы `_appendTeeth` со стороны воздуха): юнит-щупальца (`game.drawScene`) рисуются только внутри → ЗА породой скрыты, окклюзия по ТОЙ ЖЕ видимой кромке, что и мир. `drawTunnelShadow` — длинная мягкая тень-«труба»: силуэт породы с тем же рваным краем строится на оффскрине (полоса вырезается `destination-out`), затем `shadowBlur` внутрь хода (клип по воздуху) — тень идёт по неровной линии, а не по сетке. `drawBoreMarks`/`boreSide` — следы бура на стенках (дуги-борозды + блик), кроме природной пещеры города. `drawResourceVeins` — вкрапление жилы в породу кластером фигурок по типу (через `drawResourceCluster`, см. `render_resource.js`); центр сдвинут от сетки для органичности (туман сам притеняет невидимое/вне сканера). `drawCityMarkers`/`drawWildMarkers` — маркеры дружественных городов (ступенчатый + имя) и диких гнёзд (разрушенная масса + тлеющее красное ядро); в дебаге показываются без тумана. `drawPlayerCity` — **РАСТРОВЫЙ ассет города игрока** `assets/aztec_city/aztec_city.png` (`_cityImg`, грузится `new Image()` → работает и на `file://`): вписан по ШИРИНЕ фундамента (`(PRINTER.w+2)·TILE` = 5 тайлов), низ на полу базы (`(CAVE_FLOOR_Y+1)·TILE`), центр = центр принтера. **Заменил плейсхолдер-прямоугольник принтера** (его рисовка убрана; коорд. `PRINTER` ещё нужны для `render_intro` и `game.atBase`). `INDESTRUCT` рисуется тёмной плитой-основанием (банты сверху/снизу), без осколков, но даёт силуэт тени. **Трещины нестабильной породы** — `drawUnstableCracks` (из `render_falling.js`) вызывается после жил (поверх кладки): на ROCK-тайлах с `tile.unstable` рисует сеть трещин (детерминированно по координатам; при `tile.shaking` — дрожит + тлеющий блик-телеграф). Рендер закольцован: диапазон X без клампа (lookups wrap), сущности/принтер — через `camera.screenX`. Слой освещения/видимости (туман, прожектор-конус, пыль) вынесен в `render_light.js`. |
| `render_falling.js` | Рендер нестабильной породы и валунов: `drawUnstableCracks` — трещины на `tile.unstable`; `drawBoulders(ctx,world,…)` — большой одиночный КАМЕНЬ (`_bigRock`: неровный многоугольник с объёмным градиентом, гранями, бликом, крапинами; тёплый glow+дрожь при `shaking`) на `tile.boulder`-тайлах; обе зовутся из `drawWorld` (после жил, до тумана). `drawFalling(ctx,mgr,camera)` — летящие: **валун** (`b.boulder`) — `_bigRock`, **нестабильная** — битый блок с трещинами; + трейл движения; + осколки `mgr.debris`; рисуется ПОВЕРХ тумана (опасность всегда видна). `_crackNet` — ВЕТВЯЩИЕСЯ ОБЪЁМНЫЕ трещины СЛУЧАЙНОЙ ориентации (центр у середины тайла + произвольный угол, НЕ только вертикаль): 1-2 главных (профиль ширины тонко→толсто→тонко) + ветви под углом к главной (толсто у основания → нить к кончику), залиты как тапер-полигоны (`_jag` извилистая ломаная + `_taperPoly` лента переменной ширины); при `shaking` — тёплое тление. |
| `render_light.js` | Слой освещения/видимости (вынесен из `render_world.js`). `drawFog` (туман войны + свет мягким градиентом; диапазон с запасом +1 тайл за вьюпортом). `drawHeadlight` — прожектор-конус ОТ РЕАКТОРА (вершина/дрейф через `unitLightAnchor` в `render_unit.js`, конус дышит с idle-анимацией) по направлению взгляда, ОТСЕКАЕТСЯ ПОРОДОЙ по ВИЗУАЛЬНОЙ кромке (не по сетке): `_coneVisibility` пускает веер лучей, глушит их через `_coneSolid` — точка в породе «открыта», если попадает в зону эрозии грани, выходящей в воздух (тот же `_ragDepth` из render_world.js), иначе настоящая порода; бисекция → стоп ровно у рваной поверхности. `BORDER`/`INDESTRUCT` (рукотворное/край) — прямой клип по сетке. **Пенумбра**: ядро резким клипом + резкий радиальный профиль (ярко до 50% длины, затем гаснет), мягкость — только узкий размытый контур-«гало» (`destination-out`). **Ambient-гало** (`HALO`≈1.3 тайла вокруг юнита, без клипа): подсвечивает текущий тайл в тупике, где конус схлопывается. **Пыль в луче** (`_drawDust`/`_dustLayer`): ОТДЕЛЬНЫЕ пылинки (спрайт-точка `_dustDot`), а не тайл-паттерн. Каждая дрейфует МЕДЛЕННО по СЛОЖНОЙ траектории — 2 синусоиды разной частоты/фазы на ось (кривая Лиссажу, направление плавно меняется, без линейного «снега»). Разложены на параллакс-сетке (2 слоя: ближний крупнее/ярче, дальний мельче/медленнее), анкер в МИРЕ через `camera.x*par` (при движении юнита проплывают мимо), плотность гаснет по дистанции (smoothstep-фейд от вершины), клип по конусу, аддитивно, лёгкое мерцание. Вызывается в `game.drawScene` после `drawWorld`, до юнита. |
| `render_scan.js` | **Старые серверы (источники данных) + сканер.** `drawServers(ctx,world,camera,debug)` — для каждого `world.servers`, если тайл `isSeen` (ИЛИ `debug` — в дебаг-обзоре все, без тумана, как маркеры городов/гнёзд): до откопки — `_drawServerMarker` (тех-вкрапление в породе: карбон-блок с рёбрами + мигающий cobalt-LED, намёк «тут данные»), после откопки (`s.dug`) — `_drawJunk` (кучка тех-хлама: обломки/плата/мигающий LED; `done` → тусклее, LED гаснет). В дебаге поверх — `_drawServerDebug` (cobalt-кольцо + метка `СЕРВЕР`/`DATA·NN`/`DATA·OK`, чтобы не путать с железной жилой). Рисуется ПОСЛЕ `drawWorld`, ДО тумана/юнита (туман приглушает невидимое). `drawScanFx(ctx,game,camera)` — киношный сканер, пока `game.activeScan` активен (рисуется ПОСЛЕ юнита, поверх; всё на `lighter`): подсветка САМОГО объекта цветом скана (cobalt-силуэт хлама + пульсирующее гало, «горит» под лучом) + **полупрозрачный объёмный КОНУС** света — апекс РОВНО на модуле сканера (`ringModuleScreenPos(unit,camera,'sensor',bo)`; ищем `kind:'sensor'`, а НЕ `'scanner'` — то лишь флаг-стат `need`, иначе апекс падал в центр юнита) к объекту (раствор `half=atan2(r,dist)` накрывает ±r, гаснет к краю радиальным градиентом) + тусклые статичные лучи-стрики внутри («пыльный свет») + **ЯРКИЕ ТРАССИРУЮЩИЕ лучи ходят ОТ КРАЯ ДО КРАЯ** по углу конуса (главный `baseAng+sin(t·2)·half` + второй в противофазе — НЕ прибиты к центру) + бегущее пятно контакта в точке текущего луча. Все лучи/конус исходят из одной точки — модуля сканера. Конус/лучи вынесены в общий `drawScanBeam(ctx,src,tx,ty,t,r)` (apex `src`=сканер через `_scanSrc`), который переиспользуют **`drawEnemyScanFx(ctx,game,camera)`** — луч/подсветка к сканируемому врагу (`game.scanEnemy`) — и **`drawBackdropScan(ctx,game,camera)`** — луч к ОБЪЕКТУ пещеры-сцены, пока `b.scanning` (привязка к ассету: целимся в верх-центр, `r`∝размеру полости) → видно, что юнит сканирует пещеру. Гейтится `!game.debug`. Прогресс-кольцо извлечения и виджет лога — в `hud.js`; логика (накопление/пауза/done) — `game.updateServers`. |
| `render_resource.js` | Единый язык форм ресурсов: `paintResource` (одна фигура по силуэту: железо — рвано-квадратный обломок с заклёпками/ребром; органика — округлая «клетка»; кристалл — ромб) и `drawResourceCluster` (кластер мелких фигурок для вкрапления в породу, как в Dome Keeper). Общий для жил/дропа/груза. |
| `render_module.js` | `drawModuleIcon(ctx, category, cx, cy, r, color)` — монохромная иконка модуля по категории слота (drill/engine/scanner/cargo): бур/шестерня/арки-сканер/сундук. Только пиктограмма (форм-факторов гексов больше нет). |
| `render_loot.js` | `drawLoot` — выпавшие ресурсы (`paintResource`, ~1 тайл); свечение + сжатие при подборе; `drawGrabLegs` — две сегментные лапы от юнита к ресурсу при втягивании. |
| `render_fx.js` | `drawFx` — частицы `Fx` (фигурка ресурса, тающая по жизни). |
| `render_intro.js` | `drawIntro` — заставка: печать юнита (заливка снизу вверх + сканлайн), влёт реактора из принтера, вспышка установки, этап + подсказка. Плейсхолдер-прямоугольник принтера УБРАН (базу рисует ассет города `render_world.drawPlayerCity`; коорд. `PRINTER` ещё нужны для печати/реактора/`atBase`). **Кольцо** рисуется как в игре (`drawTentacles` ноги ПОД + `drawRingUnit`), а не FK-риг (для ring `drawTachikoma` ничего не рисовал). Реактор ВЫКЛ до установки: `game` intro-апдейт ставит `unit.reactorOn = t >= INTRO_PRINT+INTRO_REACTOR` (фаза печати/влёта → `reactor:off`/приглушённый) + зовёт `updateTentacles` (живые ноги в интро); по входу в `playing` → `reactorOn=true`. |
| `render_parts.js` | Ассеты деталей юнита (**гибрид**). Деталь рисуется в КАНОНИЧЕСКОМ кадре (центр 0,0, вперёд +X); враппер `drawPart(ctx,kind,x,y,S,angle,flip,t,opts)` делает translate→flip→rotate и подменяет процедуру на спрайт из **`PART_SPRITES[opts.id]`** (ассет НА ДЕТАЛЬ, по id). Если спрайта нет и `opts.proc===false` — деталь не рисуется. Процедуры: reactor/hold/sensor/drill/**engine**/**weapon**(ствол+X)/**custom**. `setPartSprite(id,img,scale,offX,offY,rot)`/`clearPartSprite` (`rot` — поворот СПРАЙТА в градусах вокруг пивота, НЕ трогает угол кости/`angle`). `PART_SPRITE_SRC` (keyed by id: url+scale/offX/offY/rot) — авто-загрузка спрайтов в ИГРЕ через `new Image()` (работает и на `file://`, в отличие от fetch JSON). Сейчас заполнен ассетами `assets/scout/*` (метаданные из `scout-rig.json`); пути с пробелом URL-кодированы. **`drawLeg(leg,R)`** — сегментная нога (кадр ноги: хип+зеркало; сегменты по z): сегмент = спрайт (пивот = px/py спрайта, у точки стыка) ИЛИ процедурная капсула пивот→стык. Под-ассет id `legL:thigh`. `drawCable` — кибер-мускул; **ассет-кабель** `PART_SPRITES['cable']` тянется по A→B. |
| `legik.js` | **Общий движок IK-ног** (FABRIK) — ОДИН код для игры и редактора (Вариант 2 «щупальца как настоящие ноги»). Нога из КОНФИГА: `hipOff` (крепление), `dir`, `segLens` (переменные длины px), `spriteIds` (`PART_SPRITES['legId:segId']`). `makeLegRig(configs)`/`updateLegRig(rig,dt,cx,cy,world,vel)`/`drawLegRig(ctx,rig,camera,drawSeg?)`. `legConfigsFromUnit(unit, scale)` строит конфиги из `UNIT_DEFS` ног С ФЛАГОМ `ik` (длины из `segments.len*R`, спрайт на сегмент). **`scale` (= `UNIT_DRAW_SCALE`) ужимает `R` → hip И длины сегментов масштабируются ВМЕСТЕ с корпусом/кольцом**; игра (`tentacles.updateTentacles`) передаёт `UNIT_DRAW_SCALE` (иначе ноги крепились в `1/scale`≈1.6× ДАЛЬШЕ, чем рисуется кольцо — отрыв ног от корпуса), редактор зовёт без `scale` (=1) — там общий `ctx.scale(zoom)`. Спрайты сегментов тоже ужимаются: `makeLegRig(configs, UNIT_DRAW_SCALE)` → `rig.drawScale`, `drawLegRig` делает `ctx.scale(drawScale)` перед `drawImage` (иначе абсолютные `sp.w` не совпадают с укороченной костью). В игре ВКЛ по умолчанию (scout — 4 ik-ноги); в редакторе — IK-превью с фейковым миром. НЕ лезут в породу (`_ikAvoidRock`); ≥`LEGIK_MIN`(2) всегда воткнуты. Ноги РАЗВЕДЕНЫ по секторам (4 ноги: 2 вниз, 2 вверх) → цепляются за РАЗНЫЕ поверхности (пол И потолок/стены), а не все в одну. СТОЯ держат бессрочно все доступные опоры (бракинг в коридоре). **СТОЙКА** `LEGIK_STAND` (доли тайла, ≈0.3): корпус приподнят «вверх ОТ ОПОРЫ» (через `bodyOff`), ноги тянутся ВНИЗ к полу и держат вес — юнит СТОИТ, а не «лежит/летает» на сетке (раньше корпус сидел на полу, ноги расплывались вбок). **«ВЕС»**: вертикальное покачивание `weight` (синусоида ~±4px, чуть тише на ходу) добавлено в `bodyOff.y` ВДОЛЬ оси опоры — и рендер корпуса (`dy`), и КОРНИ ног (`hip=cy+hoy+bodyOff.y`) берут один `bodyOff.y`, стопы прибиты → ноги сгибаются/разгибаются СИНХРОННО с корпусом (вес «продавливает» щупальца). Дёшево (одна синусоида). Собственное «дыхание» рига (`ANIM.bob`) в режиме щупалец ОТКЛЮЧЕНО (`unit.noBob` ставится в `drawTachikoma`/`unitLightAnchor` при `hideLegs`/`debugTentacles`) — иначе корпус качался отдельно от ног. NB: `t`=`performance.now()` — в синхронном eval-цикле НЕ растёт (bob «замирает»), мерить bob можно только в живом rAF. **СТАНЦ-СЛОТЫ** (idle): каждой ноге в `makeLegRig` дан `stance` (слот вдоль опоры, [-0.55..0.55] тайла, сортировка по `hipOff.x`) и `up` (верхний сектор). Стоя нога целится в свой слот (верхние — к потолку, если есть, иначе на пол) → ноги РАВНОМЕРНО лево/право, не слипаются и не торчат «антеннами» вверх на открытом полу. Воткнутая стопа, оставшаяся с ходьбы далеко от слота, тихо переставляется к нему (раз в ~0.5с, по одной, ≥`LEGIK_MIN` воткнуто) → стойка «прибирается» после остановки. **ВЫВЕРТ КОЛЕНА** (`LEGIK_BEND`): после FABRIK интерьерные суставы сдвигаются ВДОЛЬ ОПОРЫ (ось `(cos sa,sin sa)`) на `L.bend·LEGIK_BEND·sin(π·i/n)` (пик в СЕРЕДИНЕ = колено); `L.bend=±1` по знаку `hipOff.x` → нога на СТОРОНЕ БУРА гнёт колено К БУРУ (вперёд), с дальней — ОТ бура (назад). Ноги НЕ зеркалятся по взгляду, поэтому фикс-ось по `sign(hipOff.x)` верна при ОБОИХ направлениях (бур ушёл на −x → те же +x-ноги стали задними, их колено +x = назад). Замер: drill-side ноги +20…27px к буру, дальние −20…27px — стабильно на обоих faceX. (Сдвиг ВДОЛЬ опоры, не перпендикуляр цепи — иначе направление вперёд/назад зависело от позы и «гуляло».) Сдвиг задаёт FABRIK сторону сгиба, она держится. (Финальный FABRIK восстанавливает длины — без растяжения.) **ПОДЪЁМ КОЛЕНА** `L.bendUp` (px, вдоль «вверх от опоры», пик в середине) — на каждом втыке с шансом ~70% нога встаёт пологой ∧-ДУГОЙ (колено вверх, по-паучьи), иначе обычно (вниз/вбок); рандом ПО ШАГАМ → поза не повторяется. **БЕЗ РАСТЯЖЕНИЯ ЗВЕНЬЕВ**: все сдвиги позы (вывих/волна/подъём) идут ДО ФИНАЛЬНОГО `_ikFabrik` (он восстанавливает точные `segLens`; `_ikAvoidRock` — перед ним, иначе он растягивает). Рендер-точки `L.draw` (покадровый лерп растягивал) дополнительно прогоняются коротким `_ikFabrik(L.draw, ax,ay, pts[last], …)` — ДВУСТОРОННЯЯ привязка: КОРЕНЬ ЖЁСТКО на корпусе `(ax,ay)`, стопа к якорю, длины ТОЧНЫЕ (0% растяжения). NB: односторонний проход от стопы заставлял БЕДРО «скакать» при дыхании корпуса — отсюда FABRIK. **Походка-ШАГ** (фазы `ready→windup→shoot→hold→retract`), а НЕ «осьминог»: `windup` — кончик ПОДЖИМАЕТСЯ к телу (замах ~0.07с), `shoot` — ВИДИМЫЙ выстрел в породу (тайм-лерп ~0.13с, easeOut, не мгновенный снап), `hold` — стопа ПРИБИТА к якорю в мире (не лагает, корпус проезжает мимо), `retract` — нога ВЗМАХИВАЕТ ВПЕРЁД по ходу + приподнимается (фаза переноса). **ОТРЫВ — ПО РАСТЯЖЕНИЮ, не по таймеру** (иначе юнит «парит»): на ходу стопа держится за свою точку, пока корпус не оттащит её до `releaseR·reach` (`releaseR`≈0.84–0.98, рандом ПО ШАГАМ → отрыв вразнобой). Гейт по числу НА ЗЕМЛЕ (`planted` = только `hold`, НЕ `shoot` — иначе grounded падал до 0 и юнит парил): отпускаем лишь пока `planted>LEGIK_MIN`; жёсткий бэкстоп (`reach·1.02`, но не последнюю ногу) ловит переросток. Итог: ноги несут вес (≥1, обычно 2 на земле), «drag-then-step». **ВЗМАХ ∝ 1/СКОРОСТЬ** (`swingScale = clamp((TILE·3)/speed, 0.5..1)`): быстрее юнит (апгрейды привода) → короче замах/выстрел/перенос → нога быстрее на землю → доля опоры РОВНАЯ на любой скорости (avg grounded ~2 на 3–5 т/с; раньше падало 1.7→1.3). Прогноз выноса `travel = speed·0.2·swingScale`. **Опора ВПЕРЕДИ ЦЕНТРА (упреждение по предсказанному бедру)**: на ходу стопа втыкается ВПЕРЕДИ центра корпуса — 96–100% шагов, ВСЕ ноги (включая задне-закреплённые `hipOff.x<0`). Ключ: реальная длина ноги стоя и на ходу ОДИНАКОВА; мешает не длина, а ЗАДЕРЖКА замах+выстрел (~`stepT`=0.2с), за которую корпус уезжает (`travel=speed·stepT`) — стопа, выбранная под ТЕКУЩИМ бедром, к моменту втыка оказывается ПОЗАДИ. Поэтому: цель выносим от бедра на `proj = fromCtr + travel + ЛИД` (`ЛИД` и `stag`, и пауза `wait` — РАНДОМ ПО ШАГАМ: лид ≈0.4–0.8 тайла, иначе все шаги в одно место = «лок-степ»; рандомная пауза расфазирует ноги, чтобы не шагали в унисон; `fromCtr` = насколько бедро позади центра вдоль хода), а достижимость проверяем по ПРЕДСКАЗАННОМУ бедру `hipP = бедро + velAng·travel` (корпус ПОДЪЕДЕТ к моменту втыка), НЕ по текущему. Перебор убывающего выноса `f=[1,0.7,…,0]` → берём самый дальний ДОСТИЖИМЫЙ из hipP (`f=0` строго вниз — нога ВСЕГДА встаёт, не висит; реально доступный лид ограничен длиной ноги — задние ноги упираются в reach). Замер: +23px впереди центра в момент втыка, стопа впереди центра 86% времени опоры (все 4 ноги). Out-of-reach отпуск — ТОЛЬКО в `hold` (на `shoot` бедро ещё подъезжает к якорю — норма быть за пределом). **Контакт стопы — НА видимой кромке породы**: `_ikSurface` ставит точку опоры ВНУТРЬ камня (`cd = d + 0.12тайла`), т.к. видимая кромка УТОПЛЕНА внутрь сеточной на эрозию `_ragDepth`≈0.16 (render_world) — иначе стопа «висит» в воздухе перед видимой породой; излишек, ушедший в камень, СКРЫТ окклюзией. `bodyOff.x` — ОГРАНИЧЕННЫЙ лаг корпуса (вес, `min(16, speed·0.1)`), БЕЗ обратной связи от ног (гейтинг «корпус позади стопы» давал положительную ОС → убегал в кламп; отказались). Конус прожектора следует за `bodyOff` (см. `unitLightAnchor` при `debugTentacles`). **Окклюзия ЗА породой**: `drawTentacles` в `game.drawScene` обёрнут в `clipVisibleAir(ctx,world,camera)` (render_world) — клип по ВИДИМОМУ воздуху (тоннели + эрозийная кайма по тому же `_ragDepth`), щупальца рисуются только внутри → за породой скрыты (юнит не «вылезает»), стопа, ушедшая в камень, окклюдируется по той же видимой кромке, что и рендер мира. Плавность: мягкий лерп кончика + спокойный темп перешагивания + тихая «волна» + сглаживание рендера (`L.draw` догоняет solved-точки) — без рывков. `rig.supportAngle` поворачивает направление reach ног И корпус к ОПОРЕ (0=пол, −PI/2=стена справа, PI=потолок); в игре ставится из `supportDirOf`, в редакторе — из таба-сцены. Рисует СПРАЙТ вдоль сегмента (если загружен) ИЛИ капсулу. **SQUASH (присед/приземление)** `LEGIK_SQUASH`: пружина просадки корпуса `sq` (вдоль «вниз к опоре», поверх лерп-базы). Прыжок — пока `crouchT>0` цель=`crouch` (корпус СНАЧАЛА вниз), после приседа цель→0, недодемпф. пружина отыгрывает ВВЕРХ (вниз→вверх). Приземление — «удар» ТОЛЬКО при настоящем падении (`prevVelY > LEGIK_SQUASH.landMinVel`≈270, т.е. быстрее контролируемого хода/бурения ≤240; +`aY<−300`): `sq` ∝ скорости падения (`landK`/`landMax`) → корпус проседает НИЖЕ стойки, пружина возвращает. Иначе просадка срабатывала на КАЖДОМ контролируемом шаге (бур вбок/вниз) → «проседал». idle `sq≈0`. **Амплитуды в ПИКСЕЛЯХ скейлятся `rig.drawScale`** (`s`): «вес»-дыхание, `stepBob`, лаг корпуса `bxT`, весь squash (`crouch`/`landMax`/`clamp`) — иначе мелкий юнит «сильно проседал»/«нырял в породу» (фикс. px на юните ~25px). Значения squash снижены (`landMax 16`/`clamp 22`/`landK 0.1`) — мягкий «тук», без ныряния. `stiff`/`damp` (частоты) НЕ скейлятся. **ПОЛНЫЙ свод принципов локомоции (стойка/вес/походка/отрыв/колено/squash/окклюзия + тюнинг-ручки) — в `design_legs.md`.** **Фаза 1** (фундамент); далее — авторинг/превью в редакторе + замена FK-ног в игре. |
| `tentacles.js` | Ноги-щупальца юнита (IK) — тонкая обёртка над `legik.js`. **ПО УМОЛЧАНИЮ ВКЛ** (`game.debugTentacles=true`; **T** переключает на FK для сравнения). Строит ноги DATA-DRIVEN из `UNIT_DEFS` (`legConfigsFromUnit` — только ноги с `ik`), `updateTentacles(dt,unit,world)` в playing-цикле, `drawTentacles(ctx,camera)` после юнита (FK-ноги скрыты `opts.hideLegs`), `tentacleBodyOffset()` → сдвиг корпуса (опора на ноги, `drawTachikoma`/`drawRingUnit` `opts.dx/dy`). **ЯКОРЬ ВДОЛЬ «К ОПОРЕ»** (пол → вниз, стена → к стене, потолок → вверх): ставит корпус так, чтобы ОТМАСШТАБИРОВАННЫЕ ноги доставали ЛЮБУЮ поверхность — `D = TILE·(0.5+LEGIK_STAND − footAnchor)` вдоль `−up` (`anchorX=cos(π/2+sa)·D`, `anchorY=sin(π/2+sa)·D`, `sa=supportAngle`), добавляется к `cx/cy` лег-рига И в `tentacleBodyOffset().x/y`. `footAnchor` (= центр→поверхность в тайлах, из тайл-сетки редактора, `def.footAnchor`); дефолт `0.5+LEGIK_STAND` → `D=0` (старое поведение, scout/некалиброванные НЕ затронуты). Без него мелкий юнит (малый `drawScale`) «парил» НА ВСЕХ опорах: фикс. stand-лифт `LEGIK_STAND` в тайлах не скейлится, а ноги ужаты — не доставали фикс. дистанцию центр-тайла→поверхность (вертикальная-только версия чинила лишь пол, на стене/потолке оставался отступ). Вся IK-логика/поведение/правила — в `legik.js`. Боевую модель ног/лазания НЕ трогает — прототип. **Спрайты ассетов ВКЛЮЧЕНЫ в игре** (отладочный процедурный режим снят); где спрайт сегмента/детали не загружен — авто-фолбэк на капсулу/процедуру. Сегментные спрайты в `legik.drawLegRig` рисуются ТОЙ ЖЕ схемой, что в FK `drawLeg` (`sp.px/py/w/h`, `rotate(ang−π/2+rot)`) — IK-превью совпадает с FK по масштабу/повороту/пивоту. **Спрайты ПРИВЯЗАНЫ К ТИПУ КОРПУСА** (namespacing, без гонок): ключ `PART_SPRITES['<hull>:<id>']`; рендер ставит `partsHull(unit.hull)`, `spriteFor(id)` ищет `<hull>:id` с откатом на плоский `id` (для редактора — он авторит один корпус плоскими ключами). `PART_SPRITE_SRC` (scout) → ключи `scout:*`; `rigbridge` грузит пуш как `<hull>:*`; редактор экспортит/пушит namespaced. Так `core` и `scout` НЕ делят ассеты. Дефолтный юнит — `core` (кольцо; scout пока не используется). |
| `render_glitch.js` | `drawInterference(ctx, canvas, intensity, t)` — пост-эффект ПОМЕХ интерфейса от радиации (`game.radLevel` из `world.radAt`). Чисто визуально, в ПИКСЕЛЯХ канваса, поверх всего кадра (вызов в конце `drawScene`, только `playing` и не дебаг). Снимок кадра (`_glBuf`) + слой тонировки каналов (`_glTmp`) + тайл «снега» (`_glSnow`, `_SNW×_SNH`). Стадии нарастают плавно и НАКЛАДЫВАЮТСЯ: **слабо** — рой строк РАЗНОЙ высоты и скорости (крупные медленные `_glSlow` ~13 Гц + тонкие быстрые каждый кадр, ~45% бьют по ВЕРХНЕЙ зоне с HUD → интерфейс рвёт рано) + RGB-расхождение (`dx∝I*16`, онсет очень ранний — UI «расщепляется» уже на слабом фоне); **средне** — крупные срывы, бегущая полоса, начинается «снег» (читаемо); **сильно** — «снег» (ТВ-static, перегенерация ~28 Гц) густеет; **критично** — снег идёт В НЕСКОЛЬКО ПРОХОДОВ со случайным сдвигом, поэтому даже ЯРКИЙ интерфейс ПОЛНОСТЬЮ тонет (раньше панели просвечивали). RGB: 3 канала multiply→`lighter` со сдвигом X. Снег: онсет `I≈0.42`, `passes` 1→3 по `snowA`, затирающий `source-over` + искра `lighter`. Болезненный зелёный отлив на среднем+ фоне. |
| `render_ring.js` | **Юнит-КОЛЬЦО (новый тип `kind:'ring'`, см. `HULL_DEFS.core`/`UNIT_DEFS.core`).** Центр — ТОР-реактор (`drawReactorRing`); модули крепятся СНАРУЖИ по окружности (в блупринте `ang°`+`rad` в R), ориентированы НАРУЖУ. Ориентация кластера: `updateRingAim(dt,unit)` ведёт `unit._ringAim` (ПЛАВНО, только ВЕРТИКАЛЬ ±90° вверх/вниз) + ставит `unit._ringFlip` (МГНОВЕННОЕ зеркало). **Строго НАЛЕВО — это ЗЕРКАЛО вида (`ctx.scale(-1,1)`), а НЕ вращение на 180°** (иначе верхние модули уходят вниз, их прячут ноги): горизонт = `aim 0` + `flip` по `faceX===-1`; вертикаль = доворот ±90°. Вертикаль берётся из `unit.dy` (а НЕ из `drilling`) → **направление СОХРАНЯЕТСЯ после бурения вверх/вниз** (не сбрасывается в idle-горизонталь). `drawRingUnit` зеркалит кластер при `_ringFlip` и рисует каждый модуль на `ang+aim` через `drawPart` (forward=+X). **Бур «долбит»**: при `drilling` радиус крепления бура модулируется `ANIM.drillWob(t,unit)` (ход вдоль оси к породе). **Масштаб юнита — ОДИН `ctx.scale(opts.scale)` на всё кольцо** (как `ctx.scale(zoom)` в редакторе), `R=(TILE-8)/2` в ДИЗАЙН-px: позиции И размеры спрайтов ужимаются ВМЕСТЕ. Иначе `drawPart` рисует спрайт в абсолютных `sp.w` px (игнорируя `S`), и при масштабировании только позиций (через `R*scale`) спрайты выходили в `1/UNIT_DRAW_SCALE`≈1.6× КРУПНЕЕ относительно кольца, чем в редакторе. Размер спрайта реактора в игре = `sp.w·UNIT_DRAW_SCALE` (через transform). Инвентарь-превью (`scale:1` + свой внешний scale) не затронут — там масштаб уже единый. **Реактор+модули — ЕДИНЫЙ порядок по `z`** (`reactor.z` теперь учитывается — раньше тор рисовался жёстко под модулями). **Кольцо-реактор** = деталь `kind:'reactor'` в центре (не вращается), `drawRingCore`: спрайт `reactor`/`reactor:off` по `unit.reactorOn` (по умолч. ВКЛ) если `!partsProcOn()`, иначе процедурный тор `drawReactorRing`. ВЫКЛ (`reactorOn===false`): отдельный ассет `reactor:off`, если есть; иначе вкл-спрайт `globalAlpha*0.4` / процедурный тор с потухшим ядром (`lit=0.12`) — реактор виден тёмным до установки в интро. НОГИ (kind `leg`, ik) НЕ вращаются — это та же система щупалец (`tentacles`/`legik`), рисуются ОТДЕЛЬНО в `game.drawScene` ПОД кольцом (порядок: клип-видимый-воздух → `drawTentacles` → `drawRingUnit`). Прожектор: `unitLightAnchor` для ring-хала отдельная ветка (вершина у бура, направление = угол бура `drill.ang+_ringAim` с учётом `_ringFlip` по X) — FK-риг тут не применим (модули `ang/rad`, не `f/s`). `game.startSession` ставит `unit.hull = inventory.hull`. Старый scout-путь не тронут (ветка `isRing` в drawScene/update). `ringModuleScreenPos(unit,camera,kind,bo)` — ЭКРАННАЯ позиция модуля кольца (та же раскладка translate→scale→flip→`cos/sin(ang+aim)·rad·R`), используется лучами сканера (`render_scan.drawScanFx`); не-кольцо/нет модуля → центр юнита. |
| `render_unit.js` | `drawTachikoma(ctx,world,unit,camera,opts)` — рисует юнит ИЗ РИГА. Юнит ВСЕГДА собирается в нейтральной ГОРИЗОНТАЛИ (`resolveUnitRig` с dy=0), а при лазании по стене (`supportDirOf` из rig.js → `left`/`right`) ЖЁСТКО поворачивается на ±90° целиком (`ctx.rotate`) — масса и ноги не расходятся (нет бага со сдвигом ног в корпус), ноги ложатся к существующей стенке, бур — по ходу. `opts.scale` (в мире = `UNIT_DRAW_SCALE`≈0.62) ужимает риг; на ПОЛУ якорит стопы к полу тайла из СТАТИЧНОГО рига (без дрожи, приподнято на `0.28` тайла); на СТЕНКЕ — лёгкий отступ `0.1` тайла + поворот + ужатие к центру. `imageSmoothingQuality='high'` гасит мерцание при даунскейле. В инвентаре масштаб снаружи. Кабели → детали+ноги по общему `z`; присед к `legHub`; idle-bob. |
| `render_city.js` | `drawCity` — Hibernation Widget (язык дизайна `design/handoff`, без иероглифов): ВЕСЬ статус города в ОДНОЙ капсуле 360×44 со скошенными углами (top-right + bottom-left). Слева — эмблема (3 кольца контуров + общее HP в центре, цвет по фазе); посередине — меняющаяся надпись фазы + сегментный бар обратного отсчёта; справа — крупный «секунды.МС» на `FONT_DISPLAY` с glow по опасности (апгрейды ёмкости батарей дают таймер >100с → **три цифры**: слот/шрифт ужимаются, сотые скрываются, подпись `СЕК`). Капсула сидит у левого края «коридора» (x=210), чтобы не лезть на панель задания справа. Фазы: на базе `charging` `ПОДПИТКА ГОРОДА` / `full` `ГОРОД ЗАПИТАН` (toxic, бар растёт); вне базы >30с `ДО ГИБЕРНАЦИИ` (amber), 15–30с `ВЕРНИСЬ К ПРИНТЕРУ` (gold), ≤15с `СРОЧНО НА БАЗУ` (bloodBright, glow), `dying-outer/inner/core` — стадии шатдауна контуров (bloodBright/Dim мигание 200мс, число = `hp / CITY_DMG` обратный отсчёт до потери контура, бар = HP активного кольца). Смена надписи между фазами анимирована (`_drawBarText`, easeInOutCubic ~360мс): старая уезжает вверх и гаснет, новая выезжает снизу. Скретч-состояние (`city._barPhase/_barPrev/_barText/_barT0`) — на самом city. |
| `hud.js` | `drawHUD(ctx, world, unit, inv, dbg, W, H)`, `hudBar`, **техно-frame тулкит** `techPanel` (рамка+уголки+болты+шапка) + `boltHead`/`edgeFingers`/`hazardTape`/`hazardTapeV`/`serialChip`/`pulseDot`/`pulseSquare` (мигающий квадрат-буллет)/`drawStaticBg`/`drawCrtOverlay`. HUD-боксы: статус юнита (HP, без энергии — упрощённая модель), панель «ГРУЗ» (счётчики по типам ресурсов; `used/cap`), цикл справа от капсулы города, **цели сессии** (`SESSION_GOALS`) компактно СПРАВА ПОД виджетом города (y≈70, чтобы не лезть на капсулу гибернации; `short`+мигающий квадрат по `accent`). **Извлечение данных** (`dbg.scan`||`dbg.scanDoneT>0`): КРУПНОЕ (2×, r≈38) круговое прогресс-кольцо В ПРАВОМ НИЖНЕМ углу (cobalt-дуга `data·100`, подпись `ИЗВЛЕЧЕНИЕ ДАННЫХ`; по завершении `scanDoneT` секунд — полное кольцо + toxic `ДАННЫЕ ИЗВЛЕЧЕНЫ`). **Виджет лога событий — ВСЕГДА виден** (под кольцом справа-внизу): `// ЛОГ СОБЫТИЙ` + последние 3 записи `Ц<цикл> · <текст>`, либо `— нет событий —` (пустое состояние на старте). Заданий/репутации больше нет. Кнопки «Ядро» нет (модули — только перед забегом, см. `inventory.js`). |
| `ui_menu.js` | `drawMainMenu`, `drawPauseMenu`, `drawGameOver`, `drawButtons` — экранные меню. `wrapText` возвращает Y последней строки (для верстки под текстом). `drawDirectives(ctx,x,y,big)` — блок целей сессии (`SESSION_GOALS`): заголовок «// ДИРЕКТИВЫ СЕССИИ» + крупные буллеты (мигающий квадрат `pulseSquare` по `accent` + индекс + `text`); на главном меню рисуется под сюжетным текстом (`big=true`). Главное меню: кнопки — НИЖНИЙ РЯД из боксов (`drawButtons`, без `desc`): «Новый забег» + «Прогресс · NN МТ» (не перекрывают заголовок/директивы слева). **`drawGameOver(ctx,buttons,W,H,reason,meta,overT,bank)`** — финальный экран МЕТА-ПЕРЕСЧЁТА в РАМКЕ-панели (`_panelCorners` уголки + шапка `// ПЕРЕСЧЁТ ЗАБЕГА · МЕГА-ТОКЕНЫ` + статус-чип): строки `meta.rows` (акцент-квадрат + лейбл + `значение × коэф` + `+токены`) с **АНИМИРОВАННЫМИ счётчиками** (стаггер по `overT`, easeOut), снизу токен-бейдж ИТОГО `+N МТ` + В БАНКЕ (копится из `bankBefore=bank-meta.total`). Значок МТ — `drawMetaToken` (canvas-копия `_mtToken` из meta_dom: гекс+квадрат+ядро+спицы) → совпадает с экраном мета-прогресса. |
| `game.js` | `Game` — только оркестрация: режимы (menu/inventory/intro/playing/paused/gameover), главный цикл, ввод-мышь, склейка. **Навигация по меню** (menu/paused/gameover): `menuNav()` — WASD/стрелки двигают `menuSel` (с заворотом), Space/Enter — `doMenuAction` выбранной кнопки; мышь (mousemove) двигает ТОТ ЖЕ `menuSel`, клик — `menuClick`; `menuButtons()` ставит `b.hover = (i===menuSel)` (подсветка через `drawButtons`/`menuLine`); смена режима сбрасывает `menuSel=0` (`_modePrev`). `onInventoryStart` → `doInventoryStart` → `startSession` + `intro.reset()` + `mode='intro'` (модули — только перед забегом; снять во время игры нельзя). Сдача груза на базе: по ОДНОЙ единице каждые `DELIVER_INTERVAL` (`deliverCargo`→`deliverOneCargo`) при `atBase()` и `inventory.cargoUsed()>0`; сданная единица копит банк апгрейдов. Заданий/репутации больше нет. **Проходка**: `this.dugTiles` копит `unit.broke` (любой прокопанный тайл), пишет рекорд `save.bestDug` — это итоговая метрика (на gameover и в меню вместо глубины). **Помехи интерфейса**: `this.radLevel` плавно тянется к `world.radAt(unit.tileX,unit.tileY)` (полюса + очаги у базы); в конце `drawScene` (только playing) — `drawInterference` (см. `render_glitch.js`). Дебаг-обзор рисует ☢-маркеры очагов. `world.reveal(unit.tileX, unit.tileY, unit.stats.scanR || SCANNER_R)` — радиус сканера из модуля. **Серверы/данные**: `updateServers(dt)` (в playing-цикле) — ищет БЛИЖАЙШИЙ откопанный (`dug`) недокачанный (`!done`) сервер в радиусе `SCAN_RADIUS` от юнита → `this.activeScan`; копит `s.data += dt/SCAN_TIME` (уход из радиуса = пауза, прогресс СОХРАНЯЕТСЯ; возврат — докачка); по `data>=1` ставит `s.done`, `logEvent('НАЙДЕНЫ НОВЫЕ ДАННЫЕ')`, `this._scanDoneT=2.4` (надпись «ДАННЫЕ ИЗВЛЕЧЕНЫ») И `codexGainData(1)`→`codexPopupShow` (фрагмент в текущий диск кодекса + внутриигровой попап «загрузка → рост %», см. `codex_dom.js`). `logEvent(text)` пишет `{cycle, text}` в `this.eventLog` (макс. 16). `drawScene` зовёт `drawServers` (после `drawWorld`) и `drawScanFx`+`drawEnemyScanFx` (после юнита); `drawHUD` получает `scan`/`scanDoneT`/`log`. **Скан врагов**: `updateEnemyScan(dt)` (в playing-цикле) — вражеский юнит (копатель и др.) в радиусе `SCAN_RADIUS` копит `e.scan += dt/SCAN_TIME` (как серверы: уход — пауза); по `>=1` ставит `e.scanned`, `logEvent('СКАНИРОВАН ВРАЖЕСКИЙ ЮНИТ')`, `discover('unit')` (глоссарий) и `codexGainData(1)`→попап (фрагмент в кодекс, разово на юнит). `this.scanEnemy` → `render_scan.drawEnemyScanFx` (луч/конус к врагу) и кольцо HUD (через тот же `scan`-канал: `activeScan || {data:scanEnemy.scan}`). **Нестабильная порода**: `this.falling = new FallingRocks()` (в `startSession`), `falling.update(dt, world, unit)` в playing-цикле (срыв валунов + урон), `drawFalling` в `drawScene` ПОСЛЕ тумана (валуны видны в темноте). **Видения в темноте**: `this.visions = new Visions()` (в `startSession`), `visions.update(dt, unit, designW, designH)` в playing-цикле, `drawVisions` в `drawScene` ПОСЛЕ тумана/прожектора, ПОД юнитом (гейт `playing && !debug`). **Подсказки + находки**: `this.hints = new Hints()`; `checkDiscoveries(dt)` (в playing) — первая встреча сервера/гнезда/каверны/врага (по `world.isSeen`) → `discover(cat)` (→ `codexDiscoverCat` глоссарий + `logEvent` + крупная подсказка ОДНОРАЗОВО), плюс отсечки высоты `HINT_DEPTHS` при подъёме; `drawBigHint` в `drawScene`. **Оптимизировано** (не «опрос каждым кадром»): тик ~5/сек, пропуск исчерпанных категорий (`codexCatExhausted`) и замеченных объектов (`o._noticed`), полное отключение опроса флагом `_discDone` когда всё открыто и все отсечки пройдены. **Пещеры-сцены**: `updateBackdrops(dt)` (вход юнита в `world.backdrops` → свип `BACKDROP_SWEEP` → `_backdropDone`: `codexGainData(BACKDROP_DATA)`+попап + `discover('cave')` + лог); `drawBackdrops` в `drawScene` ПОСЛЕ тумана. `codexBindSave(this.save)` в конструкторе (персист кодекса). Цикл (`this.cycle`) тикает в playing; вызывает `updateEnemies` (AI вынесен в `ai.js` — домешан в `Game.prototype`). **Мета-прогресс**: `dataCount` (извлечено серверов за забег) и `directivesDone` (задел, пока 0) копятся в playing; `computeMeta()` пересчитывает метрики забега (циклы/проходка/ресурсы=сдано+трюм/данные/директивы × `META_COEF`) → строки + итог. На gameover ОДИН РАЗ (`if(!this.metaResult)`): `computeMeta` → `save.meta += total` → `writeSave`; `overT` тикает для анимации; `drawGameOver(... this.metaResult, this.overT, this.save.meta)`. `endRun`/`startSession` обнуляют `metaResult` (нет двойного начисления, накопление между забегами). **Экран мета-дерева** (режим `'progress'`): кнопка «Прогресс» на главном меню → `mode='progress'` + `metaDomShow(this)` (DOM-оверлей `meta_dom.js`); canvas под ним просто тёмный, весь ввод/выход (клик/ховер/ESC/назад) — в оверлее (`metaDomBack` ставит `mode='menu'`). **Экран «База данных»** (режим `'database'`) — аналогично: кнопка «База данных» на меню → `mode='database'` + `codexDomShow(this)` (DOM-оверлей `codex_dom.js`); ветка рисовки общая с `'progress'` (тёмный canvas), ввод/выход — в оверлее. `metaBindSave(this.save)` в конструкторе (для `metaHas` в забеге). На gameover: Enter → `endRun` + `mode='menu'` (рисовка пропускается на этом кадре, иначе `drawWorld(null)` крашится). **Дебаг-обзор (B):** свободная камера, туман выкл, юнит заморожен; мир ЖИВЁТ. |
| `ai.js` | AI диких гнёзд (вынесено из `game.js`, домешано в `Game.prototype` — `this`=Game, грузится ПОСЛЕ game.js): `onCycleStart` (спавн волн — копатель **со 2-го цикла** 1/цикл до числа городов, с ЧЕРЕДУЮЩИМСЯ `sweepSign` `±1` через `_diggerN` → копатели делят тор; собиратель с цикла 2 до `COLLECTOR_CAP`, **разведчик** с цикла 3 из гнезда, СВЯЗАННОГО `airPath` с базой), `near`/`nestAt`/`nearestResource` (хелперы), `diggerBrain`/`collectorBrain`/`raiderBrain` (поведение по типу), `airPath` (BFS по проходимому воздуху), `updateEnemies` (тик всех + despawn). **`diggerBrain`**: найдя ЛЮБОЙ город → `tocity` (докапывается ВПЛОТНУЮ в его пещеру, пробой виден игроку); чужой город — лишь ПУТЕВАЯ ТОЧКА (пробил → снова `seek`, ищет дальше, в т.ч. базу), только база → `return` домой. База даёт ТЕЛЕГРАФ: лог `КОПАТЕЛЬ ИДЁТ К БАЗЕ` + крупная подсказка `ПРОРЫВ К БАЗЕ` (на коммите), лог `МАГИСТРАЛЬ К БАЗЕ ПРОБИТА` (на пробое). Время поиска базы откалибровано ~330с±120 (Монте-Карло; ручка — vy-bias подъёма в `enemy.wanderDir` + спавн со 2-го цикла). **`raiderBrain` зовёт `city.drain(RAID_DRAIN)` (ПЕРМАНЕНТНЫЙ срез контура, не `damage`) + `logEvent('РЕЙДЕР ВЫСОСАЛ КОНТУР')`** — набеги накапливаются и рушат город. |
| `gdd_mvp_v0.3.md` | Дизайн-документ (v0.4 — **снимок дизайна в том виде, как реализован**: core-loop, тор-мир, города/гибернация, скверна, конструктор, циклы/враги, квесты, дизайн-система; §18 — что НЕ сделано). Источник стартовых значений (всё тюнится). |
| `design_meta.md` | План мета-прогресса (мета-токены, репутация, ростер городов, оригинальные механики). Не реализовано — источник правды для будущего этапа. |
| `design_cycles.md` | План системы циклов и диких городов (эскалация волн: копатели → собиратели → разведчики). Не реализовано — пост-MVP. |
| `design_legs.md` | **Полный свод принципов локомоции на щупальцах** (IK-ноги): опора/стойка, «вес»/дыхание, squash (присед/приземление), фазы шага, опора-впереди (упреждение), отрыв по растяжению, взмах ∝ 1/скорости, станц-слоты, поза/вывих колена, без растяжения звеньев, контакт по видимой кромке + окклюзия, и таблица тюнинг-ручек. Реализация — `legik.js`/`tentacles.js`. Ссылка из строки `legik.js`. |
| `design_editor.md` | **Полная спецификация редактора рига** (`tools/rig_editor.html`) и конвейера «редактор → игра»: режимы скелет/ассет, тайл-сетка/калибровка масштаба, карточки деталей и сегментов, ручки на канвасе, IK-превью, ассеты/удаление фона, юнит-кольцо (модули `ang/rad` + реактор с 2 ассетами + copy-leg-assets), проект-JSON, экспорт кода, передача в игру (`rig-core.js` файлом + `rigbridge.js` localStorage, разница origin), инварианты приёма (namespacing по корпусу, масштаб `UNIT_DRAW_SCALE` колец и ног). Ссылки из строк `tools/rig_editor.html`/`rigbridge.js`/`rig-core.js`. |
| `tools/rig_editor.html` | **Standalone-редактор рига** (переиспользует `constants/unitdefs/anim/rig/render_parts`; ring-ветка грузит `render_ring`/`legik`). Авторинг внешнего вида юнита: расстановка деталей/модулей, скелет ног (сегменты + ручки на канвасе), спрайты-ассеты, тайл-сетка для калибровки масштаба (`UNIT_DRAW_SCALE`), IK-превью щупалец, юнит-кольцо (`ang/rad` + реактор с 2 ассетами), проект-JSON (сохранить/загрузить), экспорт кода и передача в игру. **ПОЛНАЯ спецификация редактора и конвейера «редактор → игра» — в `design_editor.md`.** Открывать через превью-сервер (`/tools/rig_editor.html`). |
| `rigbridge.js` | localStorage-мост приёма снимка из редактора (грузится ПОСЛЕ `tentacles`, ДО `game`). Читает `localStorage['twilight-rig:<hull>']`: оверрайд `UNIT_DEFS[hull]` + спрайты в `PART_SPRITES['<hull>:*']`, снимает процедурный дебаг. Работает ТОЛЬКО при одном origin (игра+редактор с превью-сервера); на `file://` молчит — там `rig-core.js`. Детали — `design_editor.md` §11. |
| `rig-core.js` | Файловый снимок юнита для игры (грузится ПОСЛЕ `rigbridge`, ДО `game`). В репозитории — ЗАГЛУШКА (no-op); перезаписывается скачанным из редактора `rig-<hull>.js` (IIFE: `UNIT_DEFS[hull]=def` + спрайты `data:URI` → `setPartSprite('<hull>:id',…)`). **Работает на `file://`** (двойной клик). По файлу на корпус (при втором — добавь в загрузчик `index.html` + заглушку). Детали — `design_editor.md` §11. |
| `.claude/launch.json` | Конфиг превью-сервера. |
| `CLAUDE.md` | Этот файл. |

**Порядок подключения в `index.html`:** `constants → save → meta → world → input → unit →
camera → city → cycle → unitdefs → anim → rig → inventory → upgrades → loot → effects → falling → visions → hints →
intro → enemy → render_resource → render_module → render_world → render_backdrop → render_falling → render_loot →
render_fx → render_parts → render_unit → render_ring → render_light → render_scan → render_visions → render_hint → render_enemy → render_intro →
render_city → render_glitch → legik → tentacles → rigbridge → rig-core → hud → ui_menu → meta_dom → codex_dom → game → ai`. (`render_light` после
`render_unit` — берёт `unitLightAnchor`; `render_scan` после `render_ring` — берёт `ringModuleScreenPos`; `meta_dom`/`codex_dom` — DOM-оверлеи экранов «СЕТЬ ПАМЯТИ»/«БАЗА ДАННЫХ» (CSS `meta_dom.css`/`codex_dom.css` инжектятся загрузчиком); `render_meta` НЕ подключён (суперседед); `ai` после `game` — домешивает в `Game.prototype`.) Загрузка — ПАРАЛЛЕЛЬНАЯ с `async=false` (порядок выполнения = порядок массива; см. `index.html`), `game.js` инстанцируется через `window.bootGame()` после выполнения всех модулей. Классы и функции — глобальные (без модулей).

> Рендер — это свободные функции `draw*(ctx, ...)` (не класс), логика отдельно от
> рендера. Новую подсистему клади в свой файл и допиши `<script>` в правильном месте.

## 4. Архитектура: один тип сущностей — один файл

Главное правило структуры. **Не наращивай `game.js` — выноси новую подсистему в
свой файл.** Раскладка уже соответствует этому (см. таблицу в разделе 3):
`constants`, `save`, `world`, `unit`, `camera`, `input`, `inventory`,
`render_world`, `render_unit`, `hud`, `ui_menu`, `game`.

Будущие файлы по мере роста (создавай по той же схеме):
- `physics.js` — гравитация/коллизии, если логика движения разрастётся (сейчас в `unit.js`).

> При следующей новой подсистеме — заводи отдельный файл, а не дописывай в `game.js`.

## 5. Правила кода

- **Логика отдельно от рендера.** `update(dt, ...)` не должен зависеть от Canvas;
  `draw(...)` не меняет игровое состояние.
- **Ввод — по `e.code`** (`KeyW`/`ArrowUp`/…), не по `e.key`. Раскладко-независимо
  (на кириллице `e.key` ломается).
- **Главный цикл — rAF, `dt` в секундах, клампить** (`dt = min(dt, 0.05)`).
  Скорости/расходы — в `единицах/сек` или `/блок`, всегда умножать на `dt`.
- **Никаких магических чисел по коду.** Стартовые значения (из GDD) — в одном
  месте как именованные константы; их тюнят на плейтестах.
- **UI-тексты — по-русски.** Идентификаторы кода — латиницей/англ.
- **Комментарии — только ПОЧЕМУ** (неочевидный инвариант, причина), не ЧТО.
  По умолчанию комментариев нет.
- **Без преждевременных абстракций**, без мёртвого кода и полуреализаций.
  Не добавляй обработку невозможных кейсов.
- **Маленькие функции, явные имена**, без скрытых сайд-эффектов.
- Без сборки: классы — глобальные, общие через порядок `<script>`. Помни про это
  при добавлении файла (допиши тег в `index.html` в правильном порядке).

## 6. Чек-лист перед «готово»

- [ ] В консоли нет ошибок.
- [ ] Поведение проверено в браузере (скриншот нужных экранов).
- [ ] Трудноуловимая логика проверена через eval/пошаговую симуляцию.
- [ ] Новый код лежит в правильном файле по разделу 4.
- [ ] Менялась структура/правила — обновлён этот `CLAUDE.md`.

## 7. Эффективная работа со мной (Claude Code)

- **Дроби задачу на шаги и проверяй инкрементально** — после каждого шага
  скриншот/eval, а не «всё сразу в конце».
- **Держи файлы маленькими и сфокусированными** — дешевле контекст, точнее правки,
  меньше регрессий.
- **Делай состояние наблюдаемым** (`window.game`, методы статов) — это позволяет
  проверять логику без UI.
- **Фиксируй неочевидные решения здесь**, чтобы не терять контекст между сессиями.
- Используй задачи (TaskCreate) для многошаговой работы; помечай выполненное сразу.
- Делегируй широкое исследование сабагентам, но не дублируй их работу вручную.
