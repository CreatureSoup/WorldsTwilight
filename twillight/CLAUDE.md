# CLAUDE.md — Twilight of the World (Сумерки мира)

> Claude Code читает этот файл в начале каждой сессии. Это КАРТА: проект, файлы,
> правила, ссылки на спеки. **Детали систем живут в `spec_*.md` / `design_*.md` —
> меняешь механику → правишь её спеку; меняешь структуру → правишь этот файл.**
> Перед работой над системой ПРОЧТИ её спеку (раздел 4).

## 1. Проект

**Twilight of the World (Сумерки мира)** — roguelite-копалка, вид сбоку, с гравитацией.
Игрок-ИИ через базу-принтер управляет ботом-буром. Дизайн-источник — `gdd_mvp_v0.4.md`.

**Стек:** чистый HTML + JS + Canvas 2D. **Без npm, сборщиков, фреймворков.** Открывается
двойным кликом по `index.html` (file://) или превью-сервером. Главный цикл — rAF.

**Что уже есть:** тор-мир со стратами (тёплые вверх — погребённая цивилизация, холодные вниз —
«космос»), юнит-кольцо на IK-щупальцах (копание/клинг/гравитация), города и гибернация, враги
диких гнёзд (копатель/собиратель/рейдер) с циклами-волнами, скан данных → кодекс с глоссарием,
мета-прогресс (МЕГА-ТОКЕНЫ + СЕТЬ ПАМЯТИ), сборка юнита, апгрейды сессии, localStorage-сейв.

## 2. Запуск и проверка

- **Запуск:** `index.html` двойным кликом, либо статик-сервер из `.claude/launch.json` (порт 8123).
- **После КАЖДОГО изменения проверяй в браузере** (preview): скриншот ключевых экранов +
  `preview_console_logs` (ошибок быть не должно).
- **rAF в превью тормозит** между «тихими» eval-вызовами (фоновая вкладка). Плюс **энергосбережение**:
  при потере фокуса окна цикл падает до `BG_FPS`=24, при скрытии вкладки — ПОЛНАЯ пауза (см.
  `spec_render.md`, перф-правила). В превью-iframe без фокуса игра идёт на 24fps — eval-правки видны с
  задержкой; буди скриншотом/resize/фокусом. Логику проверяй детерминированно — шагай симуляцию вручную:
  ```js
  game.input.keys = new Set(['KeyS']);
  for (let i=0;i<120;i++) game.unit.update(1/60, game.input, game.world);
  game.input.keys = new Set();
  ```
  Либо форсни кадр скриншотом/resize (будит цикл).
- **Окно превью иногда схлопывается в 1px** — чини `preview_resize` (например 980×1000).
- **`?v=Date.now()` бьёт кэш только у JS, НЕ у самого `index.html`.** Добавил файл в загрузчик —
  браузер может отдать старый html без нового `<script>`: функции будут `undefined` БЕЗ ошибки.
  Лечение: `location.href='/index.html?cb='+Date.now()`.
- `window.game` — точка входа для отладки. **Дебаг-обзор — клавиша B** (свободная камера, туман
  выкл, юнит заморожен, мир живёт, маркеры объектов видны).

## 3. Файлы

| Файл | Суть | Спека |
|---|---|---|
| `index.html` | Лоадер-оверлей + параллельный загрузчик скриптов (`async=false`, порядок = массив, cache-bust). ⚠️ `window.game` занят браузером (canvas id) → бут идемпотентен по `window._gameBooted`. | — |
| `style.css` | Полноэкранный canvas, тёмный фон. | — |
| `constants.js` | ВСЕ константы (мир/юнит/враги/UI/мета) + палитра `PAL` + шрифты. «Закон одного пигмента» — один акцент на элемент. Новые числа — только сюда, не по коду. | по теме |
| `i18n.js` | ЛОКАЛИЗАЦИЯ — ядро. Один язык (`ru`) сейчас, переключения нет (задел). Оптимизация под Canvas: активный словарь — глобал `STR` (вложенные неймспейсы), горячий текст читается ПРЯМО `STR.menu.title1` (без парсинга ключа), параметризованные — ФУНКЦИИ `STR.menu.stats(b,r,m)`. `tr('a.b.c',…)` — для динамических ключей/фолбэка. `i18nRegister`/`i18nUse`/`i18nLang`. ⚠️ декоративная чрома (серийники/hex/рамки/глифы/латиница) НЕ локализуется. | `spec_i18n.md` |
| `lang_ru.js` (+ `lang_ru_*.js`) | СЛОВАРЬ RU (`i18nRegister('ru', {...})`). ✅ **Локализация ЗАВЕРШЕНА** — весь UI в `STR`, хардкод только декор-чрома/комменты/debug/`CITY_NAMES`. Словарь разбит на ПЕР-ДОМЕННЫЕ файлы (`lang_ru` ядро + `lang_ru_constants`/`_log`/`_meta`/`_codex`/`_upgdata`/… по системам), сливаются `i18nDeepMerge`. Грузятся в голове загрузчика до потребителей (`lang_ru_constants` — ПЕРЕД `constants`). Полный список + неймспейсы — `spec_i18n.md` §3. | `spec_i18n.md` |
| `save.js` | localStorage: `bestDug`, `runs`, `meta` (банк МТ), `metaUnlocks`, `codex`, `build` (сборка прошлого забега), `idTypes` (опознанные сканом типы), `storyMode` (режим истории — чекбокс в гл. меню, глушит дикий город), `epoch` (ГЛОБАЛЬНЫЙ ЦИКЛ существования ИИ — float, старт `EPOCH_START`, НЕ сбрасывается; тикает в меню `game._tickEpoch`, забег стартует с него `game._epochBase`, на выходе += прожитые циклы; виджет — `drawEpochClock`, в забеге — `game.cycleLabel`). | — |
| `world.js` | Карта/тайлы/генерация из seed, тор по X, туман войны, страты, города/гнёзда/серверы/артефакты-реликты (`genArtifacts`, маркер `t.artifact`)/останки роботов (`genRobots`, маркер `t.robot`, варианты `robot.kind`, логика — `hazards.js`)/ЛОВУШКИ (`genTraps`→`world.traps`, маркер `t.trap`; пул `TT=acid/seismic/cavein/MINE` — МИНА теперь ОДИН ИЗ ТИПОВ, не свой генератор; `world.mines`=фильтр `type==='mine'`; логика — `traps.js`)/пещеры-сцены, нестабильная порода. ⚠️ **ВЕРХНЯЯ СТРАТА** (над городом, `_upperBand()`) тоже засевается тематически (`SERVER_UP`/`ARTIFACT_SEED_UP`/`ROBOT_UP`/`TRAP_UP`/`BACKDROP_UP`); глубокие бэнды доходят до ~`0.97·H` (дно не пустует); `_blockedSeedRow` исключает крусты. **Очаги радиации привязаны к АРТЕФАКТАМ** (`genRadSources` = центры `this.artifacts`, вкл. верхние). **КОНТЕЙНЕРЫ-ХРАНИЛИЩА** (`genContainers`→`world.containers`, маркер `t.container`, ~20; узел `kart_hackbox` → взлом → ресурсы, логика — `containers.js`). `setAir` помечает `.dug` у server/artifact/robot/trap/container. **LoS:** `hasLineOfSight(px0,py0,px1,py1)` — ОБЩИЙ гейт стрельбы (тор по X, только AIR-тайлы между): снайпер/мортира (`ai`), турели города (`cityturret`), СВЧ-структура — НЕ бьют сквозь породу (печатные турель/рейлган — свой `Structures._los`, та же модель; стены-структуры НЕ тайлы → мортира всё ещё бьёт поверх заборов игрока). ⚠️ тест-объекты у базы — убрать перед релизом (раскладку проверять `tools/worldstats.js`). | `spec_world.md` |
| `unit.js` | Движение/бурение/HP. ⚠️ При пробитии тайла юнит НЕ двигается (решено с игроком, НЕ ЛОМАТЬ); «замок» `_dugBlock` (разовое нажатие = стой); падение непрерывное; `unit.stats` — единый источник статов. Спец-буры (гасят grind, `s.altDrill`): `s.impulse` — волна (`impulse.js`); `s.kinetic` — РАЗГОН по ВРЕМЕНИ в grind-блоке (`kinRamp`/`kinDir`/`kinIdleT`); `s.screw` — автономные щиты (`borers.js`). `world.setAir(x,y,noTrigger)` — флаг «без осыпания» для винтового хода. **ПРЫЖОК:** «вверх» → присед + подъём-ход; **«вверх+вбок»** (`reqHx`) → ДИАГОНАЛЬНЫЙ прыжок, когда диагональ вверх открыта (иначе шаг-залаз). **ТРИГГЕР надёжный:** буфер `_jumpBufT` (=`JUMP_BUFFER`) помнит нажатие «вверх» ~0.13с + walkOn прерывается при `wantUp` → прыжок ловится ВО ВРЕМЯ шага (не надо жать строго синхронно с «вбок»). **СИММЕТРИЧНАЯ ДУГА:** снижение — ОТДЕЛЬНЫЙ зеркальный ход (`_jumpDesc`/`_jumpDescDir`/`_jumpDescH`): вниз-вбок ТОЙ ЖЕ высоты и скоростью (`effectiveSpeed·JUMP_SPEED_FRAC`), что подъём → 2-я половина не быстрее/дальше 1-й; путь занят → обычная гравитация. ⚠️ ход/коп IDLE-ветки гейтятся `anchored` (в воздухе ввод не двигает вбок — без «полёта»/лишнего апекс-шага). Узел `print_jump` — высота ×2. | `spec_unit.md` |
| `camera.js` | Тороидальное следование, `screenX` — ближайшая копия по кольцу. | — |
| `input.js` | Held-state по `e.code` (`keys`) + edge (`justPressed`/`pressed()`). ⚠️ **Действия юнита — СТРОГО разделены** (`constants.js`): ГЛАВНОЕ = ВСЕГДА Пробел (`KEY_PRIMARY` — слот БУРА: импульс-заряд/винт-запуск + взаимодействие: печать, апгрейды у базы); ДОПОЛНИТЕЛЬНЫЕ = НУМЕРОВАННЫЕ цифры (`KEY_ACTION(n)` → `['Digit'+n,'Numpad'+n]`). Раскладку доп-действий по цифрам держит ДИНАМИЧЕСКИЙ МЕНЕДЖЕР (`actionbar.js`: `_actionList`/`actionKeys`) — НЕ хардкод per-модуль: активные доп-действия (сканер/aux-модули/артефакты) получают 1,2,3… по порядку, без коллизий. Метка для HUD — `keyLabel(code)`. Меню/модалки берут Пробел/Энтер (не игровое действие). | — |
| `city.js` | Таймер гибернации + 3 кольца. ⚠️ КОНТУРЫ как HP: база НЕ лечит кольца (только таймер), значение фиксируется при возврате; восстановление HP — авто-починка (`amb_regen`), возврат утерянных колец — `amb_recon`. `drain()` (рейдер) ест СНАЧАЛА таймер, потом HP активного кольца. ⚠️ **ЭНЕРГОШЛЕЙФ** (`powered`, узел `print_cable`): юнит-реактор ВОССТАНАВЛИВАЕТ таймер на расстоянии (`_updateState`, темп `recharge·CABLE_RECHARGE_MULT` — медленнее супер-чарджера базы; рейдер-кража `siphoned` приостанавливает). Раньше только «держал» — теперь дозаряжает. | `spec_enemies.md` |
| `firewall.js` | ФАЙРВОЛЛ базы (`Firewall`, `game.firewall`): взломщики у базы копят 3 сегмента; узел `amb_fw` замедляет; полный = `breached` → гейм-овер 'hack'. ⚠️ ЮНИТ на взлом НЕ влияет (защита = УБИТЬ хакеров турелями/боем); без активных хакеров файрволл сам восстанавливается (`FIREWALL_DECAY`). Логика, виджет — `render_firewall`. | `spec_enemies.md` |
| `cycle.js` | Таймер-фаза мира (~75с) — волны по циклам. | `spec_enemies.md` |
| `enemy.js` / `ai.js` | Враги + мозги/спавн (ai домешан в `Game.prototype`, ПОСЛЕ game). Копатель/собиратель/рейдер + ЛЕТАЮЩИЕ боевые (`ENEMY_FLYERS`): **охотник** (таран, РАНИТ юнита), **взломщик** (взлом файрволла), **снайпер** (дистанц. выстрелы, кайт). **+7 ТИПОВ** (ступенчатый ввод по `WAVE_CYCLE`, эскалация потолка/пачки в `onCycleStart` — `cap()`/`pack()`): **мошкара** (`swarm_midge`, рой-мелочь, контактный укус — сносится AoE), **залежень** (`lurker`, рождается В ГНЕЗДЕ → роет к юниту `travel` → `_lurkerBurrow` зарывается в стенку хода → `buried`-фриз → выпад через `cstate='charge'` → снап в нору), **закладка** (`mine_planter`, копатель-смертник к базе → `armed` ждёт → юнит близко → телеграф-мигание → `_mineBlast`), **латальщик** (`mender`, летун-саппорт, лечит раненых союзников `nearestWounded`, не себя), **таран** (`siege_ram`, наземный структуролом-мили, разгон как охотник, цель — постройки ВКЛ. СТЕНЫ, гейт «есть структуры»), **мортира** (`siege_mortar`, дальний навесной структуролом, кайт от юнита, `shots.fire(...,MORTAR_DMG)`, гейт «есть структуры»), **скверносей** (`blight_sower`, наземный краулер, роняет маяки помех — подсистема в `blight.js`). ⚠️ осадные типы гейтятся `structures.list.length>0`; боевые брейны читают `seeU = u.stealthT<=0` (стелс). ФАЗА 4: боевые бьют структуры; рейдер/взломщик доходят В ПЕЩЕРУ базы (копатель её пробивает, не «под фундамент»). Скан врага — РАЗОВО на тип. ⚠️ **СТРЕЛЬБА — LoS:** снайпер/мортира стреляют ТОЛЬКО при прямой видимости по пустым тайлам (`world.hasLineOfSight`, строгая — не сквозь породу; заменила ленивый `_hunterLOS`, который остался у охотника-тарана). ⚠️ **ОТБРОС:** отталкиватель-структура ставит `e.knockT`/`knockVx`/`knockVy` → ПЛАВНОЕ смещение в `enemy.update` (приоритет над всеми состояниями, стоп у породы; не телепорт). СМЕРТЬ: `damage()` → `dying`+`deathT`, `ai` чистит. **ДРУЖЕСТВЕННАЯ ФРАКЦИЯ** (флаг `e.friendly`, разбуженный город — `spawnFriendlyCity(c)`): копатель+сборщик в `this.enemies`, мозги ветвятся на `friendly` (копатель — локальная копка у дома; сборщик — собирает→склад в `cavern.loot`, не исчезает); тикаются и в `storyMode` (в `updateEnemies` гейт `story && !friendly`); НЕ цели турелей/скана/радара/алерта (везде `!e.friendly`). Рендер — лазурный ореол (`render_enemy`). Детали — `spec_enemies`. | `spec_enemies.md` |
| `structures.js` | Печатаемые игроком оборонные структуры (`Structures`, `game.structures`): стена/шипы (пассив) + турель/батарея (актив, энергия от юнита-реактора в радиусе; батарея — релей). Турель — хитскан+трассер (турель/рейлган/СВЧ — гейт видимости `_los`, не сквозь породу). **ОТТАЛКИВАТЕЛЬ** (`repulsor`): в радиусе пусто → молчит; враг вошёл (и кулдаун истёк) → ОДНА волна ПЛАВНО отбрасывает всех НА ДИСТАНЦИЮ РАДИУСА наружу (через `e.knockT`, НЕ телепорт) → кулдаун (`REPULSE_PUSH_DUR`). **ОСАДНАЯ БАШНЯ** (`siege_tower`, b:'siege', узел `vault_siege` после рейлгана): цель — НЕ враги, а ближайшее живое ДИКОЕ ГНЕЗДО в радиусе → авто-резонанс `damageWild` (площадь, не луч; контраст ручному `siege.js`); `active2`/`pulse`/`siegeTarget` для рендера. **КУРЬЕР-ТЕРМИНАЛ** (`courier`, b:'courier', `kind:'depot'` — энергии НЕ требует, узел `vault_courier`): НЕ боевая, ЛОГИСТИКА — `_courierTick` принимает груз юнита ВНЕ базы (`!atBase`, радиус `COURIER_DEPOSIT_R`) в склад `s.store`/`s.stored` по единице; контейнер полон (`def.store`) И станция свободна (`!s.droneOut`) И не идёт перестройка (`s.launchCd<=0`) → `game._launchCourier` (летящий дрон — `courier.js`). У терминала ОДИН дрон: `s.droneOut` держится, пока дрон в рейсе туда-обратно (естественный «кулдаун»); сбит → `s.launchCd=COURIER_REBUILD_TIME`. Логика, без Canvas; рендер — `render_structure`. ⚠️ боевые цели — только враги (везде гейт `!e.friendly`). ⚠️ `wrapDeltaPx(a,b)=a−b` (направление s→цель = `wrapDeltaPx(target,s)`). | `spec_structures.md` |
| `print.js` | Поток печати (домешан в `Game.prototype`, ПОСЛЕ game). Принтер = `MODULE_DEFS.print` в доп-слоте (`unit.modules.aux==='print'`, unlock `vault_hub`). Режимы place→build: лок юнита (`unit.frozenPrint`), голограмма по мыши (R-поворот), ЛКМ/Пробел печать, Esc-отмена без траты; ресурс из `inventory.cargo` СПИСЫВАЕТСЯ при завершении. HUD-панель — `hud.drawPrintHud`. | `spec_structures.md` |
| `impulse.js` | ИМПУЛЬСНЫЙ БУР (домешан в `Game.prototype`, ПОСЛЕ game). Вариант бура `MODULE_DEFS.drill_impulse` (unlock `mast_di`, `s.impulse`): пассивно НЕ грызёт (unit.js гасит grind при `s.impulse`), копает ВОЛНОЙ. Заряд удержанием Пробела (`game.imp`, лок `unit.frozenImpulse`), выстрел = луч `setAir` + урон врагам. Длина — узел `mast_di_len`; сила/урон — трек «СИЛА УДАРА» (`impforce`). Рендер — `render_impulse`. | `spec_unit.md` |
| `siege.js` | ОСАДНЫЙ МОДУЛЬ (домешан в `Game.prototype`, ПОСЛЕ game). Доп-модуль `MODULE_DEFS.mod_siege` (доп-слот `aux`, `s.siege`, unlock `print_siege` — после канонира). ДОП-действие (цифра от менеджера, `actionKeys('siege')`): заряд удержанием (`game.siege`, лок `unit.frozenSiege`) → сфокусированный пробойный ЛУЧ-копьё по взгляду. Бьёт hp ближайшего ДИКОГО ГНЕЗДА на линии (`damageWild`, `SIEGE_DMG_CITY`) + врагов (`SIEGE_DMG_ENEMY`). Без снарядов; кулдаун. Контраст импульсу (волна). Рендер — `render_siege`. | `spec_unit.md` |
| `stealth.js` | СТЕЛС-МОДУЛЬ (домешан в `Game.prototype`, ПОСЛЕ game). Доп-модуль `MODULE_DEFS.mod_stealth` (`s.stealth`, unlock `kart_stealth`). ДОП-действие (цифра от менеджера): РАЗОВАЯ активация (как радар, по кулдауну) → `unit.stealthT=STEALTH_DUR` → юнит НЕВИДИМ для боевых (`ai.hunterBrain`/`sniperBrain` читают `u.stealthT` через `seeU` — охотник теряет цель, снайпер не стреляет). Юнит свободно двигается (без заморозки). `game.stealth={t,cd}`. Рендер — `render_stealth`. | `spec_unit.md` |
| `jam.js` | ВЗЛОМ ЮНИТОВ (домешан в `Game.prototype`, ПОСЛЕ game). ⚠️ НЕ отдельный модуль — СПОСОБНОСТЬ модуля ВЗЛОМА (`mod_hack`, `s.hack`), которую включает узел `kart_stun` (`inventory.getStats` ставит `s.jam` при `m.hack && metaHas('kart_stun')`). ДОП-действие (цифра от менеджера): импульс-ГЛУШЕНИЕ (`_jamPulse`) — всем боевым врагам в `JAM_PULSE_R` ставит `e.slowT=JAM_PULSE_DUR` (как глушилка-структура → скорость ×`JAM_SLOW`), потом кулдаун. `game.jam={cd,pulse}`. Рендер — `render_jam` (расходящееся кольцо). ⚠️ флаги aux-модулей (`hack`/`siege`/`stealth`) + производный `jam` ставит `inventory.getStats` (без них способность мертва в реальной сборке). | `spec_unit.md` |
| `artifact.js` | АРТЕФАКТЫ (домешан в `Game.prototype`, ПОСЛЕ game). Погребённые объекты `2×1/1×2` (`world.genArtifacts` сидит `ARTIFACT_SEED_COUNT` штук из `ARTIFACT_POOL` БЕЗ повторов → разный набор/сессию; редкие — глубже). Откопал ВЕСЬ объект + юнит ВОЗЛЕ → модалка (mode `'artifact'`). Карты: техно×N (`a._offer`) · ДАННЫЕ городу · ПЕРЕРАБОТКА (`artifactChoose(idx)` по offer: idx<N техно, idx==N данные, idx==N+1 переработка). **СЛОТЫ** (`game.artifactSlots={city,unit,drone}`; ЗАЛОЧЕНО при установке — DK: слот занят → техно недоступна). Кап ДИНАМИЧЕСКИЙ: `_artifactSlotCap(slot)`=`ARTIFACT_SLOT_CAP`+узел `kart_slot_<slot>`(+1); `artifactSlotFree`/`_artifactSlotUsed` читают ЕГО. **ДВОЙНОЙ ВЫБОР** (узел `kart_dual`): `_buildArtifactOffer` добавляет 2-ю альт-техно (`_rollOfferTech`) → 4 карты. ⚠️ **РЕЖИМ ИСТОРИИ:** реликты с флагом `combat` (атака/защита-от-врагов/взаимодействие с гнёздами — `combat_drill`/`thorns`/`stun_pulse`/`blast_charge`/`city_shield`/`drone_hacker`) ИСКЛЮЧЕНЫ из пула при `storyMode` — `_rollOfferTech` их фильтрует, а боевой РОДНОЙ `a.tech` подменяется не-боевым в `_buildArtifactOffer` (некого бить/защищать; общая защита от урона — броня/щит/поглотитель — остаётся: хазарды бьют и в истории). **ПОВТОРНЫЙ АНАЛИЗ** (узел `kart_reroll`): `_artifactReroll` меняет техно, цена `ARTIFACT_REROLL_COST` КРИСТ. из `inventory.cargo`, лимит `artifactRerollMax()`=1(+1 за `kart_reroll2`)/забег (`game.artifactRerolls` сброс на старте); клавиша R / кнопка (`_artifactRerollRect`). `artifactHas(id)` — гейт эффектов (как `metaHas`); `_applyArtifacts()` шлёт флаги/скалированные значения в `unit.stats` + `city.shieldMax` — зовётся ПОСЛЕ каждого `setStats` (onChange) и на установку; ОНО ЖЕ зовёт `_syncDrone` (дрон-слот) и `upgrades.syncArtifactTracks`. **ПУЛ — 21 реликт** (`ARTIFACT_POOL`, 3 слота unit/city/drone): бой-бур (`_combatDrillTick`) · щит города (`city.js`) · защита — бронепластины/энергощит/поглотитель/ответный контур/эхо-бур (эффекты в `unit.hurt`/drill-блоке) · активки — ЭМИ-импульс/подрыв-заряд/нано-ремонт (`artifacts_active.js`, кнопки-цифры) · сложные — форсаж бура/рывок/гарпун/рентген (`artifacts_active.js` + хуки в `unit`/`borers`/`render_light`) · детектор данных (город, компас — `artifacts_active.updateDataDetector` + `render_radar.drawDataCompass`) · 5 дронов (`drones.js`). **ГОРОДСКИЕ АПГРЕЙДЫ (Батч 6):** на каждый реликт — трек ≤3 ур. (`ARTIFACT_UP` в `constants.js` = `{base,step,cap}`, единый источник); уровень `_artLvl(id)`, значение `_artScaled(id)=base+lvl·step`; стат-эффекты скалируются в `_applyArtifacts` (s.armorMult/thornsDmg/echoDrillChance/overdriveBonus/harpoonRange/dataDetectR…), логик-эффекты читают `_artScaled` на месте; треки добавляются ДИНАМИЧЕСКИ при установке (реликт найден середи забега) — `Upgrades.syncArtifactTracks`, секция «РЕЛИКТЫ» в `render_upgrades`. **АНИМАЦИЯ ВЫБОРА:** `artifactChoose(idx)` не применяет сразу — ставит `game._artChoose={idx,t,dur=ARTIFACT_CHOOSE_ANIM}` (клики/клавиши заблокированы), тик в game-цикле (`mode==='artifact'`); `render_artifact` сворачивает невыбранные карты в центр + гасит, выбранную разгорает; по концу `_artifactResolve(idx)` применяет + потребляет объект. Рендер — `render_artifact`. | `spec_world.md` |
| `artifacts_active.js` | АКТИВНЫЕ/ПАССИВНЫЕ РЕЛИКТЫ (домешан в `Game.prototype`, ПОСЛЕ game). Логика реликтов-доп-действий и пассивов: `updateArtifactsActive` (ЭМИ-импульс `stunPulse`→`e.stunT` · подрыв-заряд `blastCharge`→`_artifactBlast` воронка+урон, своих не бьёт · нано-ремонт `nanoRepair` хил во времени) · `updateDash` (рывок `unit.dashing`/`_dashStep`) · `updateHarpoon` (притяг к стене через dash-машинерию; нет стены → холостой) · `updateDrillOverdrive` (форсаж: нагрев `unit.drillHeat` от копки → множитель силы, перегрев → лок `drillOverheatT`) · `updateXray` (рентген: `unit.xrayR` вскрытие тумана с затуханием — читает `render_light.drawFog`) · `updateDataDetector` (детектор данных города → `game.dataCompass`). Флаги — `unit.stats.*` (artifact `_applyArtifacts`); кнопки/цифры — `ACTION_DEFS`. Рендер — `render_artifact` (drillHeat/dash/harpoon), `render_light` (xray), `render_radar` (dataCompass). | `spec_world.md`/`spec_unit.md` |
| `drones.js` | ДРОНЫ-КОМПАНЬОНЫ (домешан в `Game.prototype`, ПОСЛЕ game). ПО ОДНОМУ на установленный дрон-реликт (дрон-слот базово 1, узел `kart_slot_drone` → +1 = 2 дрона): `game.drones[]` (запись `{srcId,kind,…}`), `_syncDrone` реконсилирует список со слотом, `_droneOfKind(kind)` — поиск (доп-действие хакера / actionbar); `updateDrones` ведёт КАЖДОГО, ветвится по `kind`: **collector** (пылесос дропа `loot.drops`→трюм) · **courier** (пачка из трюма→город) · **battery** (питание→`city.timer`, НЕ структуры) · **scout** (`world.reveal` по пути к неразведанному гнезду, иначе патруль) · **hacker** (деплой по кнопке `droneHack` → `_droneDeployHack` → канал у сердца гнезда → `_sabotageWild` → смерть → кулдаун). Летает над рельефом (`_droneFly`/`_droneOrbit`). Параметры скалирует город-апгрейд (`_artScaled`). Рендер — `render_drones`. ⚠️ `nearestWild` возвращает `{wild,dist,…}` — берём `.wild`. | `spec_world.md` |
| `render_drones.js` | Рендер дрон-компаньона (`drawDrones`, `game.drone`): капсула-корпус (тон по типу `DRONE_COL`) + ротор-черта + ядро-глаз + несомый груз + канал-луч взлома (hacker) + кольцо-прогресс. ПОВЕРХ тумана. ⚠️ перф: без `ctx.filter`/`shadowBlur`, `'lighter'` точечно. | `spec_world.md` |
| `borers.js` | ВИНТОВОЙ БУР (домешан в `Game.prototype`, ПОСЛЕ game). Вариант бура `MODULE_DEFS.drill_screw` (unlock `mast_ds`, `s.screw`): юнит не грызёт — Пробелом запускает АВТОНОМНЫЕ щиты (`game.borers`), что прямой укреплённой проходкой роют (`setAir(...,noTrigger)` — без осыпания, `tile.screw`+`tile.screwAxis` метки), сами не стоят; Пробел рядом — возврат. ⚠️ запуск/возврат — ТОЛЬКО стоя (`unit.state===IDLE`) и НЕ от клика кнопки действия (`_actionHeld`): иначе Пробел ложно срабатывает В ДВИЖЕНИИ (тот же фикс, что у активации города). Медленно (`SCREW_DIG_BASE`=0.275). Анкер к породе (бурит в любую сторону, в т.ч. вверх) + гравитация в пустоте — `spec_unit`. Ёмкость `stats.borerMax` (2 + `mast_ds_b1/b2` → 4); скорость — трек «СКОРОСТЬ ПРОХОДКИ»; узел `mast_ds_scan` → щиты снимают туман. **АПКИП:** у щита автономный ЗАРЯД (`b.charge`/`maxCharge` = `borerLife()` = `SCREW_CHARGE_MAX`+трек `borerLife`); тратится пока активен; иссяк → `b.depleted` (щит ЛЕЖИТ, не копает); юнит в `SCREW_RECHARGE_R` → `_borerRecharge` копит за `SCREW_RECHARGE_TIME` (пульс-анимация) → оживает (если не «забрали» Пробелом). Узел меты `mast_ds_life` (ветка бура, `+SCREW_LIFE_BONUS`) ↑ заряд (16→32с). Рендер — `render_borer` (полоска заряда+«!»+пульс); HUD-статус — `hud.drawBorerStatus`. | `spec_unit.md` |
| `scanners.js` | РАДАР + ЭХО-сканеры (домешан в `Game.prototype`, ПОСЛЕ game) — варианты слота СКАНЕР (`MODULE_DEFS.scanner_radar`/`scanner_echo`, unlock `mast_rad`/`mast_ech`, `s.radar`/`s.echoScan`). ОБА — ДОП-действие (цифра от менеджера, `actionKeys('radar'/'echoScan')`, `_scanWantFire`) на **КУЛДАУНЕ**. РАДАР: по активации ОДИН оборот развёртки (`RADAR_SWEEP_PERIOD`) — залежи/враги в пройденном клине вспыхивают БЛИПАМИ; туман НЕ снимает; HUD-виджет выбора типа — 3 кнопки-сегмента разом (ЖЕЛ/ОРГ/КРИ, ширина `HUD_VW`, активная подсвечена; клик по кнопке = `radarSetType`, клавиша C = `radarCycleType` циклом), узел `mast_rad_spec` снимает фильтр (виджет прячется — фильтровать нечего). ЭХО: волна (`game.echo.wave`) на `ECHO_R` (×2 от `mast_ech_len`) метит все залежи. Кулдаун обоих — ЗАЛИВКА иконки в панели активных действий (`drawActionBar`; виджет `drawScanCooldown` ОТКЛЮЧЁН, код оставлен); треки `radarcd`/`echocd` (ОХЛАЖДЕНИЕ). Стандартный сканер (радиус) — трек `scanner` ТОЛЬКО без радара/эхо. Рендер — `render_scanners`. | `spec_unit.md` |
| `falling.js` | Срыв нестабильной породы/валунов, урон, осколки. | `spec_world.md` |
| `hack.js` | ВЗЛОМ ГОРОДА — единая механика хакинга (домешан в `Game.prototype`, ПОСЛЕ game). Модуль взлома (`MODULE_DEFS.mod_hack`, доп-слот `aux`, `s.hack`, `act:2`, unlock `kart_defuse` — ПЕРВЫЙ узел линии ВЗЛОМА; но взлом ГОРОДОВ гейтит узел `kart_wake` — `updateHack` требует `metaHas('kart_wake')`) — ДОП-действие (цифра 2): удержанием у СЕРДЦА спящего города (на ПОЛУ каверны `floorY`, центр висит в воздухе → недосягаем; радиус `HACK_RADIUS`<скан) копит канал `HACK_TIME` → `_wakeCity` (`cavern.awoken`, push в `game.awakenedCaverns`, лог). Канал ЗАМОРАЖИВАЕТ юнит (`unit.frozenHack`, как импульс-заряд, в `updateHack` ДО `unit.update`); отпустил/ушёл — спад `HACK_DECAY`. `activeHack={city,t,hx,hy}` (рендер берёт сердце). **ПОБЕДА** (узел `kart_hackcity`): пробуждение → `_winTimer` (`HACKCITY_WIN_TIME`, HUD `drawWinTimer`) тикает в 'playing' (`updateWinTimer`) → mode `'hackwin'` (кат-сцена) → `_finishHackWin`: `overReason='hack_win'`, `directivesDone++`, бонус `META_WIN_BONUS` (строка в `computeMeta`), экран победы. Пробуждение → `spawnFriendlyCity` (автономная дружественная фракция, см. `ai.js`). **ВЗЛОМ ДИКИХ ГНЁЗД** (ДВА ТИРА): `updateHack` целит и `world.wilds` — канал у сердца копит `wild.breach` (сегменты `WILD_BREACH_SEG`, зеркало файрволла). Узел `kart_jam` «Саботаж гнезда» (база): полный → `_sabotageWild` (`saboted`, волны ЗАМЕДЛЕНЫ — `_waveSlowFactor` скейлит `cycle.update`, см. `wildcity.js`); гнездо ЖИВО. Узел `kart_breach` «Нейтрализация» (апгрейд `kart_jam`): полный → `_disableWild` (подавление, директива). `activeHack={wild,t,hx,hy,breach:true,neutralize}`; спад незавершённого — `_breachDecay` (саботированное залочено). Рендер — `render_hack` (breach-вариант: красный + засечки-сегменты). | `spec_world.md` |
| `wildcity.js` | ДИКИЙ ГОРОД КАК ЦЕЛЬ (домешан в `Game.prototype`, ПОСЛЕ game) — стержень директивы «устрани угрозу». Дикие гнёзда (`world.wilds`) имеют `hp/maxHp` (`WILD_HP`); три инструмента ведут к `disabled`: осадный модуль (`siege.js`) + осадная башня (`structures.js` b:'siege') бьют `damageWild`(hp→0); контр-взлом (`hack.js`) копит `breach`. `_disableWild` → волны из гнезда стоп (`ai.onCycleStart` фильтрует `!disabled`), маркер гаснет (`render_world.drawWildMarkers`: HP-полоса/вспышка/«ПОДАВЛЕНО»). ВСЕ подавлены → `_checkThreatCleared` → ПОБЕДА (`overReason='threat_win'`, `directivesDone++`, экран `STR.gameover.*.threatwin`). `wildHeart`/`nearestWild` — хелперы цели. | `spec_world.md` |
| `actionbar.js` | МЕНЕДЖЕР + ПАНЕЛЬ ДЕЙСТВИЙ (домешан в `Game.prototype`, ПОСЛЕ game). **`_actionList()` — ЕДИНЫЙ менеджер раскладки:** активные действия по `unit.stats` (`ACTION_DEFS`, поле `key`: `'primary'`=Пробел для бур-слота / `'digit'`=доп-действие), и ДИНАМИЧЕСКИ назначает цифры 1,2,3… доп-действиям ПО ПОРЯДКУ активных (без коллизий/пробелов: радар+осада → 1,2, а не 1,3). Источник не важен — модуль ИЛИ артефакт (стат-флаг); новое действие = запись в `ACTION_DEFS` + флаг в stats, кнопка/цифра добавятся сами. `actionKeys(stat)` отдаёт назначенные клавиши логике модуля (hack/siege/stealth/radar/echoScan) — клавиша в логике и цифра на кнопке ВСЕГДА совпадают (нет дубля `MODULE_DEFS.act` — удалён). Клик/удержание (`actionBarClick`/`Release`) ИНЖЕКТИТ хоткей в `input`. ⚠️ клик у базы НЕ открывает апгрейды (гейт `!_actionHeld`). Рендер — `render_actionbar`. | `spec_ui.md` |
| `cable.js` | ЭНЕРГОШЛЕЙФ — ЛОГИКА (домешан в `Game.prototype`, ПОСЛЕ game). `_cableUpdate(dt)` ведёт ФИЗИЧЕСКИЙ трейлинг-кабель `game.cable={path,len,powered,exhausted,batt,state,grayT,fallT}`: при смене тайла юнита ВЫТРАВЛИВАЕТ точку (длина `upgrades.cableLen()`, узел `print_cable`) или СМАТЫВАЕТ. Корень — тайл спавна (`_cableRootTile`). Запитан, пока юнит держит КОНЕЦ (`powered`→`city.update`). **СТЕЙТ-МАШИНА:** `live`→ оторвался → анимация «в серое» (`grayT` свип) → БЕЗ `print_batt`: `collapsing` (РУШИТСЯ ВЕСЬ, `fallT`) → `lost` (пропал; новый трос только у БАЗЫ-корня) · С `print_batt` («Энергорелеи»): `dormant` (серый ВИСИТ, реконнектится возвратом юнита) + **ЯКОРЯ-БАТАРЕИ** (`c.anchors`): доп-действие `cableAnchor` (кнопка, гейт `metaNeed:'print_batt'`, `_cableToggleBatteryAnchor`/`_cableNearBattery`/`_cableAnchorInput`) заякоривает шлейф на батарею в радиусе `CABLE_ANCHOR_R` → бюджет длины отсчитывается ОТ ПОСЛЕДНЕГО ЯКОРЯ (reach растёт, цепочка из N батарей); за якорь не сматывается; та же кнопка открепляет; смерть батареи рвёт цепь. ⚠️ юнит-реактор ПИТАЕТ город (не наоборот; старый `c.batt` УБРАН). Сброс — `_cableInit`. Рендер — `render_cable`. | `spec_meta.md` |
| `jets.js` | ПРЫЖКОВЫЕ ДВИЖКИ — артефакт ЮНИТА (домешан в `Game.prototype`, ПОСЛЕ game; флаг `unit.stats.jets`). Доп-действие (цифра от менеджера, `actionKeys('jets')`) — **ТУМБЛЕР** (жать = ВКЛ/ВЫКЛ по ребру `input.pressed`, НЕ удержание): `unit.flying` (полёт через воздух, `unit._flyStep` — без анкера/гравитации; в породу не летит). Топливо `JETS_FUEL_MAX` тратится в полёте, опустело → **АВТО-ВЫКЛ** + ЛОК `JETS_CD` + дозаправка `JETS_REFILL`; кончилось в воздухе → падение. `game.jets={fuel,cd,on}`. Рендер — `render_jets` (выхлоп). | `spec_world.md` |
| `courier.js` | КУРЬЕР-ДРОН — летящий контейнер-курьер (домешан в `Game.prototype`, ПОСЛЕ game; парный к терминалу `b:'courier'` в `structures.js`). `_launchCourier(s)` отлепляет дрон со снимком склада терминала → `game.couriers`; `updateCouriers` ведёт его к базе (центр `PRINTER`) **по ВОЗДУШНОМУ пути** (`this.airPath` из `ai.js` — BFS по тоннелям/пустотам, как летные враги; НЕ сквозь породу): хранит `c.path`/`c.pathI`, ведёт от вейпоинта к вейпоинту, троттл-пересчёт `COURIER_REPATH` (терраин мог измениться); **путь заблокирован (airPath=null) → дрон ВИСИТ на месте и ждёт**, перепроверяя РЕДКО (`COURIER_WAIT_REPATH` — BFS без цели полный, не спамить), пока не откроется путь (юнит докопал) — НЕ летит сквозь породу. Боевой враг в `COURIER_INTERCEPT_R` бьёт (`COURIER_INTERCEPT_DPS`; обратный путь тоже опасен). Долетел → `_courierArrive` (груз в `deliveredTotal`/банк) → дрон ПОРОЖНИМ **ВОЗВРАЩАЕТСЯ** на свой терминал (`state='return'`, цель `homeX/homeY`, ссылка `c.term`) → `_courierDock` освобождает станцию (`term.droneOut=false`). Сбит → `_courierDown` (гружёный — груз ПОТЕРЯН; порожний — только дрон; терминал строит новый через `COURIER_REBUILD_TIME`). Терминал уничтожен пока дрон в пути → дрон гаснет у базы. `state`: `fly`→`return`→`docked` (+`arrived`/`down` — концовки). `game.couriers` сбрасывается на старте забега. Логика без Canvas; рендер — `render_courier` (порожний дрон на `return` — пустая коробка). | `spec_structures.md` |
| `blight.js` | МАЯКИ СКВЕРНЫ — подсистема скверносея (`blight_sower`, домешана в `Game.prototype`, ПОСЛЕ game). `blightSowerBrain` — наземный краулер ползёт/роет к юниту, по таймеру (`BLIGHT_SOW_FIRST`/`_INTERVAL`) роняет СТАЦИОНАРНЫЙ МАЯК (`_deployBeacon`, разнос `BLIGHT_BEACON_SPACING`, глоб.кап `BLIGHT_BEACON_GLOBAL_CAP`) в `game.blightBeacons[]` (HP `BLIGHT_BEACON_HP`). Маяк добавляет ВРЕМЕННЫЙ rad-источник `{x,y,r,cap:BLIGHT_BEACON_CAP=0.5}` в `world.radSources` → поднимает фон помех ≤50% в радиусе (его читает `world.radAt` ← глитч-оверлей + детектор загрязнения; ⚠️ `radAt` учитывает `s.cap`). `updateBlight` — юнит вплотную (`BLIGHT_KILL_R`) бурит маяк (`BLIGHT_DRILL_DPS`) → hp≤0 → источник снят, помехи спадают. Маяки НЕЗАВИСИМЫ от сеятеля. `game.blightBeacons` сброс на старте забега. Рендер — `render_enemy` (`drawBlightSower`/`drawBlightBeacons`). | `spec_enemies.md` |
| `hazards.js` | ПОГРЕБЁННЫЕ ОПАСНОСТИ (домешан в `Game.prototype`, ПОСЛЕ game) — останки роботов + старые мины (`world.robots`/`world.mines`, маркеры `t.robot`/`t.mine`; откоп `setAir`→`.dug`). РОБОТ-ВАРИАНТЫ (`robot.kind`, dug→wake-телеграф→эффект): **shooter** (fire веером через `game.shots`→dead) · **web** (`_robotWeb`: юнит в `WEB_R` → `unit.webT` замедление, издали избегается) · **latch** (`_robotLatch`: достал юнита в `LATCH_JUMP_R` → `unit.latchTiles` дебафф бурения, сброс по проходке ИЛИ по времени `unit.latchT`=`LATCH_TIME` — чтоб не висел вечно при простое бура, тик в game-цикле; не достал → подох) · **jam** (`_robotJam`: `game.scanJamT` — глушит снятие тумана) · **brood** (`_trigBrood` рой-кладка). Мёртвого СКАН при `kart_wreck`. **Дебаффы дублируются игроку: лог + мигающая плашка `drawDebuffBadge`.** МИНА: dug→blink→взрыв (`_mineBlast`). Узел `kart_defuse` → откоп ОБЕЗВРЕЖИВАЕТ. `updateHazards` в playing-цикле. Рендер — `render_hazards`. | `spec_world.md` |
| `render_hack.js` | FX ВЗЛОМА ГОРОДА: `drawHackFx` (читает `game.activeHack`): лиловая «интрузия» — РВАНЫЙ дрожащий тетер юнит→сердце + дата-биты + КОЛЬЦО-ПРОГРЕСС у цели + ядро-вспышка + процент (ОТЛИЧАЕТСЯ от синего конуса скана) · `drawWinTimer` (HUD-полоса ПЕРЕХВАТ РЕАКТОРА, верх-центр) · `drawWinCutscene` (кат-сцена победы — зеркало интро: ядро извлекается из сердца города → летит в юнит → вспышка интеграции). ⚠️ перф: `'lighter'`, без `ctx.filter`/`shadowBlur`. | `spec_world.md` |
| `render_actionbar.js` | Рендер ПАНЕЛИ АКТИВНЫХ ДЕЙСТВИЙ (`drawActionBar`, низ-центру): кнопка на действие из `_actionList` — тёмная плита + уголки (`_panelCorners`) + 1px рамка, простой процедурный глиф (`_actionIcon`: impulse/screw/radar/echo/hack) в акценте, мелкий ХОТКЕЙ сверху (`keyLabel`). Состояния: норма · ховер/нажатие/АКТИВНО (акцент+рамка). **ЗАЛИВКА ВСЕЙ иконки снизу-вверх** (`d.fill`: радар/эхо — перезарядка `1−cd/cdMax`, импульс/взлом — заряд/канал) — заменяет виджет кулдауна (`drawScanCooldown` отключён). Hit-rect'ы → `game._actionRects`. Гейт: playing, не печать/дебаг. ⚠️ **РАСКЛАДКА:** влезает в свободный зазор нижней полосы (между печатью слева и логом справа `HudLayout`) → центрируется ВНИЗУ; НЕ влезает (много кнопок + печать + длинный лог) → ПОДНИМАЕТСЯ на свой ряд НАД полосой (`bandTop-bh-gap`), а не уезжает за край/поверх лога; всегда кламп в пределах экрана. Лог капается по ширине (`hud._logTrunc`, `logMaxW`) → не лезет в печать. ⚠️ перф: без `ctx.filter`/`shadowBlur`. | `spec_ui.md` |
| `render_hazards.js` | Рендер опасностей: `drawRobots` (сломанный юнит-силуэт со щупальцами + красное сенсор-ядро при пробуждении, ПОД туманом где раскрыто) + `drawMines` (корпус с усиками + мигающий красный огонёк) + `drawHazardDebug` (ДЕБАГ-маркеры артефактов/роботов/мин A/R/M) + **`drawUnitDebuffFx`** (паутина-нити / комок-прыгун на буре — на юните) + **`drawDebuffBadge`** (HUD: мигающие плашки активных дебаффов с текстом, БЕЗ таймера, `STR.hud.debuff`). ⚠️ перф: `'lighter'`, без `ctx.filter`/`shadowBlur`. | `spec_world.md` |
| `traps.js` | ЛОВУШКИ (домешан в `Game.prototype`, ПОСЛЕ game). Погребённые маркеры `world.traps` (`world.genTraps`, тип acid/seismic/cavein; откоп `setAir`→`trap.dug`); `updateTraps` срабатывает РАЗОВО (`trap.triggered`) по типу: **кислотные нанороботы** (`_trigAcid` → `game.acidClouds` облако-DoT в радиусе воздуха, телеграф-разлёт→DoT→таймер; импульсом НЕ контрится) · **дышащая порода** (`_trigSeismic` → рок радиуса → `t.unstable` по вероятности+спад, без опоры → `unstableTriggers`; `game.seismicWaves` FX-волна-линза) · **разлом** (`_trigCaveIn` → воздух радиуса → ОБЫЧНАЯ порода, юнит/каверны не засыпает, кабель не рвёт). ⚠️ `_trigBrood` (рой-кладка) остался тут, но зовётся из `hazards.js` (переехал в `robot.kind`). Сброс на старте забега. Рендер — `render_traps`. | `spec_world.md` |
| `render_traps.js` | Рендер ловушек (буровые маркеры скрыты, как мины): `drawTraps` — КИСЛОТНОЕ облако (жёлто-зелёный химический шиммер нанороботов, НЕ лиловый) + СЕЙСМО-волна-ЛИНЗА (искажение, не цвет: `drawImage` bbox аннулюса кадра увеличенным → рефракция + яркий фронт). ПОВЕРХ тумана. ⚠️ перф: без `ctx.filter`/`shadowBlur`, линза — один `drawImage` (волна кратковременна). | `spec_world.md` |
| `containers.js` | КОНТЕЙНЕРЫ-ХРАНИЛИЩА (домешан в `Game.prototype`, ПОСЛЕ game). Погребённые тайники `world.containers` (`world.genContainers`, ~20: `CONTAINER_COUNT` глубокие + `CONTAINER_UP` верхние; маркер `t.container`, откоп `setAir`→`.dug`; тип+кол-во ПО РЕДКОСТИ — `CONTAINER_LOOT` (weight+min/max на тип: железо 6-10 частых / крист. 2-4 редких)). `updateContainers` (автоскан-модель как `datascan.updateServers`): юнит в `CONTAINER_RADIUS` от выкопанного контейнера + узел меты **`kart_hackbox`** → таймер `c.breach` (`CONTAINER_BREACH_TIME`, копится пока юнит рядом) → `_openContainer` роняет ресурсы РЯДОМ (`loot.spawn`, как из породы). Без узла — `game.lockedContainer` (рендер-замок «нужен взлом»); идёт взлом — `game.activeContainer`. Рендер — `render_container`. | `spec_world.md` |
| `render_container.js` | Рендер контейнеров (`drawContainers`, только выкопанные+невскрытые+в раскрытом): крейт-корпус + замок-ядро (тон ресурса) + анимация ВЗЛОМА (`activeContainer`: янтарное кольцо-прогресс сейф-крекинга + дребезг-биты + процент) + красная дужка-замок при `lockedContainer` (нет узла). В мире ПОД туманом. ⚠️ перф: без `ctx.filter`/`shadowBlur`, `'lighter'` только на FX взлома. | `spec_world.md` |
| `nav.js` | A*-ДВИЖОК навигатора «до города»: `navFindPath` по тайлам, ДВА ПРОХОДА (+ модульные массивы, аллоцируются при загрузке → грузится ДО game, ⚠️ НЕ переносить). Модель = локомоция юнита: ход/лаз (клинг) + СРЫВ-ПАДЕНИЕ (`_navFall`) + ПРОКОП породы. Проход 1 — ТОЛЬКО открытые тайлы (приоритет открытому ходу ЛЮБОЙ длины); если нет — проход 2 с прокопом. Открытый ход всегда побеждает прокоп → путь находится всегда. 4-связно, тор по X. Обвязка/триггер/рендер/тумблер — `nav_run.js`. | `spec_meta.md` |
| `nav_run.js` | НАВИГАЦИЯ + МАЯЧОК — обвязка на `Game.prototype` (домешана ПОСЛЕ game). НАВИГАЦИЯ (узел `amb_nav`): «пора домой» (`_navReturnTime>=city.timer`), троттл-путь (`_updateNavPath`), рендер линии (`_drawNavPath`), тумблер ПУТЬ (`navClick`/`navView`, клавиша N). МАЯЧОК ГОРОДА (узел `amb_beacon`, РОДИТЕЛЬ nav/predict): `_drawCityBeacon` — янтарная СТРЕЛКА к базе вокруг юнита + дистанция (как стрелки винтового бура), всегда; тумблер ГОРОД (`beaconClick`/`beaconView`, клавиша G). Оба тумблера СТЕКАЮТСЯ (маячок сверху, путь под ним — `hud.navHudRect` смещается, если есть `amb_beacon`). A*-движок — `nav.js`; виджеты — `hud.drawNavToggle`/`drawBeaconToggle`. | `spec_meta.md` |
| `datascan.js` | ИЗВЛЕЧЕНИЕ ДАННЫХ → КОДЕКС (домешан в `Game.prototype`, ПОСЛЕ game). Автоскан выкопанных СЕРВЕРОВ (`updateServers`) и вражеских ЮНИТОВ (`updateEnemyScan`) в радиусе сенсора, детект ПЕРВЫХ встреч (`checkDiscoveries`→`discover`+подсказки), объёмный скан ПЕЩЕР-сцен (`updateBackdrops`/`_backdropDone`). Скорость — `_scanT` (узел `kart_hub`), выдача — `_dataGain` (узел `kart_data`) + попап на месте HUD-кольца (`_codexAnchor`). Персист опознанных типов — `save.idTypes` (`_idKnown`/`_idMark`). ⚠️ НЕ путать со `scanners.js` (РАДАР/ЭХО-модули) и `render_scan.js` (лучи). | `spec_codex.md` |
| `dust.js` | Косметические частицы: КРОШКА (грубая, гравитация) + тонкая ПЫЛЬ (лёгкая, оседает с дрейфом, висит — не дым). Бурение = крошка+пыль (`drill` — дефолт-бур; `borerDrill(dt,b)` — АВТОНОМНЫЕ винтовые щиты у фрезы, крошка летит назад, таймер `b._dustT`; зовётся в `game.loop` per-борер); `burst` — развал тайла (импульс/пробой/щит); фон с «потолка» = рандомно крошка/пыль/вместе, РЕДКО, только в видимом окне. Логика, без Canvas. | `spec_render.md` |
| `visions.js` | Видения в темноте (логика; со стороны, противоположной взгляду). | `spec_render.md` |
| `radar.js` | Детектор загрязнения (`RadarCompass`): пеленг на очаг в зоне фона + симуляция частиц-мороси («Гейгер-ветер»), «живые»/неточные показания. СВОЙСТВО СКАНЕРА (узел `mast_sr`), не модуль. | `spec_world.md` |
| `hints.js` | Крупные сюжетные подсказки (одна за раз). | `spec_ui.md` |
| `intro.js` | Заставка печати юнита (фазы, пропуск Пробелом). | `spec_ui.md` |
| `loot.js` | Дроп ресурса (гравитация, подбор r=1, втягивание ~0.55с; трюм полон → лежит). | — |
| `effects.js` | Лёгкие частицы: сдача груза (`burst`) + всплывающий зелёный «+» лечения (`heal`, на тиках реген HP) + всплывающая НАДПИСЬ (`text(px,py,str,color)` — разовый поп, поднимается и тает; напр. `+14С` при расщеплении кристалла) + УДАР-ИСКРЫ (`hit`, референс — обстрел брони): ПУЧОК быстрых искр-КОМЕТ от точки урона, подчинённых ЛИНИИ ОГНЯ (`dir`=источник→жертва): ~55% рикошет ВДОЛЬ огня (джет) + остальные ВВЕРХ дугой (наклон по стороне огня); флаг `hot`→бело-раскалённое ядро. Триггер — `game._hitFxPass` (детект падения hp юнита/врага/структуры) + `_hitAwayDir`; счётчики `HIT_SPARK_*`; рендер — `render_fx.drawFx` (комета: тусклый хвост + яркое ядро + голова-точка, `'lighter'`). ⚠️ Удар-флэш-ОРЕОЛ (круг) при уроне УБРАН из `render_enemy`/`render_structure` — фидбэк урона ТОЛЬКО искры (`hitT` ещё тикает, задел). | `spec_render.md` |
| `projectiles.js` | Выстрелы врагов (`Projectiles`, `game.shots`): снайпер `fire`, летят по прямой, бьют юнита ИЛИ структуру на пути (`SHOT_STRUCT_DMG`, фаза 4)/гаснут о породу. Тик в `updateEnemies`; рендер — `render_enemy.drawShots`. | `spec_enemies.md` |
| `inventory.js` | Экран сборки ПЕРЕД забегом (drag модулей, живой IK-превью) + счётчики груза. Сборка ПЕРЕНОСИТСЯ между забегами (`save.build`, `bindSave`/`loadBuild`/`saveBuild` — дефолт = прошлый забег). ВЫБОР КОРПУСА — `availableHulls`/`setHull`/`computeHullTabs` (табы в НИЖНЕЙ ПОЛОСЕ левой колонки под превью — `layout.hullBand`; скрыты при 1 доступном; `core` всегда + `gun` при `metaHas('print_gun')`). ⚠️ **РАСКЛАДКА** (`computeLayout`): общая нижняя полоса кнопок (`btnY`/`btnH`) — СТАРТ под галереей СПРАВА (`layout.start`, ширина = галерея), выбор КОРПУСА под превью СЛЕВА (`hullBand`). ⚠️ МОДУЛЬ ↔ КОРПУС: `_moduleFitsHull(type)` гейтит вариант по `MODULE_DEFS[type].hullOnly` (осадный модуль `mod_siege` = `hullOnly:'gun'` → только «Канонир») — применён в галерее/`defaultBuild`/`loadBuild`/`getStats`/`_categoryActive`. ТОЛЬКО логика/раскладка/слоты/ввод; рендер — `render_inventory`. | `spec_ui.md` |
| `upgrades.js` | Апгрейды сессии (Dome Keeper): банк ресурсов, покупка удержанием. ТОЛЬКО логика/экономика/раскладка/ввод; рендер — `render_upgrades`. **ТРЕКИ РЕЛИКТОВ (Батч 6):** `syncArtifactTracks(ids)` ДИНАМИЧЕСКИ добавляет/снимает трек на установленный артефакт (cat `'artifact'`, ≤3 ур., из `ARTIFACT_UP`+`ART_UPG_META`+`STR.upgrades.artSub`) — реликты находят середи забега, поэтому НЕ через `init`-фильтр; `trCap` для них НЕ зависит от `mast_hub`. Эффект читает `game._artScaled(id)` (не `applyToStats`). | `spec_ui.md` |
| `unitdefs.js` / `anim.js` / `rig.js` | Блупринты деталей (данные) / чистый аниматор / резолв рига (один кадр: зеркало+поворот ±90°). ⚠️ ТРИ корпуса: `scout` (FK, легаси), `core` (`kind:'ring'`, дефолт), `gun` (`kind:'wheel'` «Канонир» — моно-колесо: `toothR` внешнее кольцо-зубья=встроенный бур `builtinDrill`, `ringR` втулка, слот `turret`, НОГ НЕТ; `legHub` обязателен даже без ног — `resolveUnitRig`/`ANIM.bob` его читают). `unitHasPart` знает `need:'turret'`. | `spec_unit.md` |
| `cannon.js` | АВТО-ТУРЕЛЬ канонира (домешан в `Game.prototype` ПОСЛЕ game). `updateUnitTurret` — только hull `kind:'wheel'` с `unit.stats.turret`: поворотная (`aimOverTop`/`turretAimed`/`TURRET_TURN_RATE`, как городские), сама бьёт ближайшего НЕ-friendly врага в `UNIT_TURRET_RANGE` по прямой видимости (`world.hasLineOfSight`) хитсканом (`UNIT_TURRET_DMG`, кулдаун `UNIT_TURRET_FIRECD`) → `unit._turretAim`/`_turretFlash` + трассер `_unitTurretTracers`. Зовётся в game-цикле для колеса. Рендер — `render_wheel` (`drawWheelTurret`/`drawUnitTurretFx`). | `spec_unit.md` |
| `legik.js` / `tentacles.js` | Движок IK-ног (FABRIK) + обёртка юнита. ⚠️ полный свод принципов и тюнинг — в спеке. | `design_legs.md` |
| `rigbridge.js` / `rig-core.js` | Приём рига из редактора: localStorage (один origin) / файл `rig-<hull>.js` (file://; кнопка «→ В ИГРУ» скачивает + кладёт в localStorage). ⚠️ спрайты неймспейсятся по корпусу (`<hull>:<key>`), КРОМЕ ГЛОБАЛЬНЫХ ключей `borer:*` (бур-щит) и `wheel:*` (моно-колесо) — они кладутся ПЛОСКО (иначе `render_borer`/`render_wheel` их не найдут); та же развилка в редакторе (`buildIngameFile`). ⚠️ **ФАЙЛ `rig-<hull>.js` НУЖЕН В ЗАГРУЗЧИКЕ** (`index.html`): экспорт переопределяет `UNIT_DEFS[hull]` (вкл. `drawScale`/`footAnchor`) + грузит спрайты — но только если файл в списке загрузки (сейчас там `rig-core`, `rig-gun`). Нет строки в загрузчике → ассеты/масштаб корпуса НЕ применяются (частая причина «ничего не передалось»). | `design_editor.md` |
| `render_world.js` | Кладка/жилы/маркеры/ассет города. ⚠️ ОДИН профиль кромки `_ragDepth` кормит мир, тень И клип света — менять синхронно. `clipVisibleAir` — окклюзия юнита. **ТРЕЩИНЫ БУРЕНИЯ** (`drawChunks`, гейт `t.dig>0`): ЕДИНЫЙ РЕКУРСИВНЫЙ ветвящийся разлом НА ВЕСЬ ТАЙЛ (не по осколкам), ПОВЕРХ кладки — `_crackBranch` (тонкие зигзаг-сегменты + форки + сужение к ОСТРЫМ кончикам). ⚠️ **РАСТЁТ, а не выдвигается:** геометрия ФИКСИРОВАННАЯ (кол-во веток/глубина/segLen НЕ зависят от prog, детерминированы `tileHash`), прогресс двигает `frontDist`=prog·maxReach (фронт прорастания, клип по накопленной длине `dist`) + `w0` (существующие трещины ТОЛСТЕЮТ). Растёт ОТ СТЕНКИ входа бура (`ex/ey`); общий для всех буров. **СЕРЫЙ ФОН ВИНТОВОГО ХОДА** (`screwBackColor(y)`): AIR-тайлы с `t.screw` заливаются холодным бетонно-стальным серым (вместо тёплого `backColor`) в обеих заливках воздуха `drawWorld` — контраст обычной пустоте. | `spec_render.md` |
| `render_light.js` | Туман/прожектор-конус (клип по видимой кромке)/пыль. ⚠️ `drawFog` читает `unit.xrayR` (реликт РЕНТГЕН): временное вскрытие тумана в радиусе вокруг юнита (мягкий край), НЕ трогая постоянный `world.seen`. | `spec_render.md` |
| `render_unit.js` / `render_ring.js` | FK-корпус (scout) / юнит-КОЛЬЦО (core, дефолт). ⚠️ налево = зеркало, НЕ поворот 180°. `ringModuleScreenPos` для лучей сканера. ⚠️ **при `unit.stats.screw` оба ПРОПУСКАЮТ деталь `p.kind==='drill'`** — вид винтового бура = ЩИТ (`drawCarriedBorer` в игре / `drawMountedBorer` на сборке), не дженерик-бур. `unitLightAnchor` (render_unit) — своя ветка на `kind==='ring'`/`'wheel'` (конус от бура/центра). | `spec_unit.md` |
| `render_wheel.js` | Рендер МОНО-КОЛЕСА (hull `kind:'wheel'`, `UNIT_DEFS.gun` «Канонир»): `drawWheelUnit` — кольцо-ЗУБЬЯ (`drawToothRing`, встроенный бур, ВРАЩАЕТСЯ: `updateWheelSpin` = качение на ЛЮБОМ ходу (гориз+вертик, `hypot(dpx,dpy)`) ×`WHEEL_SPIN_MOVE` медленно + `WHEEL_SPIN_DRILL` при бурении заметно быстрее; телепорт/респавн Δ>TILE не катит; **IDLE-ПОКАЧИВАНИЕ** стоя на месте (`idleActive`): колесо тихо катается вперёд-назад `WHEEL_IDLE_AMP`/`_FREQ`, зубья докручиваются синхронно `idleX/радиус` — в игре/редакторе/сборке; **ДВИГАТЕЛЬ ДРОЖИТ** вверх-вниз `ENGINE_VIB_AMP`/`_FREQ`) + КОРПУС-КОЛЬЦО (`drawWheelBody`, каркас гондолы, отцентрован; в IDLE **КОНТР-ВРАЩАЕТСЯ** — `spin = −idleSpin·flip·WHEEL_IDLE_COUNTER`, только АССЕТ, реактор/модули НЕ трогает: зубья катятся в одну сторону, внутреннее кольцо в другую; вне idle `idleSpin=0` → статично) + ВТУЛКА-реактор + модули (ang/rad, БЕЗ доворота — колесо всенаправленно) в ЕДИНОЙ z-сортировке (зубья `def.toothZ`, корпус `def.bodyZ`, реактор 10, модули 20, ТУРЕЛЬ по своему `z` детали → перекрытие ЛЮБЫХ слоёв настраивается z, в т.ч. из редактора). Турель рисуется ЭКРАННЫМ проходом (aim `unit._turretAim` мировой, не зеркалится flip'ом; ПОКОЙ турели = ствол ПО ВЗГЛЯДУ `faceX` (aim 0/π = канонический кадр редактора), не вверх — `cannon.js` `rest`; при взгляде ВЛЕВО турель ЗЕРКАЛИТСЯ `scale(-1,1)` как корпус, НЕ поворот 180° — иначе купол вверх ногами; ⚠️ спрайт турели рисуется ×`k=r/R` (= drawScale в игре, 1 в редакторе/сборке) — иначе в игре был бы огромным (спрайт fixed-size не ужимался с колесом)), но её `z` делит отрисовку слоёв на «под турелью»/«над турелью» (`drawWheelLayers` дважды). НОГ НЕТ. ⚠️ **ПОСАДКА НА ПОЛ:** `wheelGroundDy(unit)` — визуальный сдвиг вниз (низ зубьев на линию пола + `WHEEL_GROUND_SINK`), передаётся как `opts.dy` ТОЛЬКО в игре (`game.drawScene`; на сборке колесо центрируется), тот же dy добавляет `unitLightAnchor` (конус едет с колесом). Физика юнита остаётся на `unit.py` (как лаг-корпус ног). В игре колесо рисуется в клипе `clipVisibleAir` (ЗА породой, как щупальца) — крупный ассет не вылезает на камень. `blueprintScale` (inventory) для колеса меряет габарит по СПРАЙТАМ зубьев/корпуса + выносу модулей/турели (иначе крупный ассет резался кромкой панели). `wheelModuleScreenPos` (аналог ring, для лучей). `drawUnitTurretFx` — трассеры авто-турели. Ассеты: `wheel:tooth`+`wheel:body` (глоб.) + `gun:turret`/`gun:reactor` (per-part), фолбэк процедурный. ⚠️ перф: без `ctx.filter`/`shadowBlur`, `'lighter'` только на вспышке ствола. | `spec_unit.md` |
| `render_parts.js` | Спрайты деталей (namespacing `<hull>:<id>`), фолбэк-процедуры. `partSpriteId` — подмена спрайта детали на спрайт КОНКРЕТНОГО установленного варианта модуля (`mod:<id>`, по `unit.modules`). | `spec_unit.md` |
| `module_assets.js` | Манифест АССЕТОВ ВАРИАНТОВ модулей (ключ `<hull>:mod:<id>` → `{url,scale,off,rot}`) + СОСТАВНОЙ БУР-ЩИТ (ГЛОБАЛЬНЫЕ ключи `borer:mount`+`borer:shield`, читает `render_borer._shieldBody` напрямую). Генерирует редактор (под-список вариантов в карточке детали + секция «Бур-щит (составной)»). В репо — заглушка. | `design_editor.md` |
| `render_scan.js` | Серверы-маркеры/хлам + общий `drawScanBeam` (сервер/враг/пещера). | `spec_render.md` |
| `render_backdrop.js` | Фон пещер-сцен: ассет робота, паралакс, свип по маске ассета. | `spec_world.md` |
| `render_falling.js` | Трещины нестабильной породы, валуны, летящие блоки. | `spec_world.md` |
| `render_dust.js` | Частицы (`Dust`): крошка/камушек — `fillRect` (`bone`/`ash`); пыль — мягкий КЛУБ-дымка через КЭШИРОВАННЫЙ радиальный спрайт + `drawImage` (растёт+тает). В мире ПОД туманом/светом. | `spec_render.md` |
| `render_visions.js` | Рендер видений. ⚠️ БЕЗ `ctx.filter`/офскринов — перф-правила в спеке. | `spec_render.md` |
| `render_glitch.js` | Пост-помехи интерфейса от радиации (стадии по интенсивности). | `spec_render.md` |
| `render_impulse.js` | FX спец-буров: ИМПУЛЬСНЫЙ (`drawImpulseWave` узкое дрожащее искажение воздуха, `drawImpulseCharge` дуга заряда+стрелка) + КИНЕТИЧЕСКИЙ (`drawKineticHeat` полоска разгона). В мире поверх юнита/света. ⚠️ перф: без `ctx.filter`/`shadowBlur`. | `spec_unit.md` |
| `render_siege.js` | FX ОСАДНОГО МОДУЛЯ: `drawSiegeCharge` (сходящиеся скобки-фокус стягиваются к юниту по заряду + ось-копьё) + `drawSiegeBeam` (тугой яркий ЛУЧ-копьё на выстреле, гаснет). Намеренно ИНОЙ язык, чем у импульса/кинетики. ⚠️ перф: без `ctx.filter`/`shadowBlur`, `'lighter'` только на луче. | `spec_unit.md` |
| `render_stealth.js` | FX СТЕЛС-МОДУЛЯ: `drawStealthFx` (пока `game.stealth.t>0`) — лиловая «маскировка» поверх юнита: мягкий ореол + дрожащие сканлайны (расфокус силуэта), мигает в последние 1.5с. ⚠️ перф: без `ctx.filter`/`shadowBlur`, `'lighter'`. | `spec_unit.md` |
| `render_jam.js` | FX ВЗЛОМА ЮНИТОВ: `drawJamFx` (на импульсе, `game.jam.pulse` спадает 1→0) — расходящееся зелёное кольцо-помеха от юнита до `JAM_PULSE_R` + второе кольцо с задержкой. ⚠️ перф: без `ctx.filter`/`shadowBlur`, `'lighter'`. | `spec_unit.md` |
| `render_artifact.js` | АРТЕФАКТЫ: `drawArtifacts` (объект-реликт в породе) + `drawArtifactModal` (N карт из `a._offer`, ширина панели адаптивна; техно-карта показывает СЛОТ + ЗАНЯТОСТЬ `used/cap` + СВОБОДЕН/ЗАНЯТ по цвету, приглушение когда полн; кнопка ПОВТОРНЫЙ АНАЛИЗ при `metaHas('kart_reroll')` → `game._artifactRerollRect`; подсветка `game.artifactSel`, карты → `game._artifactRects`, число → `game._artifactChoiceCount`) + `drawCityShield` (артефакт `city_shield`: бирюзовый купол-полусфера над базой, яркость ∝ заряд `city.shield`, вспышка на попадании). ⚠️ перф: `'lighter'` на щите, без `ctx.filter`/`shadowBlur`. | `spec_world.md` |
| `render_jets.js` | ПРЫЖКОВЫЕ ДВИЖКИ (артефакт, `game.jets`): `drawJets` — ОДИН язык тёплого выхлопа по ЦЕНТРУ юнита, направлен ПРОТИВОПОЛОЖНО движению (`-u.dx/-u.dy`; парит → вниз), пока движки `on`; мигают при НИЗКОМ топливе. ⚠️ перф: `'lighter'`, без `ctx.filter`/`shadowBlur`. | `spec_world.md` |
| `render_scanners.js` | РАДАР-развёртка (`drawRadarSweep`: тусклый кобальт-веер + блипы залежей/врагов с затуханием) + ЭХО-волна (`drawEchoWave`: прозрачные лиловые кольца-искажение + метки) + HUD-виджет выбора типа: 3 кнопки-сегмента (`drawRadarSwitch`, ширина `HUD_VW`; активная подсвечена; хит-rect'ы кнопок → `radarSwitchButtons`, клик → `radarSetType`). ПОВЕРХ тумана (блипы сквозь тьму), приглушённо. ⚠️ перф: `'lighter'`, без `ctx.filter`/`shadowBlur`. | `spec_unit.md` |
| `render_borer.js` | ВИНТОВОЙ БУР: `drawBorers` (узкий проходческий щит + фреза-головка, анимация бурения, ПОВЕРХ тумана) + `drawCarriedBorer` (несомый щит, поворот с юнитом) + `drawBorerArrows` (узел `mast_ds_nav`: стрелки + дистанция к щитам) + `drawScrewTrail` (тихий след, рёбра ⊥ `t.screwAxis`, ПОД туманом). ⚠️ **СОСТАВНОЙ АССЕТ:** `_shieldBody` рисует СПРАЙТЫ, если в `PART_SPRITES` есть `borer:mount` (крепление, сзади) и/или `borer:shield` (бур-щит, спереди) — иначе процедурный корпус (фолбэк); масштаб `k`=калибр/`TILE·0.3`, искры-крошка (`_borerSparks`) поверх при бурении в обоих режимах. Ассеты грузит `module_assets.js` (генерит редактор). ⚠️ перф: без `ctx.filter`. | `spec_unit.md` |
| `render_cable.js` | ЭНЕРГОШЛЕЙФ (узлы `print_cable`/`print_batt`): `drawCable` рисует ФИЗИЧЕСКИЙ трейлинг-кабель по `game.cable.path` — идёт по пустым тайлам, пришпилен ШЕСТАМИ к БЛИЖАЙШЕЙ породе из 4 сторон (`hang`: потолок → свисает ниже / пол → держит чуть выше / стена → сбоку) на корне/изгибах/каждом 3-м/конце; МЕЖДУ шестами ПРОВИСАЕТ вниз (фестон, контрол-точка ∝ ГОРИЗОНТАЛИ пролёта → вертикаль прямая, в шахте без провиса), бежит искрами к юниту (только `live`). **ОБЕСТОЧИВАНИЕ:** жила/оплётка/узлы СВИПОМ уходят в серое (`grayT`, фронт от конца к корню); `dormant` (с реле) — серый ВИСИТ, вислый хвост; `collapsing` (без реле) — кабель срывается с шестов, ПАДАЕТ весь (`fallT`, drop+растворение) и пропадает; `lost` — не рисуется. **ЯКОРЯ** (`c.anchors`): лазурный узел-крепёж + линк к батарее на каждом; пульс-кольцо на батарее в радиусе якоря. Искры бегут ЮНИТ→БАЗА (реактор питает город). ПОВЕРХ тумана. ⚠️ перф: `'lighter'` на искрах, без `ctx.filter`/`shadowBlur`. | `spec_meta.md` |
| `render_courier.js` | КУРЬЕР-ДРОН (узел `vault_courier`, `game.couriers`): `drawCouriers` — бирюзовая капсула-корпус + несущие роторы + подвешенный контейнер + узкая HP-полоса (мигает при попадании); конец маршрута — вспышка-сдача (прибытие) или разлёт обломков (сбит). ПОВЕРХ тумана (виден маршрут в темноте). Терминал-структуру рисует `render_structure` (`drawCourierStruct`/`drawCourierFill`). ⚠️ перф: без `ctx.filter`/`shadowBlur`, `'lighter'` только на вспышках. | `spec_structures.md` |
| `render_enemy.js` | Дроны врагов (свой силуэт у типа: копатель/собиратель/рейдер + охотник-дротик/взломщик-интрудер/снайпер-ствол + новые 7: мошкара-блоха/залежень-горб/закладка-заряд/латальщик-эмиттер/таран-клин/мортира-ствол/скверносей-краулер) + `drawShots` (трассеры) + `drawBlightBeacons` (маяки скверны — кол-эмиттер+кольцо-помеха+HP-полоса, зовётся из `game.draw` отдельно) + `_drawEnemyHpBar` (узел меты `mast_hpbar` «Считыватель целостности»: узкая полоска HP над КАЖДЫМ не-дружественным врагом, цвет по доле — бирюза>0.5>янтарь>0.25>красный; гейт `metaHas('mast_hpbar')`) + `drawEnemyDeath` (затухающий обломок+искры при `dying`). ⚠️ глаза ВСЕГДА красные (`#d0402f`/`#ff6a4a`/`#ff9a4a`); зелёный — только инструмент (латальщик-эмиттер); перф: без `ctx.filter`/`shadowBlur`. | `spec_enemies.md` |
| `render_structure.js` | Структуры (`drawStructures`): процедурно стена-плита/шипы/турель(купол+ствол по `aimAng`)/батарея + энергобар/прогресс-бар/гибель/трассеры. В мире ПОД туманом. Ассеты — следующим этапом. ⚠️ перф-правила. | `spec_structures.md` |
| `render_alert.js` | ОБНАРУЖЕНИЕ УГРОЗ (узел меты `mast_sa`): голо-маркеры поверх врагов (весь экран) и нестабильностей породы (радиус сенсора) + HUD-тумблер (V/клик). Гейт `metaHas('mast_sa')`. | `spec_enemies.md` |
| `render_predict.js` | ИНДИКАТОР ВОЛНЫ под «ЦИКЛ N» (`drawWavePredict`, заменяет текст-секунды в `hud.js`): ПО УМОЛЧАНИЮ — только ПРОГРЕСС-БАР до след. цикла (нейтральный). С узлом `amb_predict` — бар + глиф/имя надвигающегося типа врага (`_waveGlyph`) в рамке + ТАЙМЕР ОБРАТНОГО ОТСЧЁТА (мм:сс) + тег «НОВАЯ» при эскалации; цвет угрозы разгорается к красному. Заголовок — `game._waveHeadline(cycle.n+1)` (тип по `WAVE_CYCLE`/`WAVE_TIERS`); лог «ПРОГНОЗ · НАДВИГАЕТСЯ: X» пишет `ai.js` при смене цикла. Гейт: playing, не дебаг/история. ⚠️ перф: без `ctx.filter`/`shadowBlur`. | `spec_meta.md` |
| `render_radar.js` | Виджет детектора загрязнения (HUD, «Гейгер-ветер», кибер-ацтек): линза-сонце с короной-интенсивностью + морось в ядро-глиф + внутренняя дуга-пеленг. Родня «КОЖУХА». Гейт `game._contamActive()`. **+ `drawDataCompass`** (реликт города `data_detector`): ТОЧНЫЙ компас-роза со стрелкой-пеленгом + дистанцией к ближайшему серверу с данными (`game.dataCompass`); тон кобальт; стек ПОД детектором загрязнения. | `spec_world.md` |
| `render_city.js` | Hibernation Widget — статус города одной капсулой. ⚠️ `_drawBarText` ТОЛЬКО читает поля `_bar*` — их шагает `city._stepBarText` (логика). | `spec_ui.md` |
| `render_inventory.js` | Рендер экрана СБОРКИ (`drawInventory` + хелперы): чертёж/живой риг-превью/галерея модулей/сводка. ДВА РАЗНЫХ визуала кнопок нижней полосы: **СТАРТ** («НАЧАТЬ СБОРКУ В МИРЕ», `_invDrawStart`) — сплошной золотой «люк»: плита + `_panelCorners` + левая hazard-полоса + сдвоенный шеврон + LED «готов»; **выбор КОРПУСА** (`_invDrawHullTabs`) — скошенные карточки-табы (скос верх-лев. угла) с процедурным глифом корпуса (`_invHullGlyph`: кольцо/моно-колесо) + кикер + имя, акцент корпуса (кобальт «Тор» / оранж «Канонир»). Свободные `draw*`, состояние — на `Inventory`. | `spec_ui.md` |
| `render_upgrades.js` | Рендер экрана УЛУЧШЕНИЙ (`drawUpgrades` + хелперы): шапка/кошелёк/прокручиваемые ряды-треки карточек. Свободные `draw*`, hit-rects/скролл — на `Upgrades`. | `spec_ui.md` |
| `render_firewall.js` | Виджет ФАЙРВОЛЛА (под капсулой, при атаке): 3 сегмента ацтек-кибер-лабиринта, взлом ползёт ПО ДОРОЖКЕ лабиринта (не заливкой снизу) + мерцающий фронт (вайб Eva-хака). | `spec_enemies.md` |
| `render_resource.js` / `render_module.js` / `render_loot.js` / `render_fx.js` / `render_hint.js` / `render_intro.js` | Язык форм ресурсов / иконки модулей / дроп+лапы / частицы / крупная подсказка / заставка. | `spec_ui.md` |
| `hud_layout.js` | РАСКЛАДКА HUD — единый аллокатор слотов (дизайн-система «виджеты не наслаиваются»). `HudLayout` (глобал): `begin(W,H)`/`slot(zone,w,h)`/`reserve(...)`/`mark(...)`/`validate()`. Зоны `HUD_ZONES` = `tl`/`bl`/`tc` (якорь+направление стека); виджет в зоне просит слот → стекается без наложения (порядок вызова = порядок отрисовки в `game.drawScene`); условный не рисуется → авто-рефлоу. Кликабельные слот-виджеты КЭШИРУЮТ нарисованный rect (клик читает кэш). `validate()` — DEV-сеть, ворчит в консоль на наложения (дедуп по смене набора; НЕ гейтить debug — там виджеты скрыты). ⚠️ загрузка ПЕРЕД render-группой (после `radar`). НОВЫЙ виджет = один `slot(zone,w,h)`; особый (центр/фуллскрин) — рисуй как раньше + `mark(...)` для валидатора. **СВЁРТЫВАНИЕ ЛЕВЫХ ВИДЖЕТОВ** (дёшево, для любого слот-виджета левой зоны): `slotDock(zone,w,h,id,accent)` ВМЕСТО `slot` → возвращает slid-x (виджет уезжает влево за экран при сворачивании) + регистрирует ЯЗЫЧОК-полоску цвета рамки со стрелкой (`←` развёрнут / `→` свёрнут); `drawDockTabs(ctx)` рисует язычки ПОВЕРХ (после левых виджетов, перед `validate`), `dockClick(x,y)` — тумблер по клику (роутить ПЕРВЫМ в mousedown). Одна карта состояний `_dock` + слайд по dt. Применён к алерту/эконом-чипам/радар-свитчу/компасам; ФИКС-панели (рисуются по своим координатам, не через `slot`) сворачиваются через `dockShift(id,baseX,baseY,w,h,accent)` — тот же `_dockApply`, но без выделения слота (ГРУЗ `cgx`/БАНК `bkx` в `hud.js`). Панель ЮНИТ — пока без язычка (статус всегда нужен). | `spec_ui.md` |
| `hud.js` / `ui_menu.js` | HUD-боксы+техно-рамки+кольцо извлечения+лог (`_logTrunc` — кап ширины) + `hudToggleSwitch(ctx,sx,sy,on)` (ПЕРЕИСПОЛЬЗУЕМЫЙ переключатель ВКЛ/ВЫКЛ — алерт/расщепитель) + `drawBorerStatus` (статус щитов винтового бура, зона `bl`, гейт `s.screw`) / меню+gameover-пересчёт. ⚠️ блок ЮНИТА КОМПАКТНЫЙ (`HUD_VH`=42: шапка+HP, без глубины/скорости/страты; ≈ высота блока таймера). HUD-виджеты берут позиции из `HudLayout` — не хардкодят координаты. | `spec_ui.md` |
| `meta.js` + `meta_dom.js/.css` | СЕТЬ ПАМЯТИ: граф+банк МТ + DOM-экран. 5 веток = категории анлоков, ВСЕ кастомные (юнит·синяя / ПЕЧАТЬ-ТЕЛ·красная[11 узлов РЕАЛИЗОВАНЫ — эффекты `metaHas('print_*')`, см. `spec_meta`; корпуса `print_slots/print_gun` — `wip`, нет в редакторе] / город·жёлтая / мир·фиол. / ПЕЧАТЬ-СТРУКТУР·оранж. — см. спеку); цены `META_TC` ×2 (16/20/32), коэф МТ ×0.5, узлы-анлоки модулей ×1.5; стартовый узел `core` ПОКУПАЕТСЯ и открывает ДОП-СЛОТ (`_categoryActive` гейт на `metaHas('core')`); `mast_hub` («Инженерный допуск») открывает ПОТОЛКИ апгрейдов (без него `trCap`→1; с ним бур 3 / сканер·груз·прочность 2); прочие эффекты узлов подключать через `metaHas(id)`. Обычный узел доступен при ЛЮБОМ владомом соседе; узел с полем `allDeps:[ids]` = КОНВЕРГЕНЦИЯ (несколько родителей, доступен только при ВСЕХ — `metaAvail`/`metaState` берут `.every`, как у `cap`; пример — `kart_hackcity` ← `kart_wake`+`kart_jam`). ⚠️ координаты узлов держат ПРАВИЛА РАЗМЕЩЕНИЯ — валидатор `_metaValidateLayout` ворчит в консоль на наложение/выход за холст/«втягивание» (правила — `spec_meta.md`); тесный буровой веер раскладывается КОНСТРЕЙНТ-СОЛВЕРОМ, не «на глаз» (валидатор не ловит пересечения проводов). ⚠️ холст авто-растёт во ВСЕ стороны (`MX/MY` — `let`, сдвигаются если узел уходит за лево/верх). ⚠️ временная клавиша K — полный вайп меты (тесты). | `spec_meta.md` |
| `codex_dom.js/.css` | БАЗА ДАННЫХ: кодекс-диски + глоссарий + внутриигровой попап (DOM-оверлей). | `spec_codex.md` |
| `game.js` | ТОЛЬКО оркестрация: режимы (menu/inventory/intro/playing/hackwin/upgrades/paused/gameover/progress/database; `hackwin` — кат-сцена победы взлома, см. `hack.js`), главный цикл, меню-навигация, склейка систем. Не наращивать — выноси подсистемы (скан данных → `datascan.js`, навигация → `nav_run.js`, AI → `ai.js` и т.д.). | по теме |
| `sandbox.js` | ТЕСТОВЫЙ ПОЛИГОН (домешан в `Game.prototype`, ПОСЛЕ game). Отладочный режим (флаг `game.sandbox`): полка породы под базой (`_buildSandboxTerrain`: воздух над/под, дно-ловушка, мягкая порода `SANDBOX_SHELF_HARD`) + ОДИН экземпляр КАЖДОГО объекта на **ПЕРВОМ тайле породы** (`SANDBOX_ROW_Y`, откоп в 1 тайл) в ОДНОМ ряду с чередующимися по высоте debug-подписями (`_buildSandboxObjects`: 4 ловушки/5 роботов/сервер/3 контейнера · все 26 реликтов в ряд · 3 пещеры-сцены-каверны отдельно). Бессмертие (hp≤0 → `_sandboxRespawn` у базы, БЕЗ гейм-овера — death-хук в game.js гейтит `sandbox`), всё открыто (`_sandboxUnlocks` = все `META_NODES` кроме `kart_defuse` — тумблер F; `metaHas` читает оверрайд), модули ЮНИТА — из ПОСЛЕДНЕГО забега (`inventory.loadBuild()` по `save.build`, все узлы открыты → любой сохранённый модуль валиден) + «бесконечные» ресурсы (`_sandboxRefill`), туман выкл. **НЕ ПИШЕТ В СЕЙВ:** `writeSave` заглушён флагом (save.js), на выходе (`exitSandbox` из `endRun`) сейв восстанавливается из снимка `_sandboxSnap` (in-memory дрейф `runs` откатывается; диск не трогался). Активация — **T** в гл. меню (`menuNav`); в забеге — **R** ПОЛНЫЙ рестарт (`resetSandbox`: пересобрать террейн+объекты + снести врагов/структуры/курьеров/лут + сброс цикла), **F** тумблер обезвреживания, **Esc**→меню. ⚠️ смерть юнита → `_sandboxRespawn` ТОЛЬКО оживляет юнита у базы (сцена/тоннели/спавн-враги СОХРАНЯЮТСЯ — это НЕ рестарт). **ПАНЕЛЬ СПАВНА ВРАГОВ** (справа, `drawSandboxSpawnPanel`): кнопка на КАЖДЫЙ из 13 типов (`SANDBOX_ENEMY_TYPES`, метки `ENEMY_RU`), клик → `_sandboxSpawnEnemy` создаёт 1 `new Enemy` в точке за 20 тайлов ЛЕВЕЕ города (`_sandboxSpawnTile`, маркер в мире); БЕЗ лимитов/задержек (перф-тест — можно спамить); счётчик живых на кнопке + всего в шапке. `storyMode=false` в старте (враги тикают). Клик — `sandboxSpawnClick` (роутер в game.js). Рендер — свои `drawSandboxLabels`/`drawSandboxHud`/`drawSandboxSpawnPanel`. Константы — `SANDBOX_*`. ⚠️ убрать перед релизом. | шапка файла |
| `tools/rig_editor.html` | Standalone-редактор рига (открывать через превью-сервер). Секции: детали (+под-список ассетов ВАРИАНТОВ модуля в карточке; ⚠️ `drill_screw` ИСКЛЮЧЁН — дублировал бы несомый щит) + «Бур-щит (составной)» (`buildBorerCards`: две карточки → ключи `borer:mount`/`borer:shield`; кнопка «превью» на карточке ставит `drill_screw` в слот → составной щит рисуется НА СБОРКЕ в главном канвасе через `drawBorerOnUnit`→игровой `_shieldBody` — `render_borer` в лоадере; обе ветки draw() кольцо/FK, щит по взгляду как `drawCarriedBorer`; экспорт в `module_assets.js` ГЛОБАЛЬНЫМИ ключами) + экспорт. ⚠️ свой загрузчик зависимостей (`document.write`) ДОЛЖЕН включать `i18n`+`lang_ru_*` ПЕРЕД `constants` (constants.js читает `STR` при загрузке) — иначе каскадный TDZ-краш загрузки файла юнита. | `design_editor.md` |
| `tools/worldstats.js` | DEV-инструмент СТАТИСТИКИ ГЕНЕРАЦИИ (НЕ в загрузчике; инжект `<script src="/tools/worldstats.js?cb=…">` в превью-eval). `WorldStats.run({seeds,gridCols,gridRows})`/`.print(opts)` — батч-прогон N сидов `new World(seed).generate()` → агрегаты: кол-ва (ожид/факт/срыв), соблюдение мин-дистанций (`*_MIN_DIST`, 2D по тору), ASCII-хитмап размещения+ресурсов, разрез по глубинным бэндам (разнообразие=интересность), ближайший-сосед (плотность), мёртвые зоны. Без UI — для обработки Claude. ⚠️ убрать перед релизом. | шапка файла |
| `tools/teaser.js` | DEV-тизер-директор (`window.Teaser`): рекламные ролики из РЕАЛЬНЫХ сцен. Геймплей → canvas-webm (`makeInit({clip,w,h,fps,q})`→`makeStep`→`makeMux`→скачать blob; муксер `tools/webm.js`); DOM-экраны меты/диска (canvas их НЕ ловит — foreignObject таинтит) → ЖИВАЯ запись экрана юзером по хоткеям (Shift+U — **живое воспроизведение CLIP3** на канвасе через `playClip` real-time rAF · Shift+M мета · Shift+J диск · Shift+X стоп; хоткеи — в `index.html`). Сцены — `SHOTS` (main) + `CLIP2` + **`CLIP3` «ОБНОВЛЕНИЕ»** (~29.5с, 10 сцен: рой/залежень/скверна/осада/взлом города/файрволл/курьер/дроны/форсаж+рентген/титр; у сцен `cap` — заголовок-плашка поверх кадра, `_drawCap` y=128 под виджетами). ⚠️ реликты-активки при установке стартуют С КУЛДАУНОМ — сцены сбрасывают (`g.blastCharge={cd:0}`); канал взлома копится в `cavern.hackT` (0..1), не в `activeHack.t`. ⚠️ правки `game.save` откатываются (`_restoreSave` — игра авто-сохраняется, иначе тест-значение персистнётся; `hold`/`makeInit` бампают `runs` — вернуть). Инструкции по записи — в ШАПКЕ файла. Убрать перед релизом. | шапка файла |
| `.claude/launch.json` | Конфиг превью-сервера. | — |

**Вне рабочего кода (игнорировать):** папки `legacy/` (отключённые заделы) и `reference/` (бинарники/устаревшие доки) — самодокументируются именем. ⚠️ Единственный неочевидный нюанс: `assets/round-scout/` — ЛЕГАСИ-ассеты ВНУТРИ активной `assets/`.

**Порядок подключения в `index.html`:** `i18n → lang_ru_constants → constants → lang_ru → [lang_ru_*] → save → meta → world → input → unit →
camera → city → firewall → cycle → unitdefs → anim → rig → inventory → upgrades → loot → effects → projectiles → falling → nav → dust →
visions → hints → intro → enemy → structures → radar → hud_layout → render_resource → render_module → render_world →
render_backdrop → render_falling → render_dust → render_loot → render_fx → render_parts → module_assets → render_unit →
render_ring → render_wheel → render_light → render_scan → render_visions → render_hint → render_enemy → render_structure →
render_alert → render_predict → render_radar → render_intro → render_city → render_firewall → render_inventory → render_upgrades → render_glitch → render_impulse → render_siege → render_stealth → render_jam → render_borer → render_cable → render_courier → render_jets → render_drones → render_artifact → render_scanners → render_hazards → render_traps → render_container → render_hack → render_actionbar → render_economy → render_cityturret → legik → tentacles → rigbridge → rig-core → rig-gun → hud →
ui_menu → meta_dom → codex_dom → game → datascan → nav_run → print → impulse → siege → stealth → jam → borers → artifact → artifacts_active → scanners → hazards → traps → containers → hack → actionbar → cable → courier → jets → drones → economy → cityturret → cannon → wildcity → ai → blight → sandbox`.
(`render_light` после `render_unit` — берёт `unitLightAnchor`; `render_scan` после `render_ring` —
берёт `ringModuleScreenPos`; CSS оверлеев (`meta_dom.css`/`codex_dom.css`) инжектит загрузчик;
`ai`/`print`/`impulse` после `game` — домешивают в прототип.) Классы/функции глобальные, без модулей.
⚠️ **i18n:** `lang_ru_constants` ПЕРЕД `constants` (его DATA читают `STR` при загрузке); прочие
пер-доменные `lang_ru_*.js` — после `constants`, до своих потребителей (грузятся в lang-группе
головы; сливаются `i18nDeepMerge`). Полный список файлов словаря — `spec_i18n.md` §3.

## 4. Спецификации (читать перед работой над системой)

| Документ | Что внутри |
|---|---|
| `spec_world.md` | Мир: геометрия/страты/палитра глубины, генерация, серверы, радиация, нестабильная порода и валуны, пещеры-сцены, тест-артефакты |
| `spec_enemies.md` | Город (таймер/кольца/drain), враги и мозги, спавн волн, калибровка копателя, скан врагов, смерть врагов |
| `spec_structures.md` | Печать структур: энергомодель, `STRUCT_DEFS`, логика/рендер, смерть врагов, дорожная карта фаз |
| `spec_unit.md` | Юнит: инварианты движения/бурения, статы, риг/блупринты, кольцо-рендер, спрайты деталей |
| `spec_render.md` | Слои кадра, кромки/свет/тени, скан-лучи, видения, глитч-помехи, ⚠️ перф-правила Canvas2D |
| `spec_codex.md` | База данных: диски/глоссарий, анимации, внутриигровой попап, источники данных, персист |
| `spec_meta.md` | МЕГА-ТОКЕНЫ: пересчёт забега, граф сети памяти, DOM-экран |
| `spec_ui.md` | Дизайн-система, режимы, меню, HUD, виджет города, сборка, апгрейды, интро, подсказки |
| `spec_i18n.md` | Локализация: ядро `STR`/`tr`, оптимизация под Canvas, неймспейс-дерево, дорожная карта миграции строк |
| `design_legs.md` | Полный свод локомоции IK-ног + тюнинг-ручки + превью в инвентаре |
| `design_editor.md` | Редактор рига и конвейер «редактор → игра» |
| `design_meta.md` / `design_cycles.md` | Планы будущих этапов (НЕ реализовано) |
| `gdd_mvp_v0.4.md` | GDD-снимок реализованного дизайна (§18 — что не сделано; v0.3 — устаревший, не ссылаться) |
| `registry_meta.md` | РЕЕСТР всех узлов сети памяти (91 шт, по секторам: id/имя/цена/кратко/флаги wip·story). Синхронить при правке `META_NODES`. |
| `registry_artifacts.md` | РЕЕСТР всех реликтов (26 шт, по слотам unit/city/drone: id/имя/tier/cls/апгрейд/эффект). Синхронить при правке `ARTIFACT_POOL`. |

**Экономика города (Батч 8, реликты слота city — `economy.js`/`render_economy.js`):** СИНТЕЗ (`synth_iron/organic/crystal`)
— доход в `upgrades.bank` на смену цикла (`_updateEconomy` детектит `cycle.n`, работает и в story); КОНВЕРТЕР (`converter`) — на цикл
плавит 2 ресурса в 3-й (рецепт-виджет, `_converterCycle`); ЭЛЕКТРОСТАНЦИЯ (`power_plant`) — после истечения таймера гибернации жжёт
органику из банка в `city.powerReserve` (фора перед уроном по контурам; `city._updateState` держит кольца пока резерв>0). Виджеты —
чипы верх-лево (`drawEconomyWidgets`, клик = переключение/тумблер, `_econClick`). Апгрейд-трек `costMul` (upgrades.js) индексирует редкость.
**РАСЩЕПЛЕНИЕ КРИСТАЛЛА** (узел меты `amb_split`, ТУТ ЖЕ в economy.js — не боевое, работает и в story): `_crystalSplitTick` (ДО `_powerPlantTick`)
— когда таймер гибернации истёк ВНЕ базы (`timer<=0.05`), ДО урона по контурам, если в банке есть кристалл и расщепитель включён → ОДИН кристалл
удаляется, ВОЗВРАЩАЯ часть таймера (`_crystalSplitReturn`=`SPLIT_RETURN_BASE`+трек города `splitreturn`·`STEP`); срабатывает лишь в момент
истечения (таймер снова >0 → один кристалл на продление). Лог `crystalSplit` + всплывающий текст `fx.text('+Nс')` у базы. Тумблер — чип
`РАСЩЕПИТЕЛЬ` (`_drawSplitChip`, гейт `metaHas('amb_split')`, клик `_crystalSplitToggle`→`game._splitRect`), стек с эконом-чипами верх-лево.

**Турели города (Батч 8, узлы `amb_turret/2/3` жёлтой ветки — `cityturret.js`/`render_cityturret.js`):** авто-оборона базы (не печатные
структуры). Кол-во по узлам (1 центр → 2 симметрично Л/П → 3 +центр), урон — трек `turretdmg` (metaNeed `amb_turret`). ⚠️ ОБЩИЙ медленный
поворот ствола ЧЕРЕЗ ВЕРХ — `aimOverTop`/`turretAimed` (structures.js), применён и к печатным turret/rail/mw (стреляют по наведению).

## 5. Архитектура: один тип сущностей — один файл

**Не наращивай `game.js`** — новую подсистему клади в свой файл (логика отдельно, рендер —
свободные функции `draw*` в `render_*.js`) и допиши `<script>` в правильное место загрузчика.
Детали системы — в её `spec_*.md`; сюда — только строка таблицы + ссылка.

## 6. Правила кода

- **Логика отдельно от рендера.** `update(dt,…)` без Canvas; `draw(…)` не меняет состояние.
- **Ввод — по `e.code`** (раскладко-независимо), не по `e.key`.
- **rAF, `dt` в секундах, клампить** (`dt = min(dt, 0.05)`); скорости — в ед/сек × dt.
- **Никаких магических чисел по коду** — именованные константы в `constants.js`.
- **UI-тексты — по-русски.** Идентификаторы — латиницей. НОВЫЕ UI-строки — в словарь `lang_ru.js`
  (`STR.<домен>.<ключ>`, параметризованные — функции), не хардкодом в рендере; механизм — `i18n.js` / `spec_i18n.md`.
  (Уже мигрированы `menu`/`gameover`; остальной UI — хардкод, мигрируется инкрементально по дорожной карте.)
- **Комментарии — только ПОЧЕМУ** (инвариант, причина), не ЧТО. По умолчанию — без комментариев.
- **Без преждевременных абстракций**, мёртвого кода, полуреализаций.
- Маленькие функции, явные имена, без скрытых сайд-эффектов.

## 7. Чек-лист перед «готово»

- [ ] В консоли нет ошибок; поведение проверено в браузере (скриншот).
- [ ] Трудноуловимая логика проверена eval/пошаговой симуляцией.
- [ ] Новый код в правильном файле (раздел 5); скрипт в загрузчике на правильном месте.
- [ ] Обновлена спека системы (`spec_*.md`); менялась структура — обновлён CLAUDE.md.

## 8. Эффективная работа со мной (Claude Code)

- Дроби задачу на шаги, проверяй инкрементально (скриншот/eval после шага).
- Держи файлы маленькими; состояние — наблюдаемым (`window.game`).
- Неочевидные решения фиксируй в спеке системы, а не в чате.
- Многошаговую работу веди задачами (TaskCreate); широкое исследование делегируй сабагентам.
